# Результаты: квантизация переиспользуемого кэша в диффузионной LM

Сводка всего измеренного, для продолжения в новой сессии и для письма.
Рядом: `HANDOFF.md` — состояние и список дел, `RESEARCH_PLAN.md` — замысел и
порядок, `RUNBOOK.md` — как запускать.

Всё ниже — на реальных весах. **Воспроизводимость разная у разных измерений, и
это надо знать до чтения таблиц.** Решения воспроизводятся: два независимых
прогона GSM8K на одной конфигурации совпали во всех сорока контрольных точках.
Тензорные нормы — нет: между прогонами при меняющейся загрузке карты они
расходятся на 2–5% (§4.1a), поэтому в них читается порядок эффектов, а не третья
цифра.

---

## 0. Первые пять минут в новой сессии

**Три версии transformers, и в venv лежит неподходящая ни одной.**

| | версия | как |
|---|---|---|
| LLaDA2.0 | 4.57.1 | `PYTHONPATH=$HOME/tf457` |
| LLaDA-1.5 | 4.46.3 | `PYTHONPATH=$HOME/tf446` |
| venv (`~/.local`) | **5.5.4** | не подходит ни одной; попала как побочный эффект `pip install accelerate` |

Проверка версии стоит до импорта модельного кода и падает внятно.

**Диск** 157 ГБ свободно, **оперативной** 1.8 ТБ. Тесно только с картами:
модель в bf16 требует ~32 ГБ видеопамяти, и узел обычно занят.

```bash
nvidia-smi --query-gpu=index,memory.free,utilization.gpu --format=csv,noheader,nounits | awk -F', ' '$2>34000 && $3<70 {print "карта", $1}'
```

Годится строка, где памяти больше 34000 MiB **и** загрузка ниже 70%: занятая
карта считает втридцатеро медленнее. Диагностика (`check_block_cache`,
`measure_drift`, `trace_window_path`) идёт на процессоре без потерь; оценка на
GSM8K — только на карте.

`accelerate` **не ставить**: он подтянул transformers 5.5.4 и сломал загрузку.
Без него веса едут через оперативную, чего у вас в избытке.

---

## 0a. Сводка на 22 сентября: две части

Проверено на логические ошибки 22 сентября; ниже — исправленные формулировки. Подробности и
все числа — в §5 (подразделы «Два бита на ответах», «Перенос γ», «Почему QuaRot не
спасается», «Отказ QuaRot объясняется геометрией», «Fast-dLLM-v2», «Оба пункта при
декодировании по порогу»).

### Часть 1. Сравнение LLaDA-1.5, LLaDA2.0-mini и Fast-dLLM-v2

| | LLaDA-1.5 | LLaDA2.0-mini | Fast-dLLM-v2 |
|---|---|---|---|
| архитектура | плотная 8B | MoE 16B, 1.4B активных | плотная 7B (Qwen2.5) |
| внимание | двунаправленное | блочно-каузальное | блочно-каузальное |
| параметр, пишущий фиксированный вектор в ключи | **нет** (конфиг: `include_qkv_bias: false`, `attention_layer_norm: false`) | γ QK-Norm | bias в `k_proj` |
| RoPE | полный | на половину головы | полный |

Точность, все три под порогом уверенности 0.95, эталоны 89.5 / 93.5 / 82.5 (22 сентября,
`p1_l15.sh` доводит LLaDA-1.5 до той же обвязки, что и две другие):

| ключи | LLaDA-1.5, 4 / 3 / 2 бита | LLaDA2.0-mini, 4 / 3 / 2 | Fast-dLLM-v2, 4 / 3 / 2 |
|---|---|---|---|
| по каналам | 86.5 / 85.5 / 88.5 | 92.0 / 93.0 / 90.0 | 74.0 / 58.0 / 18.0 |
| по токенам | 83.5 / 87.5 / 87.5 | 91.5 / 59.5 / 2.0 | 0.0 / 0.0 / 0.0 |
| QuaRot | 85.5 / 85.5 / 86.5 | 92.5 / 89.0 / 2.5 | 1.5 / 0.5 / 1.0 |

У LLaDA-1.5 все девять клеток лежат в 83.5–88.5 при эталоне 89.5, и **ни одна из них не
отличается от 16 бит** по парному тесту с поправкой на девять сравнений (23 сентября):
номинальные `p` = 0.012, 0.057, 0.057, 0.096, 0.180, 0.210, 0.424, 0.424, 0.727, а по Холму
наименьшее сравнивается с 0.05/9 = 0.0056. Единственное номинально значимое падение — 4 бита
по токенам (16 : 4), и по разрядности это не монотонно: обе клетки при 2 битах спокойнее
(`p` = 0.727 и 0.424), что для эффекта квантования невозможно, а для шума нормально. То есть у модели без параметра в пути ключа не решает ни ось, ни
ширина вплоть до двух бит, и это утверждение теперь опирается на тест, а не на отсутствие
монотонности.

Тензор, 2 бита (нецентрированная ошибка логитов): цена неверной оси 1.3× / 4.9× / 2.3×,
crest ключей 6.8 / 11.5 / 7.4, |cos(q, k)| 0.19 / 0.09 / 0.18, ошибка QuaRot / изотропный
множитель 0.042 / 0.035 / 0.041 (LLaDA-1.5 / LLaDA2.0-mini / Fast-dLLM-v2).

**Тезисы.**

1. **Будет ли неверная ось ключей стоить, решает архитектура — есть ли параметр, который пишет
   фиксированный вектор в каждый ключ; с какой разрядности — архитектура вместе с числом
   бит.** У LLaDA-1.5 такого параметра нет, и ось не важна даже при 2 битах; у LLaDA2.0-mini
   неверная ось решает с 3 бит, у Fast-dLLM-v2 — с 4.
2. **Сильнее всего фиксированная структура ключей у модели с QK-Norm (цена оси 4.9×), но
   раньше разваливается модель с bias.** Сила структуры не предсказывает разрядность развала
   (тезис 4).
3. **Ошибка поворотной схемы ≈ (длина квантуемого ключа) × (изотропный множитель
   ‖q‖‖k‖ / |q·k|).** Отношение держится на всех трёх моделях в пределах 0.035–0.042 — для
   нецентрированной ошибки; для центрированной не держится. У LLaDA2.0-mini запросы и ключи
   почти ортогональны, поэтому второй множитель у неё худший.
4. **Разрыв чувствительности — это два измеренных множителя, а не один порог.** При 4 битах
   по токенам центрированная ошибка у LLaDA2.0-mini и Fast-dLLM-v2 одинакова (0.351 и
   0.346), точность — 91.5 и 0.0.
   * *Длина генерации ни при чём* (`p1_len.sh`): при 512 токенах вместо 2048 у Fast-dLLM-v2
     воспроизводится вся сетка — эталон 82.0 (против 82.5), по каналам 74.0 и 58.0 (те же),
     по токенам 0.0.
   * *Разброс логитов ни при чём по построению:* центрированная ошибка **и есть** ошибка в
     единицах разброса (тождество, закреплено тестом).
   * *Первый множитель — геометрия внимания.* Ту же ошибку Fast-dLLM-v2 превращает вдвое
     большее движение распределения: KL 0.470 против 0.222. Её внимание самое острое из
     трёх — 24.2 эффективных ключа против 32.2 у LLaDA2.0-mini и 55.9 у LLaDA-1.5.
   * *Второй множитель — терпимость к движению, порядок величины* (23 сентября, `t4c.sh`).
     Бесструктурный гауссов шум на хранимых ключах при 16 битах, доза взята по измеренной
     кривой:

     | движение | LLaDA2.0-mini | Fast-dLLM-v2 |
     |---|---|---|
     | KL 0.22 | 90.0 | 3.0 |
     | KL 0.47 | 77.0 | 0.0 |

   * *Составляющая, общая для всех хранимых ключей, ровно невидима* (23 сентября,
     `t4e.sh`). Шум в режиме «один вектор на канал на запись, ко всем позициям» даёт
     центрированную ошибку логитов 0.000 и KL 0.000 при σ = 0.05, 0.10, 0.20 и 0.40 —
     доза под KL 0.22 недостижима в принципе. Причина точная: одинаковая добавка ко всем
     ключам даёт один констант на запрос, а softmax читает логиты относительно их
     собственного среднего. Планировавшийся прогон «скоррелированный шум при равном
     движении» поэтому не нужен — гипотеза о корреляции доказана в предельной форме, и
     мягкость ошибки квантователя объясняется тем, что она частично общая.

     Два следствия. Для части 2: у bias **до** RoPE составляющая идеально общая и потому
     бесплатна, а губит та часть, которая общей быть перестаёт — после RoPE bias вращается
     вместе с позицией. Для методики: ключи, испорченные на 40% по RMS, не двигают внимание
     вообще, так что `‖K − K̂‖` измеряет не то, что важно.
   * *Два возражения к этому, измерены 24 сентября* (`obj.sh`). **Сток внимания —
     возражение снято:** у обеих моделей масса на первых четырёх доступных ключах 0.06, а
     эффективное число ключей без стока 25.9 против 24.2 у Fast-dLLM-v2 и 32.6 против 32.2 у
     LLaDA2.0-mini. Концентрация не стоковая, и обратный знак у KVTuner (разреженные головы
     устойчивее) относится к sink-головам, а не к нашей величине. **Смещение Йенсена —
     возражение подтвердилось:** шум с нулевым средним умножает вес возмущённых ключей на
     `exp(σ²‖q‖²·rms²/2d)`, и при наших дозах это 35.3% разброса логитов у Fast-dLLM-v2
     против 12.0% у LLaDA2.0-mini при одном и том же движении KL 0.22. Множитель общий для
     префикса, но ключи текущего блока считаются заново и его не несут, поэтому внимание
     систематически перетекало на префикс — сильнее у той модели, где мы получили аномалию.
     Тензорный зонд этого не видел: в нём шум получают все ключи, множитель сокращается, и
     калибровка «σ → KL» мерила только случайную часть.

     Все три модели, `obj.sh`:

     | | член Йенсена на единицу дисперсии | масса на первых 4 ключах | эфф. ключей со стоком / без |
     |---|---|---|---|
     | LLaDA-1.5 | 0.85 | 0.015 | 55.9 / 55.0 |
     | LLaDA2.0-mini | 15.4 | 0.061 | 32.2 / 32.6 |
     | Fast-dLLM-v2 | 556.1 | 0.062 | 24.2 / 25.9 |

     Разброс члена Йенсена в 650 раз — это не свойство нашего контроля, а свойство модели:
     он равен `‖q‖²·rms(k)²/2d`, то есть масштабу запросов и ключей относительно разброса
     логитов. Отсюда отдельное утверждение, которое **остаётся** и которое сильнее снятого:
     внимание Fast-dLLM-v2 необычайно уязвимо к любому возмущению ключей с нулевым средним —
     не только к нашему шуму, но и к стохастическому округлению, случайным проекциям и любой
     схеме, добавляющей в кэш несмещённую случайность. Это проверяемое предсказание о
     разворачивании, а не про наш эксперимент.
   * *Но измеренный сдвиг оказался ничтожным, и возражение отпало* (24 сентября, `obj2.sh`).
     Замкнутая формула — верхняя оценка, а не эффект: она предполагает самоусреднение по
     многим ключам, а при концентрированном внимании сумму держат несколько верхних, и для
     горстки раздутие — один случайный отсчёт, а не его среднее. Зонд с шумом только на
     префиксе (последние 32 позиции чистые, как текущий блок при генерации) даёт реализованный
     сдвиг массы внимания:

     | | доза | масса на текущем блоке до → после | сдвиг |
     |---|---|---|---|
     | Fast-dLLM-v2 | σ = 0.0393 (KL 0.22) | 0.029 → 0.027 | −0.2 пункта |
     | Fast-dLLM-v2 | σ = 0.0614 (KL 0.47) | 0.029 → 0.027 | −0.2 |
     | LLaDA2.0-mini | σ = 0.1323 (KL 0.22) | 0.035 → 0.031 | −0.4 |
     | LLaDA2.0-mini | σ = 0.1961 (KL 0.47) | 0.035 → 0.026 | −0.9 |

     Формула обещала 35% разброса логитов у Fast-dLLM-v2, масса сдвинулась на 0.2 пункта. И
     сдвиг **больше** у той модели, которая выживает, то есть направление обратное нужному для
     объяснения. Третья причина, обнаруженная тем же замером: текущий блок держит всего 3%
     массы внимания, раздувать префикс некуда. Попутно: KL этого эффекта не видит вовсе — при
     шуме только на префиксе он даже падает пропорционально доле зашумлённых ключей.
   * *Форма ошибки тоже важна — и мягче, чем шум.* Утверждение снималось утром 24 сентября и
     **восстановлено к вечеру**: единственное известное конкурирующее объяснение проверено
     прямым измерением и не работает. На LLaDA2.0-mini шум и квантование при равном движении
     взаимозаменяемы (90.0 против 91.5), на Fast-dLLM-v2 нет: шум при KL 0.22 даёт 3.0, её
     квантователи рядом с этим движением — 58.0 и 43.5. На LLaDA2.0-mini шум и квантование при
     равном движении взаимозаменяемы (90.0 против 91.5 при KL 0.22). На Fast-dLLM-v2 нет:
     шум при 0.22 даёт 3.0, а её собственные квантователи рядом с этим движением — 58.0
     (KL 0.117) и 43.5 (KL 0.348). То есть **ошибка квантователя заметно безобиднее
     случайной той же величины**, и прежняя формулировка «разрыв не в форме ошибки» неверна:
     она опиралась на сравнение двух нулей (шум 0.0 против квантования 0.0 при KL 0.47).
   * *KL не достаточная величина, и это видно трижды.* (1) Равное движение не означает
     равную точность на клетках с вмешательством (`t4d.sh`): три клетки Fast-dLLM-v2 с KL
     0.348, 0.351 и 0.355 дают 43.5, 1.5 и 28.0. (2) Разницу между точным вычитанием и
     средним при 2 битах мера не видит вовсе; её видит crest хранимых ключей — 6.8 против
     9.1. (3) Движение поворота вообще не размазано: при 4 битах KL 3.24 в слое 0 и 5.62 в
     слое 27 против 0.02 в остальных 26 (`kl_profile.py`), то есть среднее по слоям описывает
     не тот объект. Проверено вмешательством — см. ниже.
5. **Устаревание и разрядность складываются сверхаддитивно при обоих декодерах, но при
   пороге для этого нужно окно подлиннее.** LLaDA2.0-mini, цена 2 бит при обновлении окна
   каждый шаг / раз в 2 / раз в 4: при фиксированном расписании 7.0 → 24.5 → 31.0, при
   пороге 9.0 → 10.5 → **18.5** (16 бит 86.0, 2 бита 67.5; добавлено 23 сентября,
   `p1_stale.sh`). Прежняя формулировка «при пороге почти пропадает» опиралась на две точки
   из трёх. Часть цены при пороге по-прежнему уходит в число шагов (8.1 → 14.5 на блок), а у
   Fast-dLLM-v2 штатное переиспользование блока бесплатно само по себе (84.0 против 82.5,
   незначимо), так что складывать там нечего.

**Движение внимания против точности** (KL между истинным и квантованным распределением,
`t4.sh`, тензор; точность — те же клетки на ответах). Внутри каждой модели порядок почти
монотонный, между моделями общего порога нет, а на клетках с вмешательством мера ломается
(см. тезис 4):

| LLaDA2.0-mini | KL | точность | | Fast-dLLM-v2 | KL | точность |
|---|---|---|---|---|---|---|
| 4 бита по каналам | 0.011 | 92.0 | | 4 бита по каналам | 0.056 | 74.0 |
| 3 бита по каналам | 0.039 | 93.0 | | 3 бита по каналам | 0.117 | 58.0 |
| 4 бита QuaRot | 0.067 | 92.5 | | 4 бита QuaRot | 0.351 | 1.5 |
| 2 бита по каналам | 0.187 | 90.0 | | 2 бита по каналам | 0.415 | 18.0 |
| 4 бита по токенам | 0.222 | 91.5 | | 4 бита по токенам | 0.470 | 0.0 |
| 3 бита QuaRot | 0.295 | 89.0 | | 3 бита QuaRot | 0.791 | 0.5 |
| 3 бита по токенам | 0.802 | 59.5 | | 3 бита по токенам | 0.937 | 0.0 |
| 2 бита QuaRot | 1.425 | 2.5 | | 2 бита QuaRot | 1.670 | 1.0 |
| 2 бита по токенам | 1.955 | 2.0 | | 2 бита по токенам | 1.582 | 0.0 |

### Развал на Fast-dLLM-v2 — это два слоя из двадцати восьми

Профиль движения внимания по слоям (`kl_profile.py` на `ke_afd`) показал, что при 4 битах
QuaRot ущерб не размазан: KL 3.24 в слое 0 и 5.62 в слое 27 против 0.02 в остальных 26,
девяносто процентов слоёв под 0.10. Отсюда предсказание: если оставить эти два слоя в 16
битах, развал должен уйти. Проверено (23 сентября, `edge.sh`):

| Fast-dLLM-v2, эталон 82.5 | как есть | слои 0 и 27 точные | McNemar | слои 13 и 14 точные |
|---|---|---|---|---|
| 4 бита, QuaRot | 1.5 | **83.0** | 164 : 1 | 0.5 |
| 4 бита, по токенам | 0.0 | **83.5** | 167 : 0 | — |
| 2 бита, по каналам | 18.0 | **51.5** | 74 : 7 | — |

Все три `p < 10⁻¹⁵`. Контроль обязателен и он отрицательный: те же 7% кэша, отданные двум
**средним** слоям, не дают ничего (0.5 против 1.5). Значит, спасают не 7% точных данных, а
конкретные слои — первый и последний.

**Что здесь своё — сужено 23 сентября по литературе.** Концентрация ущерба в слоях 0 и 27 у
Qwen2.5 опубликована дважды: KVTuner (2502.04420, ICML 2025, рис. 16 — при 8 битах ключей
высокая ошибка только у слоёв 0 и 27) и 2606.09864, где сделана та же абляция с
восстановлением критических слоёв в FP16 (слой 0 — 68.8%, слой 27 — 20.8%, остальные
≤ 12.5%). Прежняя формулировка «своё здесь — то, что слои предсказаны измерением» снята.
Остаётся: у KVTuner концентрация размазывается при 4 битах (добавляются слои 3, 7, 10, 13,
23), **а под поворотом она при 4 битах сохраняется** — это противоречие с ближайшей работой,
а не её повтор; порядок «последний слой хуже первого» (5.62 против 3.24); отрицательный
контроль на средних слоях; KL именно распределения внимания как критерий (у KV-COBRA
2609.24298 — верхняя оценка, у KVTuner — L1). Противоположное утверждение есть у OptR
(2608.02691): при повороте улучшения распределены по слоям широко.

**Занято и в практике, не только в статьях** (проверено 23 сентября). В vLLM функция
`get_boundary_skip_layers` (TurboQuant, PR 38479, мерджено 29 марта 2026) **по умолчанию**
исключает первые два и последние два слоя внимания из сжатия кэша, с измеренным
обоснованием в докстринге: «без этого GSM8K падает примерно на 30 пунктов на Qwen3-4B». Тот
же приём измерен в форке llama.cpp `turboquant_plus` (политика LA-V7: первые 2 и последние 2
слоя в q8_0, «восстанавливает 37–91% потери качества») и в открытом PR TensorRT-LLM (первые
3 слоя в FP8: GPQA 0.5547 против 0.5295). U-образная зависимость чувствительности по глубине
опубликована в RateQuant (2605.06675) и PM-KVQ (2505.18610). Публичный блог, делающий
профиль через KL логитов, прямо пишет, что статические правила «держать первый и последний
слой точными» «угадывают направление, но не величину».

**Вывод: как рецепт это не вклад, а воспроизведение практики, работающей по умолчанию.**
Остаётся ценность проверки нашей диагностики: профиль движения внимания предсказал то, что в
vLLM нашли перебором, и сделал это без поиска, градиентов и решателя — в отличие от KVTuner
(поиск), ModelOpt (решатель) и PM-KVQ (целочисленное программирование). Плюс сохранение
концентрации под поворотом при 4 битах, порядок «последний хуже первого» и отрицательный
контроль на средних слоях.

**Пересчёт на бюджет: как рецепт это невыгодно.** Два слоя из 28 в 16 битах — не
бесплатно. Средняя разрядность кэша при 2 битах с точными слоями 0 и 27 равна ровно 3.0
бита, а однородные 3 бита по каналам дают 58.0 против 51.5, и те же 3 бита с bias вне кэша —
80.5. При 4 битах с точными крайними слоями средняя разрядность 4.86, и 83.0 проигрывают
4 битам по каналам с bias вне кэша — 85.0 при бюджете 4.0. То есть при равном или меньшем
бюджете удаление параметрического выброса лучше во всех проверенных клетках. **Ценность
этого результата диагностическая, а не операционная:** он причинно локализует ущерб (и тем
оправдывает профиль движения по слоям как меру), но как рецепт хранения уступает.

**Не проверено, сознательно.** (1) Хватает ли одного слоя: движение в слое 27 (5.62) заметно
больше, чем в слое 0 (3.24), и если достаточно последнего, цена падает с 7% до 3.5%. Это два
прогона по двадцать минут, флаг `--kv-skip-layers` уже есть. (2) Переносится ли рецепт на
другие модели: измерено на одной, и у LLaDA2.0-mini в пути кэша такого флага пока нет, так
что «первый и последний слой» — свойство Fast-dLLM-v2, а не установленная общность. (3)
Работает ли это при отборе ключей, как у коллег, а не на плотном кэше.

*Оговорки:* разные длины генерации и протоколы — между моделями сравниваются контрасты внутри
каждой, а не абсолютные числа. LLaDA-1.5 отличается от двух других не только отсутствием
параметра (двунаправленная, фиксированное расписание), поэтому она — наблюдение, а не
контроль. Одна задача — GSM8K, 200 примеров.

### Часть 2. Влияние QK-Norm и параметрических выбросов ключей

**Результаты.**

- **γ создаёт пики ключей (LLaDA2.0-mini):** перенос неравномерной части γ в норму запросов
  снижает crest 11.5 → 6.3 во всех 20 слоях — до уровня LLaDA-1.5.
- **Перенос γ точен и снимает развал:** сдвиг внимания 0, 200/200 одинаковых ответов, 16 бит
  90.5 = 90.5; по токенам при 2 битах 1.5 → 57.5 (113 : 1), при пороге 2.0 → 73.0 (144 : 2);
  доза: crest 11.5 / 7.7 / 6.3 → 1.5 / 7.5 / 57.5.
- **Закон по слоям для γ:** ρ = +0.87 при 4 битах, с поправкой на глубину +0.79.
- **Остаток после переноса — пол в громких каналах:** их вклад в ошибку логитов от дозы не
  падает (0.199 → 0.169), остальных — падает втрое.
- **Bias на Fast-dLLM-v2 — тот же механизм:** хранение ключей без bias точно (199/200 при
  16 битах) и спасает все схемы — 4 бита: по каналам 74.0 → 85.0, по токенам 0.0 → 84.0
  (168 : 0), QuaRot 1.5 → 79.0 (157 : 2); 3 бита: 58.0 → 80.5, 0.0 → 76.5, 0.5 → 72.5; 2 бита
  по каналам 18.0 → 43.5.
- **Выбор формы удаления bias зависит от бюджета** (уточнено 23 сентября). При 4 и 3 битах
  точное вычитание `RoPE(b)` и вычитание среднего по позициям неразличимы: 84.0 против 83.0
  (4 бита по токенам, p = 0.80), 85.0 против 80.5 (4 бита по каналам, p = 0.15), 79.0 против
  83.0 (QuaRot, p = 0.20), 80.5 против 79.5 (3 бита по каналам, p = 0.87), 76.5 против 70.0
  (3 бита по токенам, p = 0.079). **При 2 битах точная форма значимо лучше: 43.5 против 28.0,
  45 : 14, `p = 2·10⁻⁵`** — среднее по позициям не может представить слагаемое, которое
  вращается вместе с позицией, и остаток остаётся в хранимом тензоре (пик-фактор 9.1 против
  6.8). Сам приём — чужой; наше здесь — измерение того, что он делает с per-token схемой и
  поворотом в блочной диффузионной модели, и граница по бюджету между двумя формами.
- **При 2 битах остаток сидит в ключах, а не в значениях:** только V в 2 битах — 85.0 при
  эталоне 82.5 (бесплатно), только K с bias вне кэша — 56.0; 64 : 6, `p = 2.4·10⁻¹³`.
- **Закона по слоям для bias нет** (ρ ≈ 0); он действует через слои, где составляет 91–97%
  ключа. Без bias цена оси ниже в 27 из 28 слоёв.

**Тезисы.**

1. **Фиксированного выброса ключей, созданного параметром, достаточно для развала
   по-токенной схемы кэша.** Показано точным вмешательством на двух моделях с двумя
   параметрами: убрав выброс, получаем обратно почти всю точность при γ (57.5 из 84.5; при
   пороге 73.0 из 90.0) и всю при bias (84.0 при эталоне 82.5). QK-Norm — один из двух
   проверенных источников.
2. **У γ цена оси следует разбросу множителя по слоям (ρ = +0.87), у bias такой зависимости
   нет.** Это сравнение двух моделей, а не двух параметров: модели различаются и
   архитектурой, и декодером, и длиной генерации. Объяснение «множитель задаёт выброс
   напрямую, слагаемое — лишь относительно остального ключа» — гипотеза.
3. **Поворот — из оценки части 1 (длина × геометрия).** Вычитание bias уменьшает длину
   хранимого ключа, не трогая запросы, — поворот спасается (1.5 → 79.0, 0.5 → 72.5); **для
   bias показано, что отказ поворота вызван тем же выбросом.** Перенос γ уменьшает длину
   ключа, но раздувает запросы и портит геометрию — поворот не спасается (ошибка ×1.5), а у
   по-токенной схемы остаётся пол в громких каналах. **Для γ этим вмешательством нельзя
   проверить, вызван ли отказ поворота выбросом.**
4. **Практическое правило.** Если в модели есть QK-Norm или bias в проекции ключей, ключи
   квантовать по каналам. Разрядность, с которой опасны другие схемы, зависит от модели:
   по-токенная — с 3 бит на LLaDA2.0-mini и с 4 на Fast-dLLM-v2, поворот — с 2 и с 4 бит.
   Если есть bias, хранить ключи без него: точно и бесплатно (Fast-dLLM-v2, плотный кэш,
   4 бита: 74.0 → 85.0). Приём известен (pre-bias в KVQuant*, «smooth K» в SageAttention), и
   вычитание среднего по SageAttention работает не хуже точного `RoPE(b)`, так что брать надо
   его — со ссылкой.

**Не использовать как доказательство про γ:** чтение ключей до нормы (цена оси падает в 2.3
раза) — это эффект всей нормы, включая деление на RMS; доказательство про γ — перенос.

**Позиция в литературе.** Развал поворота и по-токенной схемы на Qwen3 и Qwen2.5 при 2 битах
наблюдали (KVLinC, OptR, OScaR), не объясняя; γ QK-RMSNorm как источник выброса — HiFA4
(один слой Qwen3-8B, без вмешательства); bias и квантование до него — «Quantization Hurts
Reasoning?» (KVQuant*, как рецепт, до RoPE, «для QuaRot неприменимо»); вычитание общей для
ключей составляющей перед по-токенной шкалой — SageAttention («smooth K»). Своё: причинные
вмешательства на двух механизмах, закон по слоям для γ, объяснение поворота через геометрию
q·k, различие «перенос против удаления» и то, что удаление общей для ключей составляющей
**на хранении** оживляет по-токенную схему и поворот в блочной диффузионной модели (у
SageAttention это для вычисления внимания, у KVQuant* — до RoPE и «для QuaRot неприменимо»).
Точная форма `RoPE(b)` при этом не лучше их среднего, поэтому методом её не заявляем.

**Не закрыто:** вторая модель именно с QK-Norm (DreamReasoner-8B, основа Qwen3); остаток
Fast-dLLM-v2 при 2 битах — установлено, что он в ключах (значения бесплатны), но не
объяснён; вторая задача.

## 1. Тезис, и что с ним стало

**Что определяет допустимую разрядность переиспользуемого кэша в
блочно-диффузионной MoE, и куда девать биты.**

#### Сначала — поле кэширования dLLM, разобранное 7 сентября

Раздел прежде строился вокруг квантизации и не описывал того, откуда кэш вообще
берётся. А это большое и активное поле, и без него позиция работы не читается.

**Корень задачи:** классический KV-кэш **несовместим** с диффузионной моделью,
потому что внимание двунаправленное — представление любого токена зависит от
масок, которые ещё изменятся. Всё ниже — способы обойти эту несовместимость.

| работа | механизм |
|---|---|
| [**dKV-Cache**](https://arxiv.org/html/2505.15781) | первый кэш для dLLM; **delay caching** — токен кэшируется не сразу, а когда его представление устоялось |
| [**Fast-dLLM**](https://arxiv.org/html/2505.22618v1) | блочный **приближённый** кэш; **DualCache** держит и префикс, **и суффикс** — хвост из масок после текущего блока |
| [**dLLM-Cache**](https://arxiv.org/html/2506.06295) | длинный интервал для промпта плюс **V-verify** для ответа: обновлять записи, чей вектор значений сдвинулся |
| [**d2Cache**](https://arxiv.org/html/2509.23094) | двойное адаптивное кэширование |
| [**Sparse-dLLM**](https://arxiv.org/pdf/2508.02558), [**Mask Tokens as Prophet**](https://arxiv.org/pdf/2510.09309) | **вытеснение**, а не обновление — третья ось |
| [**FlashDLM**](https://arxiv.org/pdf/2505.21467) | кэш вместе с guided diffusion |
| [**dInfer**](https://github.com/inclusionAI/dInfer) | фреймворк вывода от авторов LLaDA2.0; две ручки — `cache_update_iter` (интервал) и `cache_type` (срез) |
| [**LaCache**](https://arxiv.org/html/2607.16339v2) | **точный** кэш для блочных моделей |
| [**Polestar**](https://arxiv.org/html/2607.14107) | селективное обновление по дрейфу представления |

**Два вывода, которые из этого следуют для нашей рамки.**

**Первый: устаревание — центральная проблема поля, а не наша выдумка.** Девять
работ существуют ровно потому, что на двунаправленной модели переиспользование
приближённо по построению. Fast-dLLM так и называет свой механизм — *approximate*
KV cache.

**Второй: на блочно-каузальной модели этой проблемы нет.** Префикс от текущего
блока не зависит, значит держать его точно; LaCache на этом и построена, а
`cache_update_iter` у dInfer выключен по умолчанию. Поэтому наши измерения
устаревания адресованы двунаправленной семье — LLaDA-1.5, LLaDA-8B, Dream, — а не
той модели, на которой их удобнее всего было снимать (§5).

**И одна ось, которой у нас нет вовсе: кэширование суффикса.** Наш стор держит
только префикс, а хвост после текущего блока мы не вычисляем. На блочной модели
это верно — блок до него не достаёт. На двунаправленной **достаёт**, и DualCache
у Fast-dLLM кэширует его намеренно. То есть половина их механизма нами не
измерена.

#### У двух наших моделей разное происхождение политики кэша

**Проверено 8 сентября по репозиториям.** dInfer — фреймворк авторов
**LLaDA2.0**, и `fix_iter` описывает политику MoE-модели. На LLaDA-1.5 это не
переносится, и не потому, что там политика другая, а потому, что её нет.

| | эталонная политика | что говорит поставляемый код |
|---|---|---|
| **LLaDA2.0** (MoE, block-causal) | **есть** — dInfer | `cache_type` ∈ {prefix, dual, prompt}; `cache_update_iter` по умолчанию `None` |
| **LLaDA-1.5** (dense, bidirectional) | **нет** | `use_cache: false` в `config.json`; `generate()` вызывает модель без кэша; в `LLaDAModel.forward` стоит `assert (past_key_values is None and not use_cache), "The kvcache is not suppotred for MDM."` |

Инфраструктура в `LLaDABlock.attention` есть — `present = (k, v) if use_cache
else None`, — и `forward` протягивает `layer_past`, но верхний ассерт её
запрещает. То есть у LLaDA-1.5 кэш не «выключен по умолчанию», а **объявлен
неподдерживаемым**.

**Из этого следует асимметрия, обратная удобной.** Модель, на которой
устаревание структурно нулевое (2.0), — единственная с эталонной политикой.
Модель, на которой устаревание реально и стоит 1.75× (1.5), — та, у которой
эталона нет вовсе.

Три следствия для рамки:

1. **На 1.5 наш `every_n` ни с чем не расходится.** Расходиться не с чем:
   вендор кэш запрещает. Это законная исследовательская ручка, а не самодеятельная
   замена чужой.
2. **Точка сравнения на 1.5 — сторонние работы**, и ни одна не запускалась на
   самой 1.5 (проверено 8 сентября по полным текстам): dKV-Cache и dLLM-Cache
   меряют на **LLaDA-8B-Instruct** и Dream, Fast-dLLM — на «LLaDA-Instruct» и
   Dream-Base. LLaDA-1.5 у dLLM-Cache есть только в списке литературы.
   Архитектурно 1.5 — это тот же 8B-Instruct после VRPO-выравнивания, поэтому
   механизмы переносятся, но **измерений на 1.5 нет ни у кого**.
3. **Всякий кэш на 1.5 — конструкция третьих лиц**, включая наш. Поэтому
   сравнение «1.5 против 2.0 под одной политикой» держится на нашей реализации с
   обеих сторон, и это надо заявлять, а не подразумевать (§2.5a).

**Ось «когда» на двунаправленной модели занята, и вот её рабочие точки.**

| работа | модель | интервал обновления |
|---|---|---|
| **dKV-Cache** | LLaDA-8B-Instruct, Dream-7B | «every N steps the stored cache would be discarded and refreshed», **N = 4, 8, 16** |
| **dLLM-Cache** | LLaDA-8B Base/Instruct, Dream-7B Base/Instruct | **раздельные часы**: промпт `K_p` = 25–100 (среднее 66), ответ `K_r` = 1–7 (среднее 5); плюс ρ = 0.25 записей отбирается V-verify |
| **Fast-dLLM** | LLaDA-Instruct, Dream-Base | интервала нет — обновление **раз на блок**, после всех шагов блока |

Два следствия, оба важные:

**Наш `every_n` — это в точности их ручка**, а не наша выдумка, и наша развёртка
по возрастам 1–16 накрывает весь опубликованный диапазон: N = 4/8/16 у
dKV-Cache и `K_r` = 1–7 у dLLM-Cache лежат внутри.

**А вот раздельных часов у нас нет.** dLLM-Cache стареет промпт в 10–20 раз
медленнее ответа (`K_p` ≈ 66 против `K_r` ≈ 5), потому что промпт не меняется, а
ответ переписывается каждый шаг. Наш стор обновляет весь префикс одним
расписанием. Это реальная непокрытая ось, и она в ту же сторону, что и
`cache_type` у dInfer: **не «когда», а «что» — и обе оси про то, что префикс
неоднороден.**

> Прежде в этой сессии я утверждал, что у 1.5 «`use_cache=True` по умолчанию, а
> кэш не вызывает только `generate()`». Неверно: в конфиге `false`, а в
> `forward` — ассерт. Отозвано.

#### Что из наших заявок оказалось занято

Первоначальная формулировка — «квантизации кэша в диффузионках нет» — **снята**.
Четыре опоры оказались заняты:

| заявка | кем занята |
|---|---|
| никто не квантует кэш dLLM | **DART**, arXiv 2601.20706, янв. 2026: W4A8KV4, MXINT4-кэш, LLaDA-8B и LLaDA-MoE-7B-A1B |
| K и V не одинаковы | **KIVI**, 2024: K по каналам, V по токенам (авторегрессия) |
| блочно-каузальный префикс точен | **LaCache**, arXiv 2607.16339 |
| 4-битные веса дёшевы для dLLM | **Layer Collapse**, arXiv 2605.06366: 3-битный GPTQ на LLaDA стоит 1.8% против 64.7% у Llama-3.1-8B |
| роутер переадресует токены от квантизации **одного кэша** | **arXiv 2608.11212**, авг. 2026: симулированный 4-битный кэш на OLMoE, доля ущерба через маршрут **RMF ≈ 0.31 ± 0.020** |
| закрепление маршрутов снимает часть ущерба | там же: закрепление чистых маршрутов возвращает +0.23…0.46 — и их аппарат строже нашего (четыре прогона: чисто/квантовано × свободно/закреплено) |
| ось `K` вдоль токенов лучше поворота `K` | **KVLinC**, arXiv 2510.05373: поворот `K` увеличивает масштабный множитель и ошибку; рекомендуют сырые `K` поканально + повёрнутые `V` по токенам |
| причина асимметрии `K/V` | **KIVI** её не постулирует, а формулирует: «в кэше ключей отдельные фиксированные каналы имеют очень большие величины, в кэше значений выраженного паттерна выбросов нет», и приводит цену — по-токенная нарезка даёт впятеро большую ошибку скоров. **Моя прежняя формулировка «KVQuant принимает асимметрию как данность, у нас она измерена» — преувеличение** |
| совпадение каналов-выбросов как измеряемая величина | **Alignment Collapse**, arXiv 2606.09864: перекрытие каналов-выбросов на критическом слое решает, спасает ли поканальная нарезка выравнивание |
| «техники для плотных моделей неадекватны для MoE» | **QuantMoE-Bench** (2406.08155), **MxMoE** (2505.05799), линия fine-grained MoE-квантизации — тезис 7 целиком занят, его место в разделе воспроизведения, а не в заголовке |
| поворот для кэша | **RotateKV**, **OSCAR** (2605.17757), **OScaR** (2605.19660), **PolarQuant** — инструмент занят плотно |
| дрейф K/V растёт с глубиной; самый посещаемый токен дрейфует меньше всех (добавлено 14 сентября) | **Elastic-Cache**, arXiv 2510.14973, ICLR 2026, в том числе на **LLaDA-1.5**. Заморозку текущего блока они **не** мерят — цена обрыва остаётся нашей |
| обучаемые множители нормировки усиливают выбросы в фиксированных каналах (добавлено 14 сентября) | **Outlier Suppression**, arXiv 2209.13325, NeurIPS 2022: γ в LayerNorm — «усилитель выбросов». Механизм известен для остаточного потока; применение к **QK-Norm на ключах** и к цене оси кэша между моделями в поиске не нашлось |
| множитель QK-RMSNorm создаёт устойчивый выброс K в фиксированном канале (добавлено 14 сентября) | **HiFA4**, arXiv 2607.04302, июль 2026: Qwen3-8B, слой 0, канал 51, γ ≈ 34 — выброс одинаков для всех токенов и голов; лечат поканальным сглаживанием для 4-битного FlashAttention. **Механизм К3 занят** на одной модели и одном слое. Не занято: цена для оси группировки кэша, вмешательство «с нормой / без» внутри модели, все слои, диффузионные модели |
| шум квантизации ключей сдвигает внимание к кэшированным позициям (добавлено 14 сентября) | **Quantized Keys Steal Attention**, arXiv 2605.26266: «Jensen bias» в авторегрессионной видеодиффузии. Кандидат в механизм пола округления, модельно-специфичным его не делает |
| развал поворотной и по-токенной схем кэша при 2 битах на моделях с QK-Norm (добавлено 17 сентября) | **Занято эмпирически, без объяснения.** **KVLinC** (2510.05373): «для семейств Qwen-2.5 и Qwen-3 QuaRot не даёт осмысленных результатов» — perplexity 202.3 на Qwen3-8B-Base и 783.0 на Qwen2.5-3B против 7.7 и 9.7 у KIVI; объяснение — «масштаб на токен неоптимален». **OptR** (2608.02691), INT2: QuaRot 2.61% против 67.58% на Qwen3-4B, 25.96 против 65.66 на Qwen3-8B, но 52.98 против 58.57 на Phi-4 без QK-Norm; объяснение — несогласованность цели с выходом внимания. **OScaR** (2605.19660), INT2: QuaRot −9.4 пункта на Qwen3-8B против −3.8 на Llama-3.1-8B, KIVI почти без потерь на обеих. QK-Norm, γ и bias не упоминает ни одна из трёх (проверено по полным текстам), вмешательств в веса нет. **Наш развал 84.5 → 1.5 — воспроизведение известного эффекта на диффузионной модели; своё — объяснение и вмешательство (§5, «Перенос γ»)** |
| параметр в пути ключа создаёт выброс, и его можно вынести из кэша (добавлено 17 сентября) | **Quantization Hurts Reasoning?** (2504.04823, прил. C.2): bias в k_proj у DeepSeek-R1-Distill-Qwen порождает экстремальные каналы; KVQuant* квантует до прибавления bias. Аддитивный аналог нашего γ, подан как рецепт, 3–4 бита, без абляции «с/без». Объясняет и то, почему Qwen2.5 без QK-Norm разваливается у KVLinC: выброс там от bias |
| **хранить ключи без bias / без общей для всех ключей составляющей** (проверено 22 сентября) | **Занято в трёх формах.** (1) «Pre-bias» KVQuant* там же (2504.04823): ключи квантуются **до RoPE** и до прибавления bias, статическая шкала по каналам; для QuaRot, по словам авторов, «неприменимо». Там же — диагноз «bias ломает по-токенные схемы, по-канальные спасают». (2) **SageAttention** (2410.02367, «smooth K»): из ключей после RoPE вычитается среднее по токенам по каждому каналу перед по-токенным INT8; точно, потому что `softmax(q(K − m)ᵀ) = softmax(qKᵀ)`. Это для вычисления внимания, не для хранения, но механизм тот же: убрать общую для ключей составляющую, чтобы по-токенная шкала её не охватывала. (3) Практика в llama.cpp (TurboQuant, `key_nf4_asym`): вычесть среднее по каналу, квантовать остаток, вернуть. **Наше отличие узкое:** для кэша, хранящего ключи **после** RoPE, вычитается `RoPE(b)` в своей позиции — точно и без калибровки, тогда как среднее по токенам после RoPE ловит только ту часть bias, что не вращается; с R4 вычитается `RoPE(b)·H`, и QuaRot, который авторы pre-bias считали несовместимым, оживает (1.5 → 79.0); по-токенная схема — 0.0 → 84.0; блочная диффузионная модель. **Заявлять как метод нельзя — только как адаптацию с этими ссылками.** Не проверено: даст ли SageAttention-подобное вычитание среднего по блоку тот же результат — это нужная базовая клетка |
| перенос масштаба из K в Q с общим множителем на пару RoPE (добавлено 17 сентября) | **QServe / SmoothAttention** (2405.04532): λᵢ = max(\|Kᵢ\|)^0.5 по активациям, ограничение λᵢ = λᵢ₊D/2 ради коммутации с RoPE, вшивается в W_Q и W_K; абляция на Llama-2-7B KV4 — perplexity 5.80 → 5.75. Также **Smooth-QK** (HiFA4, после RoPE), **BAOS** (DART, онлайн по блокам), **InnerQ**. Точное поглощение статического веса нормы K запросом — **QK-Normed MLA** (2606.16310), ради памяти MLA, не квантизации. **Сама операция занята целиком**; своё — использование её как причинного контроля; бит-точность через степени двойки — деталь, не вклад |
| выбросы Q/K вызывает RoPE на низких частотах (добавлено 17 сентября) | **Massive Values** (2502.01563): моделей с QK-Norm в выборке нет. **Конкурирующее объяснение**: на LLaDA2.0-mini перенос γ при нетронутом RoPE снимает пики во всех 20 слоях (crest 11.5 → 6.3), так что для этой модели основная доля пиков — от γ. Спорить надо явно и только про модели с QK-Norm |

DART — статья про NPU: они показали, что 4-битный кэш работает, потому что им
нужно было построить ускоритель. **Почему** он работает, где перестаёт и как
делить бюджет — не спрашивали. Плюс обе их модели полностью двунаправленные,
значит устаревание и округление у них неразделимы (см. §4).

**Проверено 3 сентября и оказалось НЕ занято** — сначала я решил обратное по
пересказу поисковика, полный текст это опроверг:

| заявка | почему свободна |
|---|---|
| допустимая разрядность **кэша** — функция доли маски | **Quant-dLLM** (2510.03274, ICLR 2026) только про веса: кэш не квантуют и не изучают, разрядность **едина на всех шагах** (2 бита), смешанная точность на уровне блоков весов и **фиксирована независимо от стадии расшумления**. Их MCS — про калибровку, а не про точность вдоль траектории |

**Что осталось от тезисов 5 и 6 после этой проверки.** Не «мы измерили то, что
другие постулировали» — измерили и они. Остаётся **диссоциация**: R4 разрушает
пространственную концентрацию (совпадение каналов 0.66–0.81 → 0.14–0.26) и **не
трогает** временную устойчивость (разброс 1.35–1.50 → 1.31–1.32, промах
калибровки 0.17 → 0.14 бита). Литература лечит обе одними средствами и нигде их
не разводит; поворот как **зонд**, различающий два свойства, не найден. Плюс сами
числа на диффузионной модели и совпадение структуры `K` у двух архитектур при
противоположном следствии (§2.3).

**И одно предупреждение из 2608.11212, которое надо нести.** Маржа роутера
предсказывает **факт** переворота (AUC 0.772) и не предсказывает его **вред**
(AUC 0.490, то есть случайно). Наша §4.6 пришла к тому же с другой стороны:
закрепление невалидно как вмешательство на уровне решений, потому что
уполовинивает зазоры. Их формулировка чище, и цитировать надо её.

**Что у роутера остаётся нашим.** 2608.11212 прямо не про диффузию, не про долю
маски и не про шаги расшумления. Значит нашим остаётся **вся зависимость от
положения на траектории**: согласие раздачи 31.7 → 51.5 → 66.3 по мере
раскрытия, немонотонность для кэша с минимумом при маске 0.50, и предсказание с
обратным знаком — **когерентность защищает**. Плюс разложение: они делят ущерб
на маршрутный и прямой, мы — на устаревание и округление. Разные разложения.
А их RMF 0.31 против наших 16–29% на другой архитектуре читается как
**независимое воспроизведение**, то есть цитируется и в поддержку.

Для введения годится цитата из **QDLM** (arXiv 2508.14896), которые сами
называют это направление незакрытым:

> Our work is orthogonal to these efforts and can be seamlessly integrated by
> quantizing dLLM caches.

---

## 2. Кэш: главные результаты

Модель LLaDA2.0-mini, bf16, **веса и активации не квантованы** — это конвенция
области (KIVI, KVQuant, GEAR, ZipCache, SKVQ) и единственный способ приписать
сдвиг предмету исследования.

Квантизация кэша — **динамическое округление к ближайшему**, шкалы считаются из
тензора при записи, **калибровки нет вовсе**. Четыре бита бесплатны самым
грубым методом из существующих: довод за переносимость, и его стоит заявить, а
не умолчать.

### 2.1. Ось группировки K решает больше, чем разрядность

Группа делит шкалу; шкалу задаёт крупнейший элемент. У K выбросы сидят в
**фиксированных каналах**. Группа вдоль каналов такой выброс накрывает и портит
шкалу соседям; группа вдоль токенов сидит внутри одного канала.

4 бита, группа 128, префикс раскрыт:

| ось K | rel. err | argmax@k |
|---|---|---|
| каналы | 1.68e-01 | 82.29% |
| **токены** | **8.03e-02** | **94.79%** |

При трёх битах — 75.00% против 91.67% (группа 32). **Верная ось стоит больше
бита.** Ось V безразлична — KIVI переносится наполовину.

### 2.2. Четыре бита неотличимы от точного кэша

94.79% — **ровно контрольная строка при 16 битах**, те же 91 позиция из 96.
При группе 128 накладные на шкалы 6.25% и сжатие **3.75×** — не 3.76×, как
писалось раньше: 224 токена группами по 128 это две группы, а не 1.75, так что
K стоит 4.286 бита, V 4.25, в среднем 4.268.

### 2.3. На верной оси размер группы не значит ничего

При 4 битах группы 32 и 128 дают 94.79% обе, ошибка совпадает до третьей цифры
(8.020e-02 против 8.027e-02). Значит брать надо крупную — она дешевле.

Прежний вывод «группа 32 лучше 128» был **артефактом неверной оси** (на ней
разрыв 6 пунктов при 4 битах и 20 при 3).

> **Переносится на плотную, 7 сентября.** Прежде это мерилось на MoE, и §2.3
> была одномодельной. `--group-size 32` против 128 при `K` на оси токенов:
>
> | 4 бита | гр. 128 | гр. 32 | разн | сигм |
> |---|---|---|---|---|
> | `block` | 46.9 | 46.3 | −0.6 | 0.19 |
> | `every_n:8` | 12.1 | 13.1 | +1.0 | 0.48 |
> | `every_n:4` | 6.1 | 5.5 | −0.6 | 0.41 |
> | `every_n:2` | 2.5 | 2.3 | −0.2 | 0.21 |
>
> При трёх битах то же: −1.3, −2.3, −0.4, +0.8, все под 1.1σ. **Знаки гуляют,
> величины в шуме.**
>
> **И это было предсказано из механизма, а не найдено.** На верной оси группа
> сидит внутри одного канала; канал-выброс крупный целиком, а не «в нём одно
> большое число», поэтому шкала описывает все его значения хорошо при любом
> размере группы. Рассуждение архитектурно-нейтральное — результат вышел таким же.
>
> **Практически: брать 128 на обеих моделях**, она вчетверо дешевле по накладным
> на шкалы.

#### Три вещи под одним именем, и переносятся они по-разному

«По каналам против группы 128» в проекте значит три разных вещи, и путать их
дорого:

| | что сравнивается | переносится? |
|---|---|---|
| **нарезка активаций** | по токенам против группы 128 по каналам | **наполовину**: цена верной одинакова (6.5 против 6.0), цена неверной нет (52 против 79) |
| **ось группировки `K`** | группа вдоль каналов против вдоль токенов | **нет**: ≈0 против +41 пункта |
| **размер группы на верной оси** | 32 против 128 | **да**: безразличен у обеих |

Читается это через сегодняшний вывод §2.5a: **где мерится тензор — модели
совпадают, где мерятся решения — MoE хуже.** Размер группы решается внутри
канала, то есть на тензоре, и переносится. Ось группировки порождает возмущение,
которое идёт через сеть, и не переносится.

### 2.4. Полная таблица, ось token, группа 128

`argmax@k`, доля маски префикса:

| биты | 0.00 | 0.50 | 1.00 |
|---|---|---|---|
| 16 (контроль) | 94.79% | 98.96% | 97.92% |
| 8 | 95.83% | 96.88% | 90.62% |
| **4** | **94.79%** | 93.75% | 87.50% |
| 3 | 88.54% | 90.62% | 88.54% |
| 2 | 80.21% | 79.17% | 82.29% |
| пол случайности | 9.38% | 23.96% | 68.75% |

**Цена битов растёт с долей маски:** четыре бита бесплатны при раскрытом
префиксе, 5.2 пункта при половинной маске, 10.4 при полной. По отдельности
значима только последняя: при 96 позициях ошибка одной строки 2.4 пункта,
разности двух — 3.4, так что 0.0 / 5.2 / 10.4 это 0.0σ / 1.5σ / **3.0σ**.
Заявлять надо концы, а середину — как согласующуюся, но не самостоятельную.

Пол случайности растёт вместе с долей маски, и это работает **в пользу**
вывода, а не против: доступный метрике диапазон сужается с 85 пунктов до 29, и
та же порча в долях диапазона идёт 0.0% → 6.9% → **35.7%**. То есть на
`argmax@k` эффект скорее занижен. Механизм — зазор
решения падает с **1.988** логита до **0.756**: замаскированные позиции несут
одну строку эмбеддинга, их распределения сходятся, решения стоят у ничьей.

Допустимая разрядность — **не константа модели, а функция положения на
траектории.** В авторегрессии такой оси нет.

### 2.5. При полной маске ломается расписание, а не токены

Там `argmax@k` вырожден (пол 68.75%), читать надо `slots@k` — совпали ли сами
фиксируемые позиции:

45.83% → 31.25 → 27.08 → 26.04 → **13.54** при поле 12.50%.

**Двухбитный кэш при полной маске уничтожает порядок раскрытия**, оставляя
токены почти верными. Отдельная поломка, и без этой метрики её не видно.

### 2.5a. Раскладка кэша: разрыв осей идёт от QK-Norm в тензоре, пол округления — от сети

> Прежний заголовок — «Раскладка кэша — свойство архитектуры, а не тензора» —
> снят 11 сентября вместе с тензорными строками 2.0, снятыми без QK-Norm.

**Оформлено 3 сентября из материала, размазанного по §2.1, §2.3 и §2.7.8.**
Утверждение опирается только на сравнение двух моделей и без второй не
существует вовсе.

**Стандартное объяснение и что оно предсказывает.** KIVI и линия за ним говорят:
ось важна, **потому что** выбросы `K` сидят в фиксированных каналах, а группа не
должна их пересекать. Отсюда следует, что ось обязана работать всюду, где
концентрация есть.

**Концентрация есть у обеих моделей, и почти одинаковая:**

| совпадение старших каналов `K` (маска 0.00 / 0.50 / 1.00) | |
|---|---|
| LLaDA-1.5 | 0.64 / 0.60 / 0.70 |
| LLaDA2.0-mini | 0.66 / 0.77 / 0.81 |
| пол случайности | 0.00 – 0.01 |

**Следствие противоположное:**

| | LLaDA-1.5 | LLaDA2.0-mini |
|---|---|---|
| разрыв «канал − токен», 3 бита | **≈ 0** (−2.7 … +0.4) | **+41 пункт** |
| пол округления, 4 / 3 бита | 0.7% / 0.4% | 3.7% / 5.4% |
| что делает R4 | **ничего** (44.7 против 48.0, в шуме) | обрушивает разрыв до +5, уполовинивает пол |

**И это не эффект пола.** Прежняя редакция §2.3 объясняла нулевой разрыв на
плотной модели тем, что весь её диапазон ущерба меньше трёх процентов. Верно для
строк `every_n:1`; при `block` ущерб на 1.5 доходит до **46–48%**, и эффекту в
сорок один пункт есть куда поместиться. Его там нет.

> **Вывод: концентрация выбросов необходима, но не достаточна.** Общепринятый
> механизм неполон, и это утверждение против занятой территории, опирающееся на
> прямое измерение обеих сторон.

#### Два кандидата на недостающее звено проверены и отвергнуты

**Маршрутизация — отпала.** Если бы усилителем был софтмакс и роутер за ним,
доминировать должен `K`. При 18 промптах `K` даёт 2.865%, `V` — 2.821%,
**0.09σ**, а `V` в софтмакс не входит вовсе (§2.7.8).

**Веер GQA как источник корреляции — отпал, и в обратную сторону.**
`--head-sharing` при равной поэлементной величине ошибки: коррелированная между
четырьмя читателями даёт **42.19%**, независимая — **48.24%**, то есть
коррелированная **менее** вредна (1.95σ). Ожидалось обратное.

**Ёмкостный кандидат снят на арифметике, до прогона.** У 2.0 вчетверо-ввосьмеро
меньше шкал — но и хранимых значений во столько же раз меньше. **Шкал на значение
поровну**, дефицита разрешения нет, и зонд мерил бы гранулярность, а не GQA.

**Третий кандидат — «`K` у GQA труднее квантуется» — проверен и отпал.**
`check_key_error.py`: `K` после RoPE, 4 полотна по 256 токенов, маска 0.50, шесть
слоёв равномерным разбросом (глубины 20 и 32, фиксированный индекс сравнивал бы
разные доли стека).

| | 4 бита, токен | 4 бита, канал | отн. 4 бит | отн. 3 бит | crest |
|---|---|---|---|---|---|
| LLaDA2.0-mini | 5.837e-02 | 1.190e-01 | **2.04×** | 2.09× | 6.10 |
| LLaDA-1.5 | 6.090e-02 | 1.211e-01 | **1.99×** | 2.04× | 6.37 |

**Совпадает всё, до четырёх процентов** — уровень ошибки на обеих осях, отношение
между осями, концентрация выбросов по головам.

> **Переснято 11 сентября: строка LLaDA2.0-mini была снята не с того тензора, и
> вывод «совпадает всё» снимается.** Зонд (`LLaDAAttentionProbe._project`) не
> применял QK-Norm, а у LLaDA2.0 она включена (`use_qk_norm=True`, унаследована
> от Ling 2.0) и стоит между проекцией и RoPE (`modeling_llada2_moe.py`, строки
> 496–503). Модель внимает и кэш хранит ключи **после** нормировки. На LLaDA-1.5
> QK-Norm нет, её строка верна. Зонд исправлен (`627c98c`), таблица переснята в
> той же постановке — 4 полотна по 256 токенов, маска 0.50, слои 0/4/8/11/15/19:
>
> | K, группа 128 | ось `token`, 4 бита | ось `channel`, 4 бита | **отн. 4 бит** | отн. 3 бит | crest K |
> |---|---|---|---|---|---|
> | LLaDA2.0-mini, без QK-Norm (прежняя строка) | 5.837e-02 | 1.190e-01 | 2.04× | 2.09× | 6.10 |
> | **LLaDA2.0-mini, с QK-Norm — тензор кэша** | **4.005e-02** | **1.913e-01** | **4.78×** | **4.44×** | **11.83** |
> | LLaDA-1.5, QK-Norm нет | 6.090e-02 | 1.211e-01 | 1.99× | 2.04× | 6.37 |
>
> У `V` отношение 1.26× и 1.28× — к единице близко, как и безразличие оси `V` на
> решениях. На `V` нормировка не действует.
>
> **Модели различаются уже на тензоре ключей, и всю эту разницу даёт QK-Norm.**
> Без нормировки ключи 2.0 неотличимы от ключей 1.5 — 2.04× против 1.99×, crest
> 6.10 против 6.37. С нормировкой, внутри одной модели и на тех же полотнах,
> отношение осей растёт вдвое, до 4.78×, а crest — до 11.83. Правдоподобный
> механизм: RMSNorm выравнивает норму каждого ключа и умножает каналы на **общие
> для всех токенов** обучаемые множители, так что каналы с большими множителями
> становятся устойчивыми выбросами. Это ровно та структура, на которой шкала на
> канал выигрывает (ошибка падает с 5.8e-2 до 4.0e-2), а шкала на токен
> проигрывает (растёт с 1.19e-1 до 1.91e-1). По знаку совпадает с решениями:
> разрыв осей +41 на 2.0 и ≈0 на 1.5.
>
> **Одна асимметрия из двух при этом остаётся ниже тензора.** На оси `token`
> ошибка K у 2.0 **меньше**, чем у 1.5 (4.0e-2 против 6.1e-2), а пол округления
> на решениях у 2.0 выше — 5.4% против 0.4%. Тензор этого не объясняет: пол
> по-прежнему живёт в отклике сети.
>
> **От доли маски это не зависит.** Та же таблица при маске 0.00: отношение
> **4.09×** при четырёх битах, 3.58× при трёх, crest 12.07. Против 4.78× и 4.44×
> при маске 0.50 — уровень плавает, вывод нет.
>
> **А расхождение с §2.1 оказалось сравнением двух разных величин.** Там 1.68e-01
> против 8.03e-02, то есть 2.09× — это отношение ошибок **логитов**, того, что
> `check_block_cache` и меряет. Здесь 4.09–4.78× — отношение ошибок **самого
> тензора K**. Ни противоречия, ни зависимости от прибора нет.
>
> **И именно это совпадение помогло дефекту уцелеть.** В прежней редакции раздела
> стояло «прибор проверен: 2.04× здесь против 2.14× в §2.1, известное число
> воспроизведено». Сошлись две разные метрики, одна из которых была снята без
> QK-Norm, и проверка выглядела пройденной. Сверять надо однородные величины.
>
> Строка «совпадение старших каналов K» выше в разделе источника не указывает;
> если она снята зондом, её строка 2.0 тоже снята до нормировки.
>
> **Что задето дефектом зонда:** только то, что читает зонд на 2.0, —
> `check_key_error`, `measure_drift`, `selfcheck` — и калибровка IA-AQ в
> калиброванных прогонах 2.0 (матрица внимания без нормировки). **Что не
> задето, проверено по импортам:** `check_block_cache`, `check_static_scales`,
> `check_lowrank` и `trace_window_path` берут `refresh_prefix` и
> `logits_for_window` из `llada2_local`, где QK-Norm стоит до RoPE и до записи в
> стор (`llada2_local.py:125`). Не задеты также вся сетка на задаче без
> калибровки, `check_block_reuse`, кэш и R4 на 2.0.

#### Три опоры под этим выводом, снятые 11–12 сентября

**Первая: внутри одной модели.** Те же ключи LLaDA2.0, с нормировкой и без неё,
на тех же полотнах и слоях: отношение осей 2.04× против **4.78×**, crest 6.10
против **11.83**. Переменная одна.

**Вторая: между моделями расходится только `K`.** Обе доли маски, обе модели:

| | 2.0 (QK-Norm есть) | 1.5 (нет) |
|---|---|---|
| `K`, отношение осей, маска 0.00 | **4.09×** | **1.75×** |
| `K`, отношение осей, маска 0.50 | **4.78×** | **1.99×** |
| `K`, crest | 12.07 / 11.83 | 6.37 / 6.37 |
| `V`, отношение осей | 1.16 / 1.26 | 1.12 / 1.22 |
| `V`, crest | 6.24 / 7.44 | 6.45 / 7.47 |

**`V` у двух моделей совпадает до третьей цифры, а `K` различается вдвое.**
QK-Norm применяется к Q и K и не применяется к V — расходится ровно то, что она
трогает. Будь причиной MoE, веер GQA или блочная маска, разошёлся бы и `V`. И
различие держится при обеих долях маски, а не только при 0.50.

**Третья: сторона RoPE проверена и отпала.** `--kv-rope post` (`e073547`) кладёт
в префикс 1.5 тот же тензор, что хранит 2.0. Разрыв «ось `token` минус ось
`channel`», среднее по четырём блокам на удержанном полотне:

| LLaDA-1.5, политика `block` | 4 бита | 3 бита |
|---|---|---|
| ключи **до** RoPE — как было всегда | −0.8 п. | 0.0 п. |
| ключи **после** RoPE — тензор 2.0 | +2.5 п. | +1.6 п. |
| *для сравнения, LLaDA2.0* | | *+41 п.* |

При 512 зафиксированных токенах на строку стандартная ошибка около двух пунктов,
так что +2.5 — примерно одна сигма. **Разрыв на 1.5 остаётся около нуля и тогда,
когда обе модели квантуют одну и ту же сторону RoPE.** Смешивающий фактор
закрыт: сторона тензора стоит пару пунктов, а не сорок.

> **Итог раздела.** Ось группировки `K` важна не сама по себе, а в меру того,
> насколько нормировка перед ней собрала выбросы в фиксированные каналы. Три
> опоры: переменная изолирована внутри модели, между моделями расходится только
> тот тензор, которого нормировка касается, и очевидная альтернатива — сторона
> RoPE — отвергнута прямым замером.

> **И третье подтверждение того же, из оси `V`.** §2.6 говорила «ось `V`
> безразлична», измерив это на MoE. Переснято на плотной, `--value-axis token`
> против `channel` при `K` на своей лучшей оси в обеих строках:
>
> | 3 бита | канал | токен | разн | сигм |
> |---|---|---|---|---|
> | `block` | 48.0 | 44.9 | −3.1 | 0.99 |
> | `every_n:8` | 15.0 | 12.9 | −2.1 | 0.97 |
> | `every_n:4` | 6.1 | 5.5 | −0.6 | 0.41 |
> | `every_n:2` | 2.7 | 3.1 | +0.4 | 0.38 |
>
> **Все разности под одной сигмой** — против +41 пункта, которые даёт ось `K` на
> 2.0. Безразличие переносится.
>
> Причём `V` на плотной **структурированнее**, чем на MoE: совпадение старших
> каналов 0.34–0.42 против 0.19–0.27. Более концентрированный тензор, и ось всё
> равно ничего не решает. **Концентрация выбросов не предсказывает важность
> оси** — теперь это измерено трижды: у `K` одинаковая структура при
> противоположном следствии, у `V` большая структура при том же безразличии, и
> тензорные ошибки совпадают там, где решения расходятся втринадцатеро.
>
> **Снято 11 сентября.** Здесь стояло «прибор при этом проверен: 2.04× здесь
> против 2.14× в §2.1 на раскрытом префиксе, известное число воспроизведено».
> Это была не проверка: 2.04× — отношение ошибок тензора `K` (и снято без
> QK-Norm), 2.14× — отношение ошибок логитов. Совпали две разные величины.

#### Механизм: разрыв осей — в тензоре ключей, пол округления — в отклике сети

> **Переписано 11 сентября.** Прежде раздел назывался «Механизм локализован: он в
> отклике сети, а не в тензоре», и таблица ниже показывала «одинаково» в трёх
> тензорных строках из четырёх. Строки 2.0 были сняты зондом без QK-Norm.

| уровень | 1.5 против 2.0 |
|---|---|
| совпадение старших каналов `K` | 0.64–0.70 против 0.66–0.81 — источник не указан, см. поправку выше |
| ошибка `K` на оси `token`, 4 бита | 6.1e-2 против **4.0e-2** — у 2.0 **меньше** |
| ошибка `K` на оси `channel`, 4 бита | 1.21e-1 против **1.91e-1** — у 2.0 больше |
| отношение осей на тензоре | 1.99 против **4.78** — **различается вдвое** |
| crest factor `K` | 6.37 против **11.83** — **различается вдвое** |
| **разрыв осей на решениях** | **≈ 0 против +41 пункта** |
| **пол округления** | **0.4% против 5.4%** |

**Две асимметрии, два адреса.** Разрыв осей на решениях следует за тензором:
QK-Norm сосредотачивает выбросы ключей в фиксированных каналах, отношение осей
растёт вдвое, и выбор оси начинает решать. А пол округления тензором не
объясняется: на верной оси ключи 2.0 округляются **лучше**, чем у 1.5, а решения
при этом страдают в тринадцать раз сильнее. Эта разница рождается в **отклике
сети**.

Для пола это сходится с §2.7.8: `K` против `V` показал, что усиление **не**
специфично для пути `K` через софтмакс — иначе `K` доминировал бы, а он равен `V`
при 0.09σ. Значит MoE-стек усиливает **любое** пришедшее возмущение, каким бы
тензором оно ни вошло. Ровно это измеряет arXiv 2608.11212 своим RMF ≈ 0.31 ±
0.020.

#### Как это заявлять

> **Переписано 11 сентября.** Прежняя формулировка — «ось группировки принято
> считать свойством тензора; мы показываем, что она свойство архитектуры» —
> стояла на тензорных строках 2.0, снятых без QK-Norm. Снята.

**Ось группировки — свойство тензора ключей, как и говорят KIVI и KVQuant. Но
насколько она важна, решает не одна концентрация выбросов, а нормировка перед
ней:** QK-Norm сосредотачивает выбросы в фиксированных каналах и удваивает цену
неверной оси. QK-Norm стоит у Qwen3, OLMo 2, Gemma 3, Ling 2.0, так что это
предписание для целого класса моделей, а не свойство одной: **у модели с QK-Norm
ключи в кэше квантовать только поканально.**

**Пол округления — свойство отклика сети**, и три отвергнутых кандидата
(маршрутизация, веер GQA, ёмкость) остаются в силе именно для него.

Оговорка, которую надо нести рядом: модели различаются шестью вещами сразу, и
QK-Norm показана как причина разницы **в тензоре** — внутри одной модели, с
нормировкой и без. Что именно она, а не MoE или блочная маска, даёт разрыв **на
решениях**, двумя моделями до конца не разделить.

### 2.8a. R4 подключён: ось и поворот оказались взаимозаменяемы

**3 сентября.** §2.8 ниже показывал, что поворот не меняет разрыв между осями, и
объяснял это тем, что удара не было: R1/R2 до `K` после RoPE не доходят. Теперь
R4 подключён (`--rotate-qk`, коммит `231f998`) — головной адамар на `Q` и `K`
после RoPE, до записи в стор, так что в кэше лежат **повёрнутые** ключи.

Предсказание было записано **до** прогона и играло против нас: ось вдоль токенов
выигрывает потому, что выбросы `K` сидят в фиксированных каналах, а R4
существует ровно чтобы их размазать. Значит преимущество оси должно сократиться.

Доля изменённых зафиксированных решений, полотно 128, блок 32, 4 промпта:

| | канал | канал + R4 | σ | токен | разрыв | разрыв + R4 |
|---|---|---|---|---|---|---|
| **3 бита**, `block` | 85.9 | 65.0 | 8.0 | 58.6 | +27.3 | **+4.5** |
| **3 бита**, `every_n:8` | 60.9 | 21.7 | 13.9 | 20.1 | +40.8 | **+4.1** |
| **3 бита**, `every_n:4` | 54.7 | 16.8 | 13.8 | 13.1 | +41.6 | **+8.4** |
| **3 бита**, `every_n:2` | 49.0 | 10.5 | 14.9 | 7.2 | +41.8 | **+5.6** |
| **3 бита**, `mask_ratio` | 62.5 | 23.2 | 13.8 | 19.5 | +43.0 | **+4.6** |
| 4 бита, `block` | 69.3 | 58.6 | 3.6 | 55.1 | +14.2 | +0.6 |
| 4 бита, `every_n:8` | 21.5 | 16.6 | 2.0 | 16.4 | +5.1 | +1.4 |
| 4 бита, `every_n:4` | 13.3 | 8.4 | 2.5 | 10.7 | +2.6 | −0.2 |
| 4 бита, `every_n:2` | 7.4 | 4.9 | 1.7 | 5.9 | +1.5 | +0.8 |

**При трёх битах разрыв между осями был +41 пункт, стал +5.**

**Это первое причинное свидетельство за историю с фиксированными каналами.** До
сих пор она была наблюдательной: выбросы сидят в каналах, значит ось вдоль
токенов выигрывает. Здесь мы **вмешались** ровно в ту структуру, которую история
называет причиной, и предсказанное следствие наступило.

**Вторая половина — и она из той же логики.** На оси токенов R4 не даёт почти
ничего: все десять клеток ниже 1.5σ, знак систематичен, но по отдельности
значима одна. Так и должно быть — ось уже решает ту же задачу, и дважды её не
решить.

> **Ось и поворот взаимозаменяемы, а не дополняют друг друга.** Порядок
> предпочтения при трёх битах и `every_n:2`: токен + R4 (4.9), токен (7.2),
> канал + R4 (10.5), канал (49.0). Практический вывод не меняется — брать ось
> вдоль токенов, — но теперь известно, **почему** она работает, и известно, чем
> её можно заменить, если ось недоступна.

**Контроль пройден.** 16 бит, `every_n:1`, R4 включён: `same path` **100.00%**,
по блокам 100.0 во всех четырёх, ноль переворотов. Патч садится там, где должен —
после RoPE и до записи в стор, — и кэшевый путь под ним согласован.

**Оговорка снята: R4 детерминирован.** Зазоры решения по прогонам ложатся в три
группы, и третья объясняется:

| прогонов | зазоры эталона | пол |
|---|---|---|
| без R4 | `3.31 6.98 6.63 8.20` ×3 | 11.91% |
| **с R4** | **`3.43 6.29 7.79 10.62` ×2** | 10.16% |
| с R4, выброс | `3.75 6.86 7.67 9.97` ×1 | 11.91% |

Внутри каждого состояния воспроизводится точно, независимо от `--bits` и
`--key-axis`. Единственный выброс — тот прогон, где в логе стояло
`[load] could not place directly on cuda ... falling back to host RAM then
.to(device)`: другой путь загрузки весов, другая арифметика при копировании.

Значит R4 сдвигает полотно **один раз и одинаково**, собственного разброса между
запусками у него нет, и биномиальные сигмы в таблице выше стоят как есть —
включая уполовинивание пола на 2.3σ.

**А на плотной модели он полотно не сдвигает вовсе.** Зазоры на LLaDA-1.5 —
`4.57 7.58 7.48 7.15` во **всех** прогонах, с поворотом и без, при любых
`--bits` и `--key-axis`.

| | сдвиг полотна от R4 | контроль 16 бит, `every_n:1` |
|---|---|---|
| LLaDA2.0-mini | `3.31…` → `3.43…`, до 20% по блоку | 100.00%, 0 переворотов |
| LLaDA-1.5 | **нет**, зазоры совпадают | 99.80%, **1** переворот из 512 |

Одно и то же возмущение bf16: на плотной модели оно меняет один коммит из
пятисот и полотно оставляет на месте, на MoE уводит траекторию. **Сдвиг вносит
не поворот, а роутер, усиливающий его округление в дискретный переворот** — и
это третий независимый случай того же механизма, первый из которых вылез не в
спланированном эксперименте, а как методический артефакт.

#### Поправка к предсказанию про статику, до прогона

**3 сентября, после того как измерена таблица выше и до того, как запущена
статика.** В подсказке к `--rotate-qk` записано, что R4 должен сломать
статические шкалы «по той же причине». **Это, скорее всего, неверно, и я склеил
два разных свойства.**

- **Ось** держится на том, что выбросы **сосредоточены в каналах**. Факт
  пространственный, внутри одного полотна: группа не должна пересекать большой
  канал и маленькие.
- **Статика** держится на том, что диапазоны **устойчивы между полотнами**. Факт
  временной.

R4 разрушает первое. Второго он касаться не обязан: поворот — фиксированное
ортогональное отображение, и если поканальные диапазоны были устойчивы, их
линейная функция устойчива тоже. Более того, каждый повёрнутый канал есть смесь
многих исходных, а усреднение **снижает** относительный разброс — то есть
статика после R4 может стать **надёжнее**, а не капризнее. Риск для статической
шкалы — упирание, и оно зависит от того, как диапазон гуляет между полотнами, а
не от того, насколько он велик.

**Ставка: статика под R4 останется бесплатной.** Прогон теперь разделяет то, что
§2.6b склеивает фразой «работают ровно потому же»: сломается — свойство
действительно одно; устоит — это два разных факта, и объяснение в §2.6b неверно
при верном числе.

#### Прогон сделан: свойства действительно два

`check_static_scales.py --rotate-qk`, 4 полотна, 4 калибровочных, доли маски
0.00 / 0.50 / 1.00. Читается **обзор**, а не свод: обзор это медианы по каналам и
слоям, свод — доли по 16 позициям, и скрипт сам помечает его `UNDERPOWERED`.

| `K` | без R4 | с R4 |
|---|---|---|
| **совпадение восьмёрки старших каналов** | 0.66 / 0.77 / 0.81 | **0.14 / 0.19 / 0.26** |
| **разброс шкалы между полотнами** | 1.35 / 1.36 / 1.50 | **1.32 / 1.31 / 1.31** |

**И внутренний контроль, который делает это убедительным.** R4 поворачивает `Q` и
`K`, а `V` не трогает — и `V` воспроизвёлся в двух независимых прогонах до
второго знака: совпадение 0.26/0.27/0.19 против 0.24/0.26/0.19, разброс
1.40/1.44/1.72 против 1.40/1.43/1.72. Значит фоновый шум обзора порядка **0.02**,
а совпадение каналов у `K` упало на **0.5**.

**Результат в одну строку: пространственная концентрация уничтожена, временная
устойчивость не шелохнулась.**

Больше того, статике стало **лучше**. Промах калибровки на чужих полотнах упал с
**0.17 до 0.14 бита** против 0.25–0.29, которые стоит хранить динамическую шкалу.
Зависимость от доли маски выровнялась (1.35 → 1.50 стало 1.31 → 1.31), и доля
дисперсии, объясняемая корзиной, упала с **68% до 51%** — то есть после поворота
корзины стоит выбросить, они хранение и лишний поиск.

Механизм очевиден задним числом: каждый повёрнутый канал есть смесь многих
исходных, а усреднение снижает относительный разброс.

> **Что из этого следует для текста.** §2.6b пишет, что статика работает «ровно
> потому же», что выигрывает ось — потому что выбросы сидят в фиксированных
> каналах. **Число верное, объяснение нет.** Ось держится на том, что выбросы
> сосредоточены в каналах *внутри* полотна; статика — на том, что диапазоны
> устойчивы *между* полотнами. R4 разрушает первое и не касается второго. Два
> результата, две причины, и объединять их в одну фразу больше нельзя.

**Поправка была записана до прогона** (коммит `241400f`), а не подогнана после.

#### Предсказание 2: R4 уполовинивает пол округления

`--policies every_n:1`, то есть без устаревания вовсе, ось `token`, 4 промпта:

| | без R4 | с R4 | σ | во сколько |
|---|---|---|---|---|
| 4 бита | 4.50% | **1.97%** | 2.3 | **2.28×** |
| 3 бита | 5.65% | **2.75%** | 2.3 | **2.05×** |

База воспроизвела документированный пол §2.7.8 (3.7% / 5.4%), значит установка та
же и сравнение честное.

**И это проверка закона композиции ручкой, которой в нём нет.** На строках
устаревания той же оси R4 не даёт почти ничего — все клетки ниже 1.5σ, а на
`block` даже хуже (55.1 → 58.0). На чистом округлении, где `d_s = 0`, режет
вдвое.

Закон говорит: в режиме поглощения удешевление меньшей ошибки не покупает
ничего, потому что те позиции уже потеряны на большей. R4 уменьшает `d_r`.
Значит он обязан помогать там, где `d_r` связывающая, и переставать там, где
правит `d_s`. Наблюдается ровно это:

| ось `token`, 3 бита | пол (`n:1`) | `n:2` | `n:4` | `n:8` | `block` |
|---|---|---|---|---|---|
| изменение от R4 | **−2.90** | −2.3 | −4.7 | −2.5 | **+1.9** |

Закон снят на разрядности; здесь он предсказал поведение **поворота**. Градиент
между короткими интервалами неровный — 4 промпта, — но концы разделены: на полу
эффект есть, на `block` его нет и знак обратный.

### 2.8. Поворот кэша не трогает — потому что не трогает K

Проверяли, переживает ли ось группировки QuaRot: поворот **размазывает** выбросы
по каналам, а ось `token` выигрывает ровно потому, что выбросы **сидят в
фиксированных каналах**. Выглядело как прямое противоречие.

`check_block_cache.py`, обе руки в одном прогоне, 24 полотна, доли маски 0.00 и
0.50, три разрядности.

**Разрыв между осями не изменился:**

| | без поворота | с поворотом |
|---|---|---|
| 4 бита, маска 0.00 | 12.50 п. | 9.37 п. |
| 3 бита, маска 0.00 | 39.58 | 32.29 |
| 2 бита, маска 0.00 | 71.88 | 70.83 |
| 4 бита, маска 0.50 | 5.20 | 6.25 |
| 3 бита, маска 0.50 | 35.42 | 45.83 |
| 2 бита, маска 0.50 | 57.29 | 59.38 |

Знак изменения гуляет, величина внутри шума. Отношение ошибок `channel/token`
при четырёх битах: **2.14 без поворота, 2.27 с поворотом.**

**Причина не в том, что ось устояла под ударом. Удара не было.**

Геометрия остатка K на повёрнутой модели совпала с неповёрнутой до третьей
значащей цифры: доля первой компоненты 36.5% против 36.6%, несущих каналов 1.0
против 1.0, совпадение крупнейшей четвёрки 78% против 78%. Так не бывает, если
тензор изменился.

Разбор плана поворота объясняет:

| | что крутит | K |
|---|---|---|
| **R1** остаточный поток | базис residual stream, вплавлен в проекции | **не меняет** — вход повёрнут, вес компенсирован, K выходит тот же |
| **R3** головы значений | подпространство V | не касается |
| **R2** онлайн-адамар | перед down-проекцией MLP, установлено 0 | не касается |

**Ни одно вращение в плане QuaRot не трогает ключи.** Выбросы 8.7 → 3.4 из
отчёта поворота — это активации остаточного потока, а не K.

**Что из этого следует, и это положительный результат.** Три результата,
стоящие на фиксированных каналах K — ось группировки, статические шкалы,
одноканальный остаток — **переносятся в развёрнутую конфигурацию без оговорок.**
Не «выдержали проверку», а «проверка их не задевает»: QuaRot и наша ось работают
с разными тензорами и не конфликтуют.

**И появляется резкий вопрос, которого не было.** У QuaRot в полном рецепте
есть **поворот K-кэша** — онлайн-адамар на запросах и ключах, ровно затем, чтобы
K годился в низкую разрядность. В нашем плане его нет. То есть поворот ключей и
верная ось группировки — **два альтернативных решения одной задачи**, и сравнить
их теперь можно чисто: обе борются с выбросами K, обе стоят почти ничего, и
никто их не сталкивал.

Это следующий эксперимент, и он хорошо поставлен: добавить адамар на K и
померить против оси `token` при равной памяти.

### 2.9. Разрядность и политика обновления не разделяются

Вопрос, который две соседние литературы оставляют между собой. Работы про
политики обновления кэша диффузионок — Elastic-Cache, dInfer, DSB — подбирают,
когда и где пересчитывать, на **точном** кэше. Работы про квантизацию кэша
(DART) выбирают разрядность и политику не трогают. Ни те, ни другие не видят,
что делает первый выбор со вторым.

`check_block_reuse.py`, 128 токенов, 4 блока по 32, **8 промптов**, текст
закреплён по эталону, **1024 зафиксированных позиции на клетку**. Повторяет
прогон на четырёх промптах с вдвое большей выборкой; числа сошлись до десятой,
где сравнимы.

**Ущерб против интервала обновления** (доля изменённых коммитов):

| биты | n:1 | n:2 | n:3 | n:4 |
|---|---|---|---|---|
| 16 | 0.0% | 5.0 | 8.4 | 11.4 |
| 4 | 3.9% | 5.4 | 8.9 | 10.6 |
| 3 | 5.6% | 7.9 | 9.8 | 12.5 |

Строку 16 бит / n:1 читать как ноль по построению: это и есть эталон, с
которым сравниваются остальные клетки, и ошибки выборки у неё нет.

#### Чтение нулевое: у квантованных кривых есть пол, и это и есть ответ

Интервал — не величина. «Раз в два шага» означает разное при длине блока 32 и
64, и ещё раз другое, когда сэмплер фиксирует три позиции за шаг вместо одной.
Инвариант — во что обновление обходится:

    пересчитанных позиций за шаг = длина блока × (1 − hit rate)

Hit rate печатается тем же скриптом, поэтому пересчёт не требует прогона.
`scripts/frontier.py` перекладывает уже снятые строки на эту ось.

| политика | бюджет | 16 бит | 4 бита | 3 бита |
|---|---|---|---|---|
| `every_n:1` | 32.0 | **0.0** | **3.9** | **5.6** |
| `every_n:2` | 16.0 | 5.0 | 5.4 | 7.9 |
| `every_n:3` | 11.0 | 8.4 | 8.9 | 9.8 |
| `every_n:4` | 8.0 | 11.4 | 10.6 | 12.5 |
| `block` | 1.0 | 62.3 | 62.0 | 63.7 |

**Сдвигает ли разрядность саму кривую или только точку на ней** — вопрос, ради
которого ось и строилась. Ответ в первой строке: при полном бюджете точный кэш
даёт ноль, четыре бита — 3.9%, три — 5.6%, и **обновлением этого не убрать**.
Обновление выкупает устаревание и не выкупает округление.

Если бы биты и обновления были взаимозаменяемы, все разрядности лежали бы на
одном фронте с одной асимптотой. Асимптоты разные — значит кривые разные. Это
самая короткая формулировка результата: три числа, без статистики.

**Но сдвиг схлопывается с падением бюджета.** Доля ущерба, приписываемая
разрядности:

| бюджет | 4 бита | 3 бита | разрыв 3 бит, σ |
|---|---|---|---|
| 32.0 | **100%** | **100%** | 7.8 |
| 16.0 | 7% | 37% | 2.7 |
| 11.0 | 6% | 14% | 1.1 |
| 8.0 | ≈0 | 9% | 0.8 |
| 1.0 | ≈0 | **2%** | 0.7 |

Доля падает монотонно на всех пяти точках. **Абсолютный** разрыв так себя не
ведёт — 1.1 пункта при бюджете 8 и 1.4 при бюджете 1, — но оба ниже одной
сигмы, то есть там уже нечего упорядочивать. Точная формулировка: разрядность
перестаёт быть измеримой начиная с бюджета около 11 и дальше не проявляется.
`−0.8` и `−0.3` у четырёх бит — шум того же порядка.

**Горизонтальное чтение даёт курс обмена** — сколько бюджета нужно, чтобы
удержать тот же ущерб:

| ущерб | 16 бит | 4 бита | 3 бита | наценка за 3 бита |
|---|---|---|---|---|
| 8.0% | 11.6 | 12.3 | 15.7 | **+36%** |
| 10.0% | 9.4 | 9.1 | 10.8 | +15% |
| 12.0% | 7.9 | 7.8 | 8.6 | +8% |

> Сжатие кэша до трёх бит стоит 36% дополнительного пересчёта, чтобы удержать
> то же качество, — и наценка растёт по мере ужесточения требования.

То есть память меняется на вычисление, курс измерен, и он **непостоянный**.

**Оговорки по оси.** Цена считает, что обновление пересчитывает весь блок;
реализация, обновляющая только замаскированные позиции, сожмёт ось монотонно,
и порядок кривых сохранится. Ось учитывает только пересчёт — сама квантизация
тоже стоит вычислений и экономит память, и ни того, ни другого на ней нет.
Строка `block` для трёх бит не измерена, интерполяция между точками линейная.

#### Чтение первое: цена битов не константа, она зависит от политики

Разрыв между точным и трёхбитным кэшем в той же таблице, по колонкам:

| интервал | n:1 | n:2 | n:3 | n:4 |
|---|---|---|---|---|
| 3 бита минус 16 бит | **5.6** | 2.9 | 1.4 | 1.1 |
| | 7.7σ | 2.7σ | 1.1σ | 0.8σ |

**Монотонно вниз, от полностью значимого до неотличимого от нуля.** У четырёх
бит то же самое и быстрее: 3.9 → 0.4 → 0.5 → −0.8, то есть начиная с n:2 цена
четырёх бит не измерима вовсе.

Это самая сильная формулировка результата, и бьёт она прямо в DART: **число
«кэш в четыре бита стоит X» не определено без указания политики обновления.**
Здесь оно меняется в пять раз — от 5.6 пункта до статистического нуля — от
ручки, которой в той работе нет.

> **Уточнено 17 сентября.** «5.6 пункта» — это пункты **доли перевёрнутых решений**
> у **трёх** бит, не точность на задаче и не четыре бита (у четырёх 3.9 → 0.4). На
> ответах эта ось не проверялась (§5). И спад отчасти задан метрикой: переворот
> бинарен, и при сильном устаревании округлению остаётся меньше что переворачивать;
> на непрерывных зазорах поглощение тоже есть (§2.7.7), опираться надо на них.
>
> **Проверено на ответах 18 сентября — знак обратный.** При переиспользовании
> текущего блока цена 2 бит на точности **растёт** с устареванием: 7.0 пункта при
> обновлении каждый шаг, 31.0 при обновлении раз в 4 шага (§5, «Устаревание ×
> разрядность на ответах»). Поглощение на решениях до ответов не доходит.

#### Чтение второе: выигрыш от каждого удвоения частоты

Та же таблица с другой стороны, и это исходная формулировка:

| | 16 бит | 4 бита | 3 бита |
|---|---|---|---|
| **n:2 → n:1** | **+4.95** | **+1.48** | **+2.35** |
| n:3 → n:2 | +3.45 | +3.52 | +1.88 |
| n:4 → n:3 | +3.03 | +1.75 | +2.73 |

Последнее удвоение: 4.95 против 1.48 — разница 3.48 пункта, **3.0σ** (грубая
несопряжённая оценка; сопряжённая теснее, кластеризация по промптам — шире).
Две квантованные строки **воспроизвели друг друга**: 1.48 и 2.35 расходятся на
0.6σ, обе примерно на 3σ ниже точной. То есть это не одна клетка.

**Что пришлось снять против версии на четырёх промптах.** Тогда строки шли
вровень на всех удвоениях, кроме последнего, и это подавалось как механизм.
На восьми промптах n:4 → n:2 даёт +6.48 против +5.28 — расхождение есть и там,
просто втрое меньше. Полный путь от n:4 до n:1 стоит 11.4 пункта на точном
кэше и 6.8 на четырёхбитном. Эффект крупнее заявленного, но «расходятся
**только** на самых частых обновлениях» — неверно, и держаться за это
предложение нельзя.

#### Механизм — измерен на множествах, а не выведен

Устаревание и округление борются за одни и те же позиции — за **близкие ничьи**,
— и позиция, уже сорванная большей ошибкой, для меньшей потеряна. Не
объединение, а «кто успел первым».

Это не рассуждение: §2.7.2 меряет это прямо, множествами перевёрнутых позиций.
Предельная цена четырёх бит поверх устаревания — **4 перевёрнутые позиции из 40
при обновлении через шаг** (поглощение 90%) и ноль или меньше при более редких
интервалах. Более того, 22 позиции, которые округление роняет в одиночку, при
добавленном устаревании **не падают вовсе**.

Отсюда обе таблицы выше: на редких обновлениях устаревание съедает всё и
разрядности не видно; по мере учащения оно уходит, и из-под него проступает пол
из округления, который битами и задан.

Согласуется с §2.7, где вдоль траектории устаревание перекрывало округление в
4–10 раз по норме: здесь то же отношение видно на решениях.

**И округление дешевле одного шага устаревания.** 4 бита с обновлением каждый
шаг дают 3.9, точный кэш через шаг — 5.0. Квантованный кэш, обновляемый часто,
точнее точного кэша, обновляемого вдвое реже.

#### Эффект живёт в первом блоке

Поблочно, 16 бит при n:4: **78.1** согласия в первом блоке против 93.0, 89.1,
94.1 в остальных. На одном первом блоке последнее удвоение стоит +7.4 против
+2.0 у четырёх бит.

Это предсказано зазором решения, который по блокам растёт (3.31 → 6.98 → 6.63
→ 8.20): ранние блоки — широкая маска, сомкнутые логиты, дешёвый переворот.
Значит среднее по четырём блокам эффект **разбавляет**, и первый блок стоит
показывать отдельной строкой.

**Формулировка для статьи:**

> Две настройки не разделяются. Нельзя подобрать политику обновления на
> неквантованном кэше, а потом квантовать — и нельзя назвать цену разрядности,
> не назвав политику. Цена трёх бит падает с 5.6 пункта до неизмеримой, когда
> интервал обновления растёт с одного шага до четырёх.

**Оговорки.** Сравнение идёт на **фиксированных** интервалах, а у соседей
политики адаптивные; перенос настройки с точного кэша на квантованный внутри
адаптивного семейства — отдельный эксперимент, и он не поставлен. Интервалы
n:6, n:8, n:16 и «блок» мерились только на четырёх промптах (ущерб 13–56%);
там строки шли вровень, что этой картине не противоречит, но выборка вдвое
меньше. Оценки σ биномиальные и не учитывают ни сопряжённость позиций, ни
кластеризацию по промптам.

### 2.6b. Подтверждение KVQuant на MoE-диффузионной модели — плюс измеренная причина асимметрии

> **Приоритет, снято 24 августа.** Ранее этот раздел назывался
> «Шкалы K можно вынести из кэша» и подавался как результат. **Это не результат.**
> Офлайн-калиброванные поканальные шкалы для K-кэша — это **KVQuant** (NeurIPS 2024,
> arXiv 2401.18079), и мотивированы там точно тем же: при поканальной оси шкалу
> пришлось бы пересчитывать на каждый новый Key и обновлять все прошлые. Более того,
> сочетание **«статика поканально на K, динамика по токенам на V, группа 128»** ходит
> в литературе как именованный бейзлайн **KVQuant\***. Совпадает и частность про три бита:
> у них без выноса выбросов статика на трёх битах проседает, с выносом — сравнивается
> с динамикой.
>
> Наше здесь два пункта, и оба скромные: **воспроизведение на другом семействе**
> (MoE, двунаправленное внимание, переиспользуемый кэш — ни одного из этого у них не было)
> и **измеренная причина асимметрии K/V** — почему статика работает на K и не работает
> на V. KVQuant принимает эту асимметрию как данность и в числах её не объясняет.
>
> **В статью этот раздел идёт как подтверждение чужого результата со ссылкой,
> не как свой.** Цифра 3.88× остаётся верной и полезной для конфигурации, но
> новизны в ней нет.

Фаза C, пункт 1, снято. `check_static_scales.py`, 24 полотна, доли маски 0.00 и
0.50, три и четыре бита, K вдоль токенов.

**На решениях не разрешилось ничего.** Двенадцать разностей `argmax@k`, все
внутри двух стандартных ошибок (±3–5 пунктов при 96 позициях). Средний
`argmax@k` у чемпиона и у полностью статической конфигурации совпал до второго
знака: **90.62% против 90.62%**.

Поэтому вывод берётся из `rel. err` — она среднее по многим числам, а не доля
по 96 позициям, и мощности у неё несравнимо больше. Курс берётся из самой
таблицы: бит уполовинивает ошибку, значит экономия `b` бит честна, если ошибка
выросла меньше чем в `2^b` раз.

Безубыточность считается **не** как `2^b`: у ошибки есть пол, который битами
не убирается — оконная арифметика, 16-битный контроль даёт 4.53e-02 против
1.104e-01 на трёх битах, то есть 17% дисперсии. Биты уполовинивают только
остальные 83%.

| строка | сэкономлено | заплачено ошибкой | безубыточность | |
|---|---|---|---|---|
| одна шкала на канал (потолок) | 0.07 бита | +2.3% | +4.2% | даром |
| статика на K, динамика на V, одна корзина | 0.15 | +4.6% | +9.2% | даром |
| статика на K, динамика на V, корзины | 0.15 | +2.1% | +9.2% | даром |
| статика на K и V | 0.27 | +24.1% | **+17.4%** | **не окупается** |

**Вывод: калибровать K бесплатно, калибровать V — нет.** Шкалы K уходят из кэша
в модель, сжатие **3.75× → 3.88×**, и это не стоит ничего измеримого. А
статика на обеих сторонах платит 24% ошибки за безубыточные 17% — то есть
чистые **4.00×** покупаются реальной потерей, а не даром.

Это ровно раскладка KVQuant, полученная независимо и на другой архитектуре.
Ценность строки — не в самом выводе, а в том, что он **переносится**: на MoE с
двунаправленным вниманием, где кэш вдобавок переиспользуется несвежим, ничего
не ломается.

#### Единственное здесь своё: асимметрия K/V измерена, а не постулирована

KVQuant берёт «на K статику, на V динамику» как рабочую практику. Причину можно
померить, и она одна:

> статическая шкала опирается на то, что **выбросы сидят в фиксированных
> каналах**, а это свойство **K**, не V.

Совпадение восьмёрки крупнейших каналов между полотнами — **0.64–0.76 у K
против 0.27 у V** при случайности 0.01. То есть у K набор «дорогих» каналов
почти один и тот же от полотна к полотну, и калибровка его ловит; у V набор
плавает, и заранее посчитанная шкала мажет — отсюда те самые +24% ошибки.

Это тот же признак, что объясняет и выигрыш оси `token` у K при полном
безразличии V к оси (§2.3): **одно свойство тензора управляет двумя разными
решениями о формате.** Проверяемое следствие: там, где совпадение каналов у K
упадёт, должны разом отвалиться и статика, и ось — их нельзя выбирать
независимо.

Обрезание не мешает: доля упёршихся записей 0.08–0.85%, калиброванный размах
покрывает данные.

#### Воспроизведено на второй архитектуре

`check_static_scales.py --model-type llada`, LLaDA-1.5, 4 полотна, те же две
разрядности и три доли маски. Три итоговые разности во **всех шести** клетках:

```
granularity   100.00% -> 100.00%   одна шкала на канал вместо одной на 128 токенов
transfer      100.00% -> 100.00%   те же шкалы, калиброванные на других полотнах
buckets       100.00% -> 100.00%   что стоило хранить долю маски
```

Единственная клетка с движением помечена самим скриптом как **UNDERPOWERED**:
16 позиций, стандартная ошибка 3.6 пункта против разброса строк 6.2. Чтобы
разрешить такой зазор на двух сигмах, нужно шесть полотен против четырёх снятых.

Совпадает и всё остальное: совпадение старших каналов `K` **0.60–0.68** против
0.64–0.76 у MoE при 0.34–0.42 у `V`; смещение шкалы при переносе **0.16 бита**
против 0.25–0.29 бита, которые стоит хранить динамическая; статика на обеих
сторонах платит **+35…+59%** ошибки против +24% здесь.

**То есть переносится не только структура `K`, но и все следствия из неё.**
KVQuant-подобная схема работает на двух семействах, корзины по доле маски
бесполезны на обоих, `V` не поддаётся на обоих.

**И это загоняет объяснение полa округления в угол.** Структура `K` одинакова,
поведение шкал одинаково — а пол различается в 5–13 раз (§2.7.8). После того как
эта гипотеза и гипотеза про масштаб дефицитов отпали, единственным кандидатом
остаётся роутер, и §4.4 его находит.

**И отрицательное — по самой диффузионной части замысла.** Корзины по доле
маски **не заработали ничего**: одна корзина и пять корзин неразличимы во всех
четырёх таблицах, а по ошибке одна корзина даже чуть лучше в среднем.
Экономию даёт калибровка как таковая, а не диффузионный поворот с корзинами.
Значит метод проще, чем задумывался, и заявлять надо это, а не корзины.

**Оговорка по чтению этого прогона.** Он шёл на коде, где разность помечалась
«real» при одном стандартном отклонении (68%), а строка про нужное число
полотен решала не ту задачу. Обе метки исправлены после прогона; числа в
таблицах верны, пометки — нет.

### 2.6c. Расширить два канала из 128 — впятеро выгоднее, чем добавить бит

Фаза C, пункт 2, развёртка на решениях. 24 полотна, K вдоль токенов, база
3 бита, V держится на базовой ширине во всех строках.

**Сначала потолок для всего пункта.** Полбита, выданные **только K**, снижают
ошибку логитов на 10.9% при доле маски 0.00 и на 16.3% при 0.50. Отсюда прямо:
**K несёт 41% и 60% дисперсии ошибки** соответственно, остальное — V и оконная
арифметика. Значит любой метод, трогающий один K, ограничен сверху 23% и 37%
снижения общей ошибки, даже если сделает K точным. Это касается и ранга, и
каналов.

**Внутри этого потолка каналы выигрывают, и обе таблицы согласны:**

| поправка | цена | маска 0.00 | 0.50 | те же биты плоско |
|---|---|---|---|---|
| ранг 1, факторы 4 бита | +0.02 бита | −0.3% | −2.7% | −0.5% / −0.7% |
| 1 канал +4 бита | +0.01 | +0.5% | −1.7% | −0.2% / −0.4% |
| **2 канала +4 бита** | **+0.03** | **−3.4%** | **−5.6%** | −0.7% / −1.1% |

Два расширенных канала из 128 идут **впятеро** выгоднее, чем те же биты,
потраченные плоско, и одинаково в обеих таблицах (×4.9 и ×5.3). Порядок строк
тоже повторяется: два канала лучше ранга, ранг не хуже одного канала.

**На решениях не видно ничего, и так и должно быть.** `argmax@k` и `slots@k` в
обеих таблицах внутри двух стандартных ошибок; скрипт печатает `UNDERPOWERED`
сам. Эффект в 3–6% ошибки — это доли пункта при разрешении в 2–3 пункта, и
чтобы развести строки при 0.50, понадобилось бы 234 полотна.

**Оговорка по инструменту, важная для чтения.** Столбец ±% рядом с `rel. err` —
это разброс **сложности полотен**, общий для всех строк, а не интервал на
разность. Строки меряются на одних и тех же полотнах, поэтому сравнивать их
надо попарно; парная разность добавлена (`Comparison.paired_delta`, колонка
`vs base`) и её ошибка выходит в несколько раз меньше. Числа выше пересчитаны
по средним и подтверждены совпадением двух независимых таблиц, но парных
интервалов у них ещё нет — они появятся в следующем прогоне.

### 2.7. Текущий блок: устаревание против округления вдоль траектории

Единственное измерение в проекте, где обе ошибки тезиса встречаются. Раньше
кэшировался только префикс, а он точен по построению, так что устаревание было
структурным нулём. `check_block_reuse.py`, 128 токенов = 4 блока по 32,
4 промпта, карта.

Текст **закреплён по эталону**: каждый коммит перезаписывается токеном точного
прогона, оба читают одно полотно, и колонки считают только то, что кэш изменил
на этом шаге. Без этого разошедшиеся траектории мерили бы форк, а не порчу.
128 коммитов на блок, ошибка 2–4 пункта.

**Контроль:** обновление на каждом шаге при 16 битах — 100.0 во всех четырёх
блоках. Переиспользование при нулевом устаревании тождественно, значит порча
ниже — устаревание, а не проводка.

**Цена на блоке 0** (доля перевёрнутых коммитов):

| источник | цена |
|---|---|
| один просроченный шаг (обновление через шаг, 16 бит) | 4.7% |
| четырёхбитное хранение без устаревания | 6.2% |
| обновление раз в 4 шага (16 бит) | 14.1% |
| без обновления внутри блока (16 бит) | **69.5%** |

Первое, что видно: **на рабочей точке две ошибки одного порядка** — один
просроченный шаг стоит примерно как четырёхбитное хранение. Но устаревание
растёт с интервалом обновления куда быстрее: 4.7 → 14.1 → 69.5 при интервале
2 → 4 → 32. **Политика обновления решает больше, чем разрядность.**

#### 2.7.1. Накопления между блоками нет

Совпадение **растёт** с номером блока во всех строках: при обновлении раз в 4
шага 85.9 → 89.8 → 95.3 → 96.1, это 4.0σ. То же при 4 битах: 82.8 → 96.1.

Это закрывает главную незакрытую дыру, и закрывает благоприятно: «четыре бита
бесплатны на шаге, но не на шестнадцати блоках» — не подтвердилось.

Но объяснение — **не разбавление**. Зазор решения по блокам измерен и растёт:
**3.50 → 5.84 → 7.93 → 10.88 логита**, в 3.1 раза к последнему блоку. То есть
поздние решения просто труднее сдвинуть, и дешевеет **любое** возмущение, а не
именно кэш. Это та альтернатива, которую я держал открытой, и данные выбрали её.

Поэтому формулировать надо осторожно: **накопление не опровергнуто, оно не
разрешено.** Совпадение растёт потому, что растёт зазор; есть ли под этим
накопление, покажет только сравнение при **равных зазорах**. Что можно
утверждать: катастрофического накопления, которое перечеркнуло бы оконные
таблицы, нет.

Зазор, растущий вдоль блока, — сам по себе диффузионное наблюдение и сходится
с §2.4, где зазор падает с ростом доли маски: в начале блока всё замаскировано
и решения стоят у ничьей, к концу контекст закрывает выбор.

#### 2.7.2. Меньшая ошибка тонет в большей — субаддитивность подтверждена

Единственный способ ответить — считать не доли, а **сами перевёрнутые позиции**:
текст закреплён по эталону, значит позиция сравнима между конфигурациями, и
множества можно пересекать.

`check_block_reuse.py --samples 8 --bits 16 4`, 1024 зафиксированные позиции на
клетку, база — четыре бита с обновлением каждый шаг (устаревания нет), 40
переворотов.

| строка устаревания | переворотов | общих | от меньшего | объединение | вместе |
|---|---|---|---|---|---|
| через шаг | 51 | 14 | 35% | 77 | **55** |
| раз в 4 шага | 117 | 23 | 58% | 134 | **109** |
| раз в блок | 638 | 35 | 88% | 643 | **635** |

**Вместе ниже объединения во всех трёх строках.** Но читать надо не эту пару:
`together` против `union` — нечувствительный тест, когда одно множество много
больше другого.

**Информативная величина — предельная цена битов:**

```
предельная цена 4 бит при интервале I = together(I) − stale(I)
```

| интервал | предельная цена | поглощение |
|---|---|---|
| каждый шаг | 40 | 0% — калибровка, устаревания нет |
| **через шаг** | **4** | **90%** |
| раз в 4 шага | −8 | ≥100% |
| раз в блок | −3 | ≥100% |

**Начиная с обновления через шаг, девять десятых ущерба от четырёхбитного
хранения уже нанесены устареванием.** С интервала 4 предельная цена битов равна
нулю или отрицательна.

#### Вторая разрядность: поглощение зависит от отношения ошибок

Тот же прогон при `--bits 16 4 3`, база — три бита с обновлением каждый шаг,
**57 переворотов** (предсказано 5.6% от 1024, то есть 57).

| строка устаревания | переворотов | общих | объединение | вместе | предельная цена |
|---|---|---|---|---|---|
| через шаг | 51 | 17 | 91 | **81** | **+30** |
| раз в 4 шага | 117 | 27 | 147 | **128** | **+11** |
| раз в блок | 638 | 50 | 645 | **652** | **+14** |

**Поглощение оказалось не константой.** При обновлении через шаг четыре бита
теряют 90% своей цены, три — только 47%:

| | R | S | вместе | предельная цена | поглощение |
|---|---|---|---|---|---|
| 4 бита | 40 | 51 | 55 | +4 | **90%** |
| 3 бита | 57 | 51 | 81 | +30 | **47%** |

Контраст 26 ± 12 позиций, около **2.2σ**. Прежняя формулировка «биты
поглощаются» верна только там, где округление много меньше устаревания.

#### Механизм: квадратура, а не вложенность

Обе ошибки — векторы смещения в одном пространстве, и то, что читает внимание,
раскладывается точно:

    Q(K_old) − K_new  =  [Q(K_old) − K_old]  +  [K_old − K_new]
                          округление            устаревание

В пространстве такой размерности два не связанных вектора почти ортогональны,
поэтому длины складываются **в квадратуре**, а не линейно. Если число
переворотов растёт с длиной возмущения, отсюда прямое предсказание:

    вместе ≈ √(R² + S²)

Проверка на всех шести клетках:

| биты | строка | вместе | √(R²+S²) | отклонение |
|---|---|---|---|---|
| 4 | n:2 | 55 | 64.8 | −1.3σ |
| 4 | n:4 | 109 | 123.6 | −1.4σ |
| 4 | block | 635 | 639.3 | −0.2σ |
| 3 | n:2 | 81 | 76.5 | +0.5σ |
| 3 | n:4 | 128 | 130.1 | −0.2σ |
| 3 | block | 652 | 640.5 | +0.4σ |

**Ни одного отклонения больше 1.5σ, знаки не систематичны.** Две разрядности,
три интервала. Строки `block` при этом веса нести не могут — 638 из 1024 близко
к насыщению, и линейная связь между длиной и числом переворотов там не
работает; информативны четыре клетки при n:2 и n:4.

Модель объясняет и то, почему поглощение зависит от отношения: прирост равен
`√(R²+S²) − S`, то есть при малом `R` растёт как `R²/2S` — **квадратично**. При
`R/S = 0.78` поглощение почти полное, при `R/S = 1.12` — половинное.

Предсказание про косинус **подтвердилось на тензорах** (§4.1a: −0.001…−0.036,
отношение к квадратуре 0.994–1.000), но **на решениях модель не выжила**:
измерение самих зазоров дало положительную корреляцию сдвигов и композицию ниже
квадратуры. Механизм оказался не «квадратура из ортогональности», а **насыщение
на общих хрупких позициях** — см. §2.7.3. Квадратура верна там, где измерена,
на длинах смещений; на решениях она даёт лишь верхнюю границу, и совпадение
шести клеток с ней в пределах 1.5σ надо читать как совместимость, а не как
подтверждение.

#### Компенсация: возможна, но данными не установлена

Из 40 позиций, которые роняет одно округление, 14 роняет и одно устаревание.
Остаётся 26 «своих» — а при обеих ошибках добавляется всего 4. Отсюда напрашивается
вывод, что 22 позиции устаревание **спасает**: сдвигает логиты так, что округление
перестаёт переваливать через границу решения.

**Данные этого не требуют.** Квадратура даёт тот же дефицит без всякого гашения:
при ортогональных смещениях `вместе` и должно быть заметно ниже `R + S − shared`.
Наблюдаемые 55 против предсказанных 64.8 — это −1.3σ, то есть согласие, а не
остаток, который нужно объяснять компенсацией.

Решающая проверка — знак. Гашение означало бы `вместе` **ниже** большего
слагаемого. При интервале 2 наблюдается 55 против 51, то есть **выше**: суммарного
гашения нет.

Отрицательные приросты всё же есть, и только у четырёх бит: −8 при интервале 4 и
−3 на блоке. Оба в шуме (−1.4σ и −0.2σ), но знак у четырёх бит отрицателен во всех
длинных строках, а у трёх положителен во всех. Если это переживёт замер угла,
компенсация станет находкой; пока это зацепка.

**И числа рядом с ней в скрипт не выводятся.** Сколько позиций округление роняет
в одиночку, а вместе с устареванием нет, из напечатанного не извлечь — нужны
`|R \ together|` и `|together \ R|`. Две строки.

Порядок хрупкости при этом реален и растёт с интервалом: доля переворотов
округления, накрытых одним устареванием, идёт **35% → 58% → 88%** при четырёх
битах и **30% → 47% → 88%** при трёх. Превышение над случайным ожиданием — 7.0×,
5.0×, 1.4× и 6.0×, 4.1×, 1.4×. Обе ошибки роняют одни и те же близкие ничьи, но в
абсолюте пересечение мало, поэтому композиция ведёт себя как квадратура, а не как
вложенность.

#### Что это возвращает

**Правило распределения битов**, снятое предыдущей версией этого раздела:

> Согласовывать интервал обновления с разрядностью и не платить за ошибку,
> которая не является связывающей. На интервале 2 и реже четыре бита стоят
> около нуля сверх того, что уже стоит устаревание.

#### Что было не так в предыдущей версии

Числа в ней верны, **вопрос был не тот**. Она сравнивала `together` с `union`,
получила дефицит 3 из 25 при 0.6σ и заключила независимость.

Пересчёт её же данных правильной формулой: `together − stale` = 22 − 15 = 7 при
базе 14, то есть **поглощение 50%** было видно уже там. Вывод «ущерб независим»
снимается переинтерпретацией собственных данных, а не новым прогоном.

#### Оговорки

**Значимость — оценка.** Дефицит 22 при интервале 2. Для теста Макнемара нужны
расходящиеся позиции `|union \ together|` и `|together \ union|`, а скрипт
печатает только мощности. При любом правдоподобном разбиении выходит
**2.8–4.7σ**; ниже 2σ не опускается ни при каком. Две строки в скрипт — и число
станет точным.

**Строку «раз в блок» в выводы не брать.** Согласие там 25.8 / 33.6 / 40.2 /
51.2 при поле случайности 18.4 / 16.0 / 21.5 / 16.8 — на первых двух блоках
почти пол. Её 62% ущерба частично насыщение, а не сигнал.

**Базовые счётчики старого и нового прогона не сходятся.** База округления 14
при 512 позициях против 40 при 1024, хотя удвоение выборки предсказывает 28.
Строка «раз в блок» воспроизвелась хорошо (60.4% против 62.3%), короткие
интервалы — нет. Конфигурации по флагам идентичны (проверено по истории
вызовов), код между прогонами измерение не менял. Несведено; на выводы не
влияет, потому что новый прогон вдвое больше и внутренне согласован — мощности
множеств совпали с долями таблицы ущерба во всех восьми клетках.

**И отдельно про сквозную метрику.** В первой таблице скрипта «тот же путь» —
это чувствительность, а не качество: четыре бита без всякого устаревания уводят
траекторию (21.5% совпавших токенов), при том что те же четыре бита на GSM8K не
стоят ничего — **90.50% против 90.50%, 181 из 200 в обоих**, при том что сами
множества решённых задач расходятся на двадцать вопросов, ровно по десять в
каждую сторону (McNemar `p = 1.0`). Маршрут другой, пункт назначения тот же —
и теперь не «в пределах шума», а с точностью до одного вопроса.

Пол случайности там **10.74%** (кэш перемешан вдоль оси токенов), и читать
сквозную колонку надо только над ним:

| политика | 16 бит | 4 бита |
|---|---|---|
| каждый шаг | 89.3 | 10.7 |
| через шаг | 22.5 | 10.4 |
| раз в 4 шага | 15.8 | 5.7 |
| раз в блок | 3.9 | **2.4** |

(проценты над полом). Так видно, что политика значит много и при четырёх битах
— просто вся шкала сжата вчетверо. А строка «раз в блок» при четырёх битах
(13.09% при поле 10.74%) практически **на полу**: там траектория не связана с
эталонной ничем, кроме априорных предпочтений модели.

#### 2.7.3. Прямо на зазорах: три предсказания, два не подтвердились

> **Перепрогон на знаковом зазоре сделан 30 августа**
> (`out/margins_signed2.json`), и числа §2.7.3–2.7.7 — уже с него. До 25
> августа `check_block_reuse.py` писал **собственный top-2 зазор клетки**: для
> перевёрнутой позиции это зазор между **другой парой кандидатов**, то есть
> регрессируемая величина переставала быть той же величиной, что и регрессор.
> Теперь пишется `logit(эталонный токен) − max по остальным` — один порядок
> кандидатов во всех клетках, знак меняется ровно при перевороте.
>
> **Проверка прошла:** в эталонной клетке `16/every_n:1` ровно **0
> отрицательных зазоров из 1024**, как обязано быть по построению, а доли
> отрицательных в остальных четырнадцати совпали со строками согласия,
> напечатанными самим прогоном.
>
> **Что правка изменила.** Ни одного вывода не отменила. Уточнила `d_биты`
> (0.172 → **0.160** при четырёх битах, 0.237 → **0.220** при трёх), точку
> перехода поглощения в компенсацию (2.3 → **2.4**) и коэффициент квадратуры
> (0.949 → **0.953**). Закон возраста переподогнан и стал **втрое точнее**
> (§2.7.5). Параллелизм в §2.7.4 не сдвинулся ни на десятую — он считается по
> `top1_prob` и от определения зазора не зависит, что служит контролем: между
> прогонами не изменилось ничего постороннего.
>
> **Гипотеза о строках «блок» опровергнута.** Ожидалось, что низкое `r` там
> (0.198) — следствие загрязнения. На чистом зазоре оно **0.218**, то есть
> практически то же. При 62% переворотов эталонный зазор действительно не
> предсказывает возмущённый; это свойство режима, а не артефакт измерения.
> Знаковая форма показывает то же прямо: свободный член там **−1.27**.

Множества перевёрнутых позиций — пороговый, огрублённый взгляд: позиция
считается, только когда её зазор перешёл ноль, и это выбрасывает и величину
сдвига, и его знак. `--dump-margins` записывает сам зазор на каждой позиции в
каждой конфигурации, `scripts/margins.py` считает по нему три подгонки.
8 промптов, 1024 позиции, шесть клеток, эталон — 16 бит с обновлением каждый
шаг.

**Сжатие зазора: рамка 2608.06564 в этой постановке не воспроизводится.**

| клетка | c | b | r |
|---|---|---|---|
| округление, 4 бита | **0.840** | 0.809 | 0.821 |
| округление, 3 бита | **0.780** | 0.983 | 0.776 |
| **устаревание, n:2** | **0.787** | 0.757 | 0.759 |

При четырёх битах 0.840 против их 0.86 — похоже; при трёх 0.780 против **0.33**
— не похоже. И сравнение с самого начала было натянутым: они квантуют **веса**,
здесь квантуется **кэш**, то есть K/V одного блока, доля вычисления
несопоставимо меньше, и коэффициент ближе к единице ожидаем. Совпадение на
четырёх битах скорее случайно.

Форма `m′ = c·m + b` ложится посредственно: `r` = 0.77–0.83, а свободный член
около **+1.0 логита** — у позиций с малым зазором он не сжимается, а
прибавляется. **Ссылаться на эту рамку стоит, строить на ней — нет.**

**Что всё-таки получено, и этого нет ни у кого:** коэффициент сжатия для
**устаревания** — 0.787 при b = 0.757. Одно просроченное обновление сжимает
зазор примерно так же, как переход на четыре бита (0.840). Развёртка по
возрасту сделана отдельно и дала закон — **§2.7.5**.

**Композиция: ни линейности, ни независимости.**

| биты | политика | наклон | corr(dr, ds) | sd(both) | квадратура | сумма | насыщение |
|---|---|---|---|---|---|---|---|
| 4 | n:2 | **0.479** | **+0.322** | 3.199 | 3.952 | 5.571 | **45%** |
| 3 | n:2 | **0.404** | **+0.381** | 3.408 | 4.149 | 5.866 | — |
| 4 | n:4 | 0.455 | +0.390 | 3.796 | 4.667 | 6.462 | **53%** |
| 4 | block | 0.697 | +0.293 | 4.457 | 5.157 | 7.037 | **47%** |

Колонка **насыщения** — доля позиций, где обе ошибки вместе двигают зазор
**меньше**, чем большая из них поодиночке. Нулевая полоса посчитана
симуляцией: если бы сдвиги складывались точно при наблюдаемой корреляции,
вышло бы 37–40%. Измерено 45–53%, то есть субаддитивность есть и **сверх** той,
что объясняется корреляцией.

Предсказывался наклон 1: сдвиг зазора — линейный функционал возмущения, значит
сдвиги обязаны складываться. **Вышло 0.4–0.5.**

Причина в том, что возмущение не малое: разброс сдвига около 3 логитов при
среднем зазоре 5.5. Первый порядок на таком масштабе неприменим, и аргумент от
линейности снимается.

Предсказывалась нулевая корреляция — вышла **положительная, +0.32…+0.38**. При
этом `sd(both)` = 3.20 сидит **ниже квадратуры** 3.95.

**Тензоры и решения разошлись, и расхождение осмысленно.** В пространстве `K`
косинус около нуля (§4.1a) — смещения почти ортогональны. На решениях эффекты
положительно коррелированы: обе ошибки бьют по одним и тем же хрупким позициям,
толкая зазор в одну сторону, к нулю. **Направление смещения не общее, а мишень
общая.**

А то, что композиция уходит ниже квадратуры, — **насыщение**: зазор, уже
съеденный одной ошибкой, вторая съесть повторно не может.

**Механизм переписывается.** Не «квадратура из ортогональности», как
формулировалось по тензорам, а **насыщение на общих хрупких позициях**.
Квадратура остаётся верной там, где измерена — на длинах смещений; на решениях
она даёт лишь верхнюю границу. Измеренные результаты — пол, курс обмена,
падение предельной цены битов — от смены объяснения не зависят.

Прямая проверка насыщения: доля позиций, у которых `|d_b| < max(|d_r|, |d_s|)`.
Одна строка в `margins.py`, прогона не требует.

#### 2.7.4. Потолок параллелизма есть, но его создаёт не разрядность

Сэмплер LLaDA2.0 фиксирует за шаг все позиции, чья вероятность первого
кандидата выше **0.95**, но не меньше квоты расписания. Значит доля позиций над
порогом — это и есть достижимый параллелизм. Тот же дамп её печатает.

| клетка | блок 0 | блок 1 | блок 2 | блок 3 | всего |
|---|---|---|---|---|---|
| эталон, 16 бит, каждый шаг | **32.8%** | 68.4 | 65.2 | 68.0 | **58.6%** |
| 4 бита, каждый шаг | 30.9 | 66.8 | 65.2 | 69.1 | 58.0 |
| 3 бита, каждый шаг | 34.0 | 62.9 | 65.2 | 67.2 | 57.3 |
| устаревание, через шаг | 28.1 | 62.9 | 60.2 | 64.5 | 53.9 |
| устаревание, раз в 4 шага | 26.6 | 49.6 | 47.7 | 53.1 | 44.2 |
| **устаревание, раз в блок** | **1.2** | **3.1** | **5.9** | **7.8** | **4.5%** |
| обе, 4 бита, через шаг | 23.4 | 62.5 | 61.3 | 65.2 | 53.1 |
| **обе, 4 бита, раз в блок** | **1.2** | 3.1 | 7.4 | 7.8 | **4.9%** |

**Разрядность сама по себе долю не снижает** — при трёх битах в первом блоке
даже 34.0% против 32.8% у эталона. Предсказание, выведенное из коэффициента
сжатия, не подтвердилось; оно и опиралось на рамку, которая не перенеслась.

**Снижает переиспользование.** Числа на возрастах, которые вообще мыслимы как
рабочие:

| возраст записи | доля выше порога |
|---|---|
| 0 (пересчёт каждый шаг) | **58.6%** |
| 1 | 53.9% |
| 2 | 44.2% |
| **4** | **37.6%** |

Монотонно. И на возрасте 4 разрядность впервые становится видна: 37.6% при
шестнадцати битах против **34.1%** при трёх — три с половиной пункта.

С обеими ошибками при возрасте 1 первый блок падает с 32.8% до **23.4%**, на
29% относительно, при том что поздние теряют 3–6%.

**Строка «раз в блок» — верхняя граница, а не рабочая точка.** Там доля
падает до 4.5%.

> **Отозвано 3 сентября: «в тринадцать раз» — это не про шаги.** Здесь стояло,
> что параллельное декодирование перестаёт работать вовсе. Доля выше порога
> действительно падает в тринадцать раз, но **в число шагов это не переводится**:
> прямое измерение (§5) даёт **1.75×**, потому что даже одна уверенная позиция
> за шаг двигает блок. Долю читать как долю, а цену — только из `check_parallelism`.

Возраст записи там доходит до 31 шага, и **так не делает никто**: во всех трёх
режимах кэша dInfer текущий блок пересчитывается каждый шаг, а `cache_update_iter`
по умолчанию `None`. Читать её надо как оценку того, во что переиспользование
может обойтись в пределе, и не выносить в заголовочные числа.

> **Сильнее того — на блочно-каузальной модели устаревания нет вовсе.** Префикс
> от текущего блока не зависит, значит держать его точно, а не приближённо. Вся
> ось возраста здесь описывает режим, которого никто не включает; её адресат —
> двунаправленные модели, где переиспользование приближённо по построению (§5).

Разрядность к этому по-прежнему почти ничего не добавляет — 4.9% при четырёх
битах против 4.5% при шестнадцати.

Локализация предсказана верно: зазор в первом блоке 3.37 логита против 6.4–7.3
дальше, и порог там пересечь легче всего в обе стороны.

**Формулировка:**

> При возрасте записи в два шага доля позиций, проходящих порог уверенности,
> падает с 58.6% до 44.2%. Параллельное декодирование опирается на
> уверенность, а устаревание уверенность и съедает — значит переиспользование
> кэша и параллелизм торгуются друг против друга, а разрядность к этому почти
> ничего не добавляет. В пределе, при отказе от обновления внутри блока, доля
> падает до 4.5%; это верхняя граница, режим за пределами того, что поставляется.
> **Во сколько это обходится по времени — см. §5: 1.75×, а не тринадцать.**

Мишень — dInfer, Fast-dLLM, DMax: все трое продают токены за форвард и меряют
против полного пересчёта, то есть часть выигрыша в параллелизме оплачена
потерей, которую они не считают. DMax отчитывает 2.04 → 5.47 токена за форвард
на этой же модели.

**Оговорка.** Доля над порогом — не то же самое, что число зафиксированных за
шаг: наше расписание фиксирует ровно квоту, а родной сэмплер берёт максимум из
квоты и числа прошедших порог. Измеренное — верхняя оценка параллелизма, а не
он сам. Прямое измерение требует родного сэмплера и отдельного прогона.

#### 2.7.5. Закон сжатия зазора по возрасту записи

Развёртка того же измерения по четырём возрастам. `--bits 16 4 --policies
every_n:1 every_n:2 every_n:4 block`, 1024 позиции на клетку, эталон — 16 бит с
обновлением каждый шаг.

**Устаревание сжимает зазор решения мультипликативно, и коэффициент падает
монотонно:**

| возраст записи | политика | c | b | r |
|---|---|---|---|---|
| 0 | `every_n:1` | 1.000 | 0 | 1 — эталон по построению |
| **1** | `every_n:2` | **0.787** | 0.757 | 0.759 |
| **2** | `every_n:4` | **0.663** | 0.616 | 0.615 |
| **16** | `block` | **0.135** | −1.266 | **0.218** |

Четвёртая точка снята отдельным прогоном — возраст 4 даёт **c = 0.503**, — и по
четырём точкам форма определяется: это **растянутая экспонента**

    c = exp(−0.240 · возраст^0.762)

| возраст | 1 | 2 | 4 | 16 |
|---|---|---|---|---|
| измерено | 0.787 | 0.663 | **0.503** | 0.135 |
| формула | **0.786** | **0.665** | **0.501** | **0.137** |
| **корреляция регрессии `r`** | **0.759** | **0.615** | — | **0.218** |
| прежняя подгонка (0.253, 0.84) | 0.776 | 0.636 | 0.445 | 0.074 |

Максимальный промах **0.0022** — на всех четырёх точках. Прежняя подгонка,
снятая на загрязнённом зазоре, мазала до 6 пунктов на возрасте 4 и вдвое на 16;
на чистом зазоре форма определяется настолько точно, что подгонка перестала
быть подгонкой. Показатель **0.762 < 1** значит, что устаревание накапливается
**медленнее линейного** — то же насыщение, что и в композиции двух ошибок, и
выражено оно сильнее, чем казалось. Полная форма с обеими ручками — §2.7.6.

**Читать так: каждый шаг устаревания сжимает зазор решения примерно на 21%.**

**Дальняя точка закон не несёт, а подтверждает.** Возраст 16 — это отказ от
обновления внутри блока, режим за пределами того, что поставляет кто бы то ни
было, и там же `r` падает до 0.185, то есть регрессия почти вырождена.
Проверка: подгонка только по возрастам 1 и 2 даёт λ = 0.239, α = 0.766, и
экстраполяция на 16 предсказывает **0.135** против измеренных **0.135** — на
чистом зазоре дальняя точка ложится на закон, построенный без неё. Значит закон стоит на ближних точках, а дальняя служит
независимой проверкой, а не опорой.

**Заявлять надо на рабочем диапазоне.** Возраст 1–2 — то, что мыслимо как
настройка: там `c` = 0.787 и 0.663, и этого достаточно, чтобы поставить
устаревание в один ряд с разрядностью. Возраст 16 подавать отдельно и как
верхнюю границу.

И `r` падает вместе с `c`: 0.759 → 0.615 → **0.218**. На блочном уровне
эталонный зазор перестаёт предсказывать возмущённый вовсе — решения
декоррелированы.

#### Это прямой аналог их закона, по оси, которой у них нет

2608.06564 померила коэффициент по **разрядности**: 0.86 / 0.33 / 0.00 при
4 / 3 / 2 битах. Здесь тот же коэффициент померен по **возрасту**: 0.77 / 0.63
/ 0.07 при 1 / 2 / 16 шагах. Одинаковая форма, одинаковый коллапс к нулю.

Общая шкала даёт курс между двумя ошибками — впервые не в перевёрнутых
позициях, а в самой величине, которая их определяет:

| | c |
|---|---|
| округление, 4 бита | 0.840 |
| **один просроченный шаг** | **0.787** |
| округление, 3 бита | 0.780 |
| два просроченных шага | 0.663 |
| отказ от обновления в блоке | 0.135 |

**Один просроченный шаг стоит примерно как квантизация кэша в четыре бита.**
Два шага дороже трёх бит. Отказ от обновления внутри блока — коллапс,
сопоставимый с их двухбитными весами.

Это самая переносимая форма результата: она выражает обе ошибки в одной
величине и не зависит ни от длины блока, ни от числа токенов за шаг, ни от
метрики ущерба.

**Оговорка, которая остаётся.** Их коэффициент померен для квантизации
**весов** на авторегрессионных моделях, наш — для **кэша** на диффузионной.
Совпадение 0.840 с их 0.86 при четырёх битах скорее случайно, а при трёх битах
(0.780 против 0.33) расхождение уже явное. Сравнивать надо **внутри** нашей
таблицы; их числа — ориентир формы, а не значения.

#### 2.7.6. Замкнутая формула на два аргумента

> **Коэффициент 0.95 отозван 1 сентября: он локален.** На LLaDA-1.5, где
> дефициты впятеро меньше, отношение измеренного к квадратуре **1.154**
> (0.945–1.387) против **0.953** здесь — композиция там выше квадратуры, ближе
> к простому сложению, и колонка `diff` в §2.7.7 меняет знак. Квадратура
> оказалась видом одной кривой вблизи рабочей точки этой модели, а не законом.
> Что переносится и в какой форме — §2.7.8. Формула ниже верна там, где
> измерена, и подавать её надо так.

Всё измеренное в §2.7 и §4.1 сводится к одному выражению. Обозначим `d = 1 − c`
— долю зазора, которую ошибка съела.

    d(возраст, биты) ≈ 0.95 · √( d_возраст² + d_биты² )     [только LLaDA2.0-mini]

        d_возраст = 1 − exp(−0.240 · возраст^0.762)
        d_биты    = 0.160 при 4 битах,  0.220 при 3

Два аргумента, замкнутый вид. Ниже — обе половины и проверка.

**Первая половина: дефициты складываются в квадратуре.** Прямоугольник закрыт
целиком — две разрядности × четыре возраста:

| клетка | возраст | d измер. | квадратура | измер/квадр | d_s/d_r |
|---|---|---|---|---|---|
| 4 бита | 1 | 0.265 | 0.266 | **0.995** | 1.33 |
| 4 бита | 2 | 0.350 | 0.373 | **0.938** | 2.11 |
| 4 бита | 4 | 0.460 | 0.522 | **0.881** | 3.11 |
| 4 бита | 16 | 0.861 | 0.880 | **0.979** | 5.41 |
| 3 бита | 1 | 0.280 | 0.306 | **0.914** | 0.97 |
| 3 бита | 2 | 0.391 | 0.403 | **0.972** | 1.53 |
| 3 бита | 4 | 0.518 | 0.544 | **0.953** | 2.26 |
| 3 бита | 16 | 0.886 | 0.893 | **0.993** | 3.93 |

**Восемь клеток: 0.881 … 0.995, среднее 0.953, sd 0.037.** Недобор в 5%
систематический, и он опознан: детерминированная компенсация от обрезания
квантователя (§4.1b). Структуры в остатке нет — по отношению `d_биты/d_возраст`
он не упорядочен, самое низкое значение приходится на середину диапазона.

Конкуренты отвергаются с запасом. **Мультипликативное сжатие** `c_возраст ·
c_биты` — то, что напрашивается из формы `m′ = c·m + b`, — мажет на 15–36%.
Простая сумма дефицитов мажет вдвое.

**Различают правила не крайние клетки, а средние.** В углу (возраст 16, три
бита) квадратура даёт 0.960, мультипликативное 0.947, сумма упирается в
потолок — разница 1.3 пункта при разбросе коэффициента 0.032, различить нечем.
Разделяют клетки со средними и сопоставимыми дефицитами: три бита при возрастах
1, 2, 4, где предсказания расходятся на 6–8 пунктов и наблюдения ложатся
однозначно. Угол закрывает прямоугольник и проверяет, держится ли форма у
потолка насыщения — но фальсифицировать закон он не может.

**Вторая половина: возраст входит растянутой экспонентой.**

| возраст | измерено | `exp(−0.240·age^0.762)` | чистая экспонента |
|---|---|---|---|
| 1 | 0.787 | **0.786** | 0.845 |
| 2 | 0.663 | **0.665** | 0.713 |
| 4 | 0.503 | **0.501** | 0.509 |
| 16 | 0.135 | **0.137** | 0.067 |

**Строку 16 читать с поправкой на её `r`.** Корреляция регрессии там **0.218**
против 0.759 и 0.615 на возрастах 1 и 2: эталонный зазор почти не предсказывает
возмущённый, и §2.7.7 помечает те же строки курсивом как ненадёжные. Отвержение
чистой экспоненты на этой точке не держится — но и не нуждается в ней: на
возрасте 1 чистая экспонента мажет на 5.8 пункта при `r = 0.759`, и этого
достаточно. Для возраста 4 `r` не записан, прогон был отдельный.

Максимальный промах **0.0022** против 5 пунктов у чистой экспоненты (λ = 0.169).

Показатель **0.762 < 1** содержателен: устаревание накапливается **медленнее
линейного**, каждый следующий просроченный шаг добавляет чуть меньше
предыдущего. Это то же насыщение, что видно на композиции двух ошибок, — зазор,
уже съеденный, второй раз не съесть, и от возраста тоже.

#### Почему это спина работы

Квадратура **наблюдается** на трёх уровнях независимо — но **выведена только на
первом**, и это важное различие, разобранное в §2.7.7:

| уровень | чем проверено | точность |
|---|---|---|
| векторы смещения в пространстве `K` | косинус ≈ 0, `\|r+s\|` против `√(\|r\|²+\|s\|²)` | 0.994–1.001 |
| **сжатие зазора решения** | **таблица выше** | 0.88–0.99 |
| число перевёрнутых решений | шесть клеток против `√(R²+S²)` | в пределах 1.5σ |

Тензоры → зазоры → решения. Три разные величины, три разных инструмента.

**И средний уровень оказался местным.** На LLaDA-1.5 то же отношение даёт
1.154 на префиксной оси и 0.763 на оконной против 0.953 здесь (§2.7.8), так что
из трёх строк переносится верхняя — почти-ортогональность смещений — и
структура разложения, но не коэффициент. Называть это спиной работы можно
только про `κ`, не про квадратуру.

**Но связи между уровнями нет.** Наивный мост через `d = f(|δ|)` даёт неверное
предсказание, потому что `f` вогнута: один бит удваивает `|r|` (×2.10), а
`d_биты` растёт лишь в 1.38 раза. Формулировку «один закон на трёх уровнях»
приходится снять — совпадение форм на верхних двух уровнях случайно, а точное
разложение существует и выглядит иначе (§2.7.7).

И из формулы **выводится** то, что раньше мерилось порознь:

* **пол округления** — при `d_возраст = 0` остаётся `0.95·d_биты`, и обновлением
  это не убрать;
* **отдача от лишнего бита** — `∂d/∂d_биты = d_биты/√(d_возраст² + d_биты²)`, то
  есть **равна доле битов в суммарном дефиците**: 0.61 при возрасте 1 и 0.18 при
  возрасте 16;
* **зависимость поглощения от отношения ошибок** — прямое следствие того, что
  малое слагаемое входит в квадратуре.

#### Правило распределения, теперь выводимое

> Тратить биты имеет смысл, пока `d_биты` сравним с `d_возраст`. Дальше отдача
> падает как их отношение, и лишний бит покупает долю, равную своему весу в
> квадратичной сумме.

Это не эвристика и не подгонка под наблюдения — это производная формулы,
проверенной на семи клетках.

#### Оговорки

`d_биты` измерено на двух разрядностях, `d_возраст` на четырёх возрастах.
Показатель 0.84 подогнан по четырём точкам и на возрасте 4 промахивается на 5%.
Коэффициент 0.95 — среднее по восьми клеткам, sd 0.032; считать его константой
можно только в этом диапазоне.

И вся формула снята на **неповёрнутой** модели с bf16 весами: она описывает
кэш, а не развёрнутую конфигурацию.

#### 2.7.7. Точное разложение: вся субаддитивность сидит в одном числе

Квадратура выше подана как закон. Строго говоря, **на уровне зазоров она не
выведена, а совпала численно**, и это стоит сказать прямо, потому что вывод
есть — только для другой величины.

**Что выводится.** Коэффициент `c` — наклон регрессии, то есть

    c = cov(m_ref, m_cell) / var(m_ref),      m_cell = m_ref + Δm
    ⟹  d = 1 − c = − cov(m_ref, Δm) / var(m_ref)

Ковариация билинейна. Значит **если** сдвиги складываются, дефициты
складываются **точно**:

    Δm_вместе = Δm_r + Δm_s   ⟹   d_вместе = d_r + d_s

Это алгебра: ни размерности, ни концентрации меры, ни допущений.

> **И потому «это же просто ANOVA» надо снимать самим, а не ждать от
> рецензента.** Разложение «главные эффекты плюс взаимодействие» — двадцатые
> годы: ANOVA, индексы Соболя, значения Шепли, бюджет ошибок в инженерии, теория
> возмущений. Для любого линейного функционала аддитивность точна, а остаток
> есть определение, а не находка. Правильный ответ на возражение — **«да»**.
>
> **Вклад не в формуле, а в выборе измеряемой величины.** Работы по квантизации
> меряют ущерб нормами: относительная ошибка, MSE, прирост перплексии, доля
> перевёрнутых `argmax`. **Ни одна из них не линейна по возмущению** —
> `‖A+B‖ ≠ ‖A‖+‖B‖`, — поэтому точно разложить их нельзя, и на их месте
> появляется квадратура как эмпирическое наблюдение, что ошибки складываются
> «примерно как катеты». Наклон регрессии зазора линеен, и потому раскладывается
> **точно**. Найдено не соотношение, а **что мерить, чтобы соотношение было
> точным**; всё остальное следует.
>
> **Второе возражение, тоже справедливое:** линейность — артефакт величины
> первого порядка, а вся нелинейность **определена внутрь `κ`**. Отвечать надо не
> отрицанием, а поведением `κ`: она растёт с отношением ошибок, пересекает
> единицу, даёт компенсацию, и её диапазон совпадает у двух архитектур
> (0.675–1.231 против 0.754–1.224). Будь `κ` шумом или константой, тезис был бы
> пустым.
>
> **Проверка по литературе здесь слабая, и это надо признавать.** Форма
> настолько общая, что в какой-нибудь области она наверняка есть; искать её —
> примерно как искать «мы взяли производную». Значимо другое: к композиции ошибок
> KV-кэша её никто не прикладывал, и линейно-разложимую метрику ущерба никто не
> выбирал. Работы по кэшу считают накопление ошибки (KVarN) и делят биты (RDKV),
> но композицию двух источников как отдельный объект не ставят.
>
> **Формулировка, которая не переоценивает:** ущерб кэша, измеренный как сжатие
> зазора решения, — линейный функционал возмущения; поэтому две ошибки
> раскладываются точно, взаимодействие сводится к одному скаляру, и этот скаляр
> измерим. Мы его измеряем и находим три режима, включая компенсацию. Претензии
> на алгебру здесь нет.

**Отсюда всё взаимодействие сводится к одной величине.** Пусть `ρ` — невязка
аддитивности сдвигов:

    ρ = Δm_вместе − Δm_r − Δm_s
    κ = cov(m_ref, ρ) / var(m_ref)

Тогда **тождественно**:

    d_вместе = d_r + d_s − κ

Проверка на клетке «4 бита, возраст 1»:

| | значение |
|---|---|
| сумма дефицитов | 0.398 |
| квадратура | 0.284 |
| **измерено** | **0.278** |
| **κ измеренное** | **0.120** |
| κ, требуемое квадратурой | 0.114 |
| расхождение | **0.006** |

**Квадратура — не закон, а численное совпадение на измеренном диапазоне.**
Настоящая величина — `κ`, и она измерима напрямую, без подгонки.

#### Почему сдвиги и дефициты ведут себя по-разному

Это снимает противоречие, которое иначе выглядит необъяснимым:

| величина | отношение к квадратуре |
|---|---|
| сами сдвиги, `sd(вместе)` против `√(sd_r² + sd_s²)` | **0.809** |
| дефициты `d` | **0.979** |

Разные величины с разной структурой. У `d` есть точное аддитивное разложение с
единственным поправочным членом; у сдвигов такого нет. Совпадение форм на двух
уровнях — случайность, и формулировку «один закон на трёх уровнях» надо снять.

**Что остаётся выведенным:** квадратура на **смещениях в пространстве `K`** —
там разложение точное (`|r+s|² = |r|² + |s|² + 2(r·s)`), почти-ортогональность
следует из концентрации меры, и косинус измерен (−0.001 … −0.036 при нулевой
полосе ±0.008).

**Что не выводится:** переход от смещений к дефициту зазора. Наивная связь через
`d = f(|δ|)` даёт **неверное** предсказание: `f` заведомо вогнута — один бит
удваивает `|r|` (×2.10), а `d_биты` растёт только в 1.38 раза, то есть
`f(x) ∝ x^0.43`. Подстановка даёт 0.238 против измеренных 0.278, тогда как
квадратура по дефицитам даёт 0.270. Мост между уровнями не держит.

#### Правильная формулировка механизма

> Дефицит сжатия зазора **аддитивен по построению**. Всё взаимодействие двух
> ошибок сводится к одной измеримой величине — невязке аддитивности сдвигов,
> спроецированной на эталонный зазор. На измеренном диапазоне эта невязка
> численно близка к той, что даёт квадратура, но это следствие, а не причина.

Это выводимо, а не подогнано, и оно указывает, что именно надо объяснять:
**почему `ρ` коррелирует с `m_ref` и с каким коэффициентом.**

#### κ измерена: поглощение переходит в компенсацию

`scripts/margins.py`, секция 5, на `out/margins_signed2.json` — знаковый зазор
в эталонном порядке. Тождество проверено численно:
`κ`, посчитанная из дефицитов, и `κ`, посчитанная напрямую из невязки `ρ`,
совпали **во всех восьми клетках** до третьего знака. Отдельная проверка —
эталон против себя даёт `d = −0.0000`, ровно ноль, как и обязан.

| разрядность | возраст | d_r | d_s | d_вместе | **κ** | **κ/d_r** | d_s/d_r | нужно кв. | разн |
|---|---|---|---|---|---|---|---|---|---|
| 3 бита | 1 | 0.220 | 0.213 | 0.280 | 0.153 | **0.695** | 0.97 | 0.127 | +0.026 |
| 4 бита | 1 | 0.160 | 0.213 | 0.265 | 0.108 | **0.677** | 1.33 | 0.107 | +0.002 |
| 3 бита | 2 | 0.220 | 0.337 | 0.391 | 0.166 | **0.755** | 1.53 | 0.155 | +0.012 |
| 4 бита | 2 | 0.160 | 0.337 | 0.350 | 0.147 | **0.919** | 2.11 | 0.124 | +0.023 |
| 3 бита | 4 | 0.220 | 0.497 | 0.518 | 0.199 | **0.902** | 2.26 | 0.173 | +0.025 |
| 4 бита | 4 | 0.160 | 0.497 | 0.460 | 0.197 | **1.229** | 3.11 | 0.135 | +0.062 |
| *3 бита* | *16* | *0.220* | *0.865* | *0.886* | *0.199* | *0.906* | *3.93* | *0.193* | *+0.007* |
| *4 бита* | *16* | *0.160* | *0.865* | *0.861* | *0.164* | *1.025* | *5.41* | *0.145* | *+0.019* |

**Строки с возрастом 16 курсивом — им нельзя верить.** Корреляция в регрессии
там **0.190–0.224**, эталонный зазор почти не предсказывает возмущённый, и `c`
определён плохо. Выводы ниже — по шести надёжным клеткам, где `r` = 0.47–0.82.

Это проверено, а не предположено: на загрязнённом зазоре `r` там был 0.198, и
ожидалось, что чистое определение его подтянет. Не подтянуло — **0.218**.
Значит дело не в измерении: при 62% переворотов эталонный зазор действительно
не несёт информации о возмущённом, и строка мертва по существу режима.

#### Три режима вместо одного

    d_s/d_r:   0.97   1.33   1.53   2.11   2.26   3.11
    κ/d_r:     0.695  0.677  0.755  0.919  0.902  1.229

Растёт монотонно, с одной перестановкой в каждом конце. И `κ/d_r = 1` — это не
предел, а **точка пересечения**:

| `d_s/d_r` | что происходит |
|---|---|
| около 1 | поглощается ~70% меньшей ошибки |
| **≈ 2.4** | **полное поглощение**: `κ = d_r`, значит `d_вместе = d_s` ровно, меньшая ошибка не добавляет ничего |
| выше | **компенсация**: `d_вместе < d_s`, добавление меньшей ошибки суммарный дефицит **снижает** |

При `d_s/d_r` = 3.11 выходит `d_вместе` = 0.460 против `d_s` = 0.497. Разница
0.037 при `r` = 0.50, то есть регрессия там определена нормально и эффект вне
шума.

Раньше компенсация виднелась только в множествах переворотов как отрицательные
приросты (−8 и −3) в пределах шума. На непрерывной величине она измерена.

**Формулировку «меньшая ошибка тонет в большей» надо заменить на эти три
режима** — она верна лишь в среднем диапазоне.

#### И это объясняет систематический недобор квадратуры

Колонка «разн» положительна **во всех восьми клетках**, от +0.002 до +0.062:
измеренная `κ` всегда **больше** той, что нужна квадратуре, в 1.02–1.46 раза.

Отсюда и то, что отношение измеренного `d` к квадратуре во всех клетках ниже
единицы (0.881…0.995). Не «примерно верна с шумом», а **верна с систематическим
знаком**, и знак объясняется через `κ`.

#### Что осталось необъяснённым

`κ` — не константа и не пропорциональна `d_r`. Она растёт с `d_s` и, похоже,
упирается в потолок около **0.20** при обеих разрядностях: при `d_s` = 0.497
получается 0.197 и 0.199, хотя `d_r` различается в 1.38 раза, и на возрасте 16
дальше не идёт (0.164 и 0.199). Почему потолок и почему именно там — открыто.
На чистом зазоре потолок стал видён отчётливее: три из четырёх старших клеток
лежат в 0.197–0.199.

И откуда берётся компенсация. Косинус между смещениями на тензорах отрицателен,
но мал (−0.001 … −0.036 на слоях 4–16), и этого недостаточно, чтобы объяснить
`κ/d_r` выше единицы. Значит на уровне решений действует что-то сверх
геометрии смещений — вероятнее всего то же насыщение, что даёт наклон 0.43–0.48
в регрессии сдвигов и долю 42–54% позиций, где обе ошибки вместе двигают зазор
меньше большей из них.

#### 2.7.8. Вне выборки: переносится структура, не коэффициент

> **Переписано 1 сентября после того, как у плотного пути появился оконный
> стор.** Первая версия этого раздела сравнивала LLaDA2.0 с LLaDA-1.5 на
> **разных осях** — там старел текущий блок, здесь префикс — и делала из этого
> вывод про «одну кривую `κ` через две архитектуры». Вывод снят: он был
> артефактом объединения двух разных измерений. Ниже — обе оси порознь.

Три сетки, по пятнадцать клеток каждая, один скрипт и одни настройки:
LLaDA2.0 (текущий блок), LLaDA-1.5 с оконным стором (текущий блок, сравнимо) и
LLaDA-1.5 с префиксным (ось, которой у блочно-каузальной модели нет).

| | `κ/d_r` | измер/квадратура | точка перехода `d_s/d_r` |
|---|---|---|---|
| LLaDA2.0-mini | 0.675 – **1.231** | 0.953 | ≈ 2.4 |
| LLaDA-1.5, окно | 0.754 – **1.224** | **0.763** | ≈ 1.4 |
| LLaDA-1.5, префикс | −0.04 – 0.68 | 1.154 | не достигается |

**Переносится структура.** Диапазон `κ/d_r` на сравнимой оси совпал почти
точно, и вместе с ним три режима: поглощение, переход через единицу,
компенсация. На 1.5 компенсация в трёх клетках — 0.158 против `d_s` = 0.162,
0.101 против 0.116, 0.125 против 0.144, все при четырёх битах.

**Не переносится коэффициент.** 0.763 / 0.953 / 1.154 на трёх сетках, и точка
перехода 1.4 против 2.4. Квадратура — местная подгонка, и §2.7.6 помечен
соответственно.

#### Пол округления на плотной модели в 5–13 раз меньше

При `every_n:1`, то есть без устаревания вовсе:

| | 4 бита | 3 бита |
|---|---|---|
| LLaDA2.0-mini | **3.7%** | **5.4%** |
| LLaDA-1.5 | **0.7%** | **0.4%** |
| отношение | **5.3×** | **13×** |

Обе строки — доля изменённых зафиксированных решений при `every_n:1` из
закреплённой таблицы `check_block_reuse`, один скрипт, полотно 128, блок 32,
группа 128, ось `token`, восемь промптов. Числа 2.0 взяты из `margins_signed2`,
чтобы совпадать с остальным §2.7; прежняя сетка §2.9 давала на тех же клетках
3.9 и 5.6.

Квантизация кэша на плотной модели почти бесплатна, на MoE стоит измеримо.

> **Атрибуция к роутеру не подтвердилась — 3 сентября.** Здесь стояло, что
> усилитель найден: роутер превращает малое возмущение в дискретный переворот
> маршрута (§4.3, пик дрейфа 3.65 против 0.111 при закреплённых маршрутах). Это
> измерение верно, но **разрыв пола оно не объясняет**, и вот прямая проверка.
>
> `--key-bits` против `--value-bits` при равной разрядности, `every_n:1`,
> 4 бита:
>
> | квантован | ущерб | переворотов |
> |---|---|---|
> | только `K` | 2.34% | 12 |
> | только `V` | 3.32% | 17 |
> | оба | 4.49% | 23 |
>
> **Если бы усилителем был софтмакс и через него роутер, доминировать должен
> `K`** — он формирует веса внимания, `V` в софтмакс не попадает. Он не
> доминирует: разница 0.98 пункта, **0.94σ, неразличимо**, и номинально `V`
> даже больше.
>
> Сопоставимый вклад — это подпись **GQA**: у 2.0 четыре головы `K` и четыре
> `V` читаются шестнадцатью запросами, у 1.5 — одна к одной. Обе стороны делят
> этот веер поровну, поэтому и портят поровну.
>
> **Переснято на 18 промптах, и «не различили» стало «равны».**
>
> | квантован | ущерб | переворотов из 2304 |
> |---|---|---|
> | только `K` | **2.865%** | 66 |
> | только `V` | **2.821%** | 65 |
>
> Разница — **один переворот, 0.043 пункта, 0.09σ**. Оба прогона делят один
> эталон (зазоры `3.75 5.51 6.80 8.11`, пол 12.54%), так что сравнение парное.
> Разрешение теперь таково, что эффект **больше 34% от `K`** был бы виден; его
> нет. Прежняя разница 2.34 против 3.32 на четырёх промптах была шумом, как
> 0.94σ и говорили.
>
> **Гипотеза софтмакса опровергнута, а не просто неподтверждена.** `V` не входит
> в софтмакс вовсе; если бы усилителем был он, `K` обязан был бы превысить `V`.
> Вклады равны.
>
> **Вывод:** разрыв 5–13× **не объясняется маршрутизацией**. Равный вклад двух
> тензоров — подпись **GQA**: четыре головы `K` и четыре `V` читаются
> шестнадцатью запросами, обе стороны делят один веер. У LLaDA-1.5 веера нет
> (32 на 32), и пол там в 5–13 раз ниже.
>
> Оговорка измерения: `K` стоит на своей лучшей оси (`token`), `V` — на
> канальной. §2.6 говорит, что ось `V` безразлична, но подтвердить это на
> текущей установке стоит.

#### Закон держится на паре, которой не видел: `K` против `V`

Переснято на 18 промптах, 2304 зафиксированных позиции, общий эталон:

| квантован | ущерб | переворотов |
|---|---|---|
| только `K` | 2.865% | 66 |
| только `V` | 2.821% | 65 |
| **оба** | **3.429%** | **79** |

Сумма одиночных — 5.686% (131 позиция). Вместе — 3.429% (79). **Вместе
составляет 60% от суммы**, `κ = 2.257` пункта.

**Насколько это далеко от независимости.** Если бы `K` и `V` переворачивали
позиции независимо, пересечение составило бы `2304 × 0.0287 × 0.0282 = 1.9`
позиции. Недобор — **52**, то есть в **28 раз** больше случайного. Они сбивают
одни и те же ничьи, а не разные.

**И режим предсказан верно.** Отношение `d_V/d_K = 0.98` — далеко от точки
перехода 2.4, значит **поглощение**, а не компенсация. Так и есть: `d_вместе`
больше каждого из одиночных. `κ/d_K = 0.788`.

> **Что это добавляет к §2.7.7.** Закон выводился и мерился на паре
> «устаревание × округление». Здесь он приложен к паре, которой в его выводе не
> было вовсе — двум тензорам одного кэша, — и субаддитивность той же силы. Значит
> закон описывает не конкретную пару ошибок, а то, как **любые два возмущения
> делят между собой одни и те же ничьи**.
>
> **Две оговорки.** Первая: 52 — это **недобор суммы**, а не измеренное
> пересечение; прогон «оба» мог перевернуть позиции, которых не трогал ни один
> одиночный, и инклюзия-эксклюзия здесь допущение. Вторая: `κ` в §2.7.7 считается
> регрессией зазоров, здесь — счётом переворотов. Величины разные, и близость
> `κ/d` (0.788 против 0.695 при том же отношении) читается как согласие по
> порядку, а не как одно измерение.

Это самое переносимое из всего, что дало сравнение: не число, а механизм,
объясняющий, **почему** числа разные.

#### Обе модели: кэш ничего не стоит на задаче

| | без кэша | с кэшем 16 бит | политика | McNemar |
|---|---|---|---|---|
| LLaDA-1.5, полотно 512 | **83.50%** | 80.50% | `every_n:4`, префикс стареет | **p = 0.263** |
| LLaDA2.0-mini, полотно 512 | 91.50% | **90.50%** | `every_n:4`, префикс точен | знак не установлен |

> **Строки меряют разное, и это надо держать в голове.** У LLaDA2.0 под
> блочно-каузальной маской шестнадцатибитный префиксный кэш **точен по
> построению** — старение туда попасть не может. У LLaDA-1.5 внимание
> двунаправленное, и при том же `every_n:4` префикс **действительно стареет**.
> То есть верхняя строка это цена приближения, нижняя — цена ничего. Что обе
> внутри шума, содержательно; что они сравнимы между собой — нет.

На 1.5 три пункта в минус при `p = 0.263`. На 2.0 два прогона одной и той же
конфигурации дали 90.50 и 93.00, охватив беcкэшевые 91.50 с обеих сторон;
августовское число отозвано (§5), рабочее — **90.50**, но знака это не даёт —
знака нет, и причина расхождения не выяснена (§5).

Твёрдая форма утверждения обходится без беcкэшевого сравнения: на 2.0
**четыре бита против шестнадцати дают 181 из 200 оба**, двадцать вопросов
разошлись, ровно десять в каждую сторону, `p = 1.0`. **Маршрут другой, пункт
назначения тот же** — теперь с точностью до одного вопроса, а не в пределах
шума.

#### 2.7.8a. Прежнее сравнение на префиксной оси



`check_block_reuse.py --model-type llada`, LLaDA-1.5, те же 8 промптов, полотно
128, блок 32, группа 128, ось `token`, те же три разрядности и пять политик.
Плотный кэш-путь написан в `dllmquant/models/llada_local.py`, проверен на
эквивалентность: 16 бит с обновлением каждый шаг дают **100.00%** совпадения
траектории и **ноль** перевёрнутых позиций.

**Оговорка первая, и она ограничивает всё остальное: кэшируются разные вещи.**
Под блочно-каузальной маской LLaDA2.0 префикс точен по построению, поэтому
устаревание там можно впрыснуть только в **текущий блок** — активно
расшумляемые позиции, меняющиеся каждый шаг. У LLaDA-1.5 внимание
двунаправленное, точного префикса нет, и стареет **префикс** — в основном уже
зафиксированные токены. Отсюда разница в масштабе: максимум ущерба **3.0%**
против **63.7%**. Это не тот же закон на другой модели, а тот же закон на
другом объекте, и чтобы сравнить честно, плотному пути нужен оконный стор.

#### Что не перенеслось

| | измер/квадратура | измер/сумма |
|---|---|---|
| LLaDA2.0-mini | **0.953** (0.881–0.995) | 0.730 |
| LLaDA-1.5 | **1.154** (0.945–1.387) | 0.849 |

Композиция на 1.5 **выше** квадратуры, то есть ближе к аддитивной, и колонка
`diff` отрицательна в семи клетках из восьми — здесь она положительна во всех
восьми. Коэффициент 0.95 отозван как локальный (§2.7.6).

#### Что перенеслось, и в лучшей форме

Тождество `d = d_r + d_s − κ` держится: `κ` из дефицитов и `κ` из невязки
совпали во всех восьми клетках и на плотной модели тоже. Это алгебра.

Содержательное — поведение самой `κ`. Дефициты на 1.5 впятеро меньше
(0.03–0.13 против 0.16–0.87), и на **обеих** моделях сразу:

| масштаб дефицитов `√(d_r²+d_s²)` | 0.06 | 0.08 | 0.10 | 0.12 | 0.27 | 0.37 | 0.52 | 0.88 |
|---|---|---|---|---|---|---|---|---|
| **κ / min(d_r, d_s)** | −0.07 | +0.62 | +0.56 | +0.68 | +0.67 | +0.92 | +1.23 | +1.02 |

> **Эта таблица снята 1 сентября.** Она объединяла точки с **разных осей** —
> префиксной у 1.5 и оконной у 2.0, — и монотонность была артефактом такого
> пула. На сравнимой оси точки 1.5 дают `κ/min` = 0.91–1.37 при масштабе
> 0.11–0.21, тогда как префиксные при том же масштабе — 0.22–0.28. Одной
> функции масштаба нет; сохраняется таблица как запись того, что было
> проверено и отвергнуто.

Казалось, что это одна монотонная кривая через две архитектуры и
пятнадцатикратный диапазон.
При малых дефицитах `κ → 0` и ошибки складываются **аддитивно**; при больших
`κ → d_r`, наступает поглощение, дальше компенсация.

> **Правильная формулировка: `κ` — не константа архитектуры, а функция того,
> насколько дефициты велики по сравнению с зазором.** Насыщение требует, чтобы
> возмущение было сравнимо с тем, что оно съедает; на 1.5 съедать нечего,
> поэтому ошибки складываются в лоб. Квадратура — вид этой кривой вблизи одной
> точки.

Это сильнее прежнего утверждения: вместо коэффициента, подогнанного на одной
модели, получается зависимость, проверенная на двух и объясняющая, почему они
дают разные коэффициенты.

#### Оговорка вторая: предсказание про накопление было поставлено неверно

Ожидалось, что на 1.5 `c` по блокам **упадёт**, раз префикс стареет сам. Не
упал — профиль такой же растущий, как на 2.0.

Но проверять было нечего: при **закреплённом полотне** накопление через
зафиксированные токены исключено по построению, а префикс пересчитывается из
текущего полотна при каждом обновлении, так что прошлый ущерб в сторе не
задерживается. Предсказание было сформулировано без учёта того, что делает сама
измерительная установка.

#### Ось группировки: разрыв не воспроизвёлся, но и не мог

На 1.5 при `every_n:1` ущерб составляет 0.88% против 0.78% при четырёх битах и
1.38% против 0.88% при трёх; в переворотах — **14 против 9**. Направление то же,
величина ничтожна.

> **Отсюда убраны числа 2.0, стоявшие рядом — 7 сентября.** Здесь было «на 2.0
> ось вдоль каналов стоит 12 пунктов при четырёх битах и 20 при трёх», и это
> **сравнивало два разных измерения**: двенадцать — из `argmax@k` §2.4, оконного
> замера на префиксе, а 0.88 против 0.78 — из `check_block_reuse`, согласия
> зафиксированных токенов вдоль траектории. Разные скрипты, разные величины.
> Двадцатка вдобавок была не разрывом осей, а разрывом группы 32 против 128 на
> неверной оси (§2.3); разрыв осей при трёх битах там 16.67.
>
> Сравнимые числа с одного скрипта есть, и они в §2.5a: разрыв «канал − токен»
> при трёх битах — **≈ 0 на 1.5 против +41 пункта на 2.0**, обе строки из
> `check_block_reuse` с одинаковыми флагами.

> **Переснято 3 сентября на оконном сторе, и «эффект пола» оказался неверным
> объяснением.** Прежде здесь стояло, что весь диапазон ущерба на 1.5 меньше трёх
> процентов, эффект в двенадцать пунктов туда не поместился бы, и потому вопрос
> открыт. Это верно для строк `every_n:1`, но **при `block` ущерб на 1.5 доходит
> до 46–48%** — места сколько угодно.
>
> Разрыв «канал минус токен», один скрипт, одни настройки:
>
> | политика | 1.5, 3 бита | 2.0, 3 бита |
> |---|---|---|
> | `block` | +0.0 | +27.3 |
> | `every_n:8` | −2.7 | +40.8 |
> | `every_n:4` | +0.3 | +41.6 |
> | `every_n:2` | +0.4 | +41.8 |
>
> На плотной модели знак гуляет и величина не выходит за три пункта при
> доступном диапазоне в сорок шесть. **Это отрицательный результат, а не предел
> разрешения: выбор оси группировки — свойство MoE, а не общее.**
>
> **Полная развёртка разрядностей на плотной, 7 сентября: 16 / 8 / 4 / 3 / 2.**
> Прежде у неё были только 16 / 4 / 3, у 2.0 — вся пятёрка, и таблица зияла.
>
> | биты | `block` | `n:8` | `n:4` | `n:2` | `mask_ratio` |
> |---|---|---|---|---|---|
> | 16 | 44.5% | 11.7% | **4.7%** | **2.0%** | 15.2% |
> | 8 | 45.3% | 11.7% | 5.5% | 2.5% | 15.0% |
> | 4 | 46.9% | 12.1% | 6.1% | 2.5% | 14.3% |
> | 3 | 48.0% | 15.0% | 6.1% | 2.7% | 18.2% |
> | **2** | 51.8% | 13.1% | **5.3%** | **3.5%** | 18.8% |
>
> **Вся ось разрядности, четырнадцать бит, стоит почти ничего.** При `every_n:2`
> плюс 1.5 пункта (1.5σ), при `every_n:4` плюс 0.6 (0.4σ), при `every_n:8` плюс
> 1.4 (0.7σ). Значима одна строка — `block`, +7.3 пункта, 2.3σ.
>
> **Устаревание при тех же шестнадцати битах: 2.0% → 44.5%, в двадцать два раза.**
>
> **Двухбитный кэш на плотной работает** — 3.5% ущерба при `every_n:2`. Это
> сильнее того, что мы заявляли: «сжимать агрессивно» на этой архитектуре значит
> до двух бит, а не до четырёх.
>
> Немонотонность стоит отметить, не объясняя: два бита лучше трёх при `n:8`
> (13.1 против 15.0) и при `n:4` (5.3 против 6.1). Либо шум на 512 позициях, либо
> область компенсации по §2.7.7.
>
> **Развёртка на 2.0 снята, и различие подтвердилось.**
>
> | биты | `block` | `n:8` | `n:4` | `n:2` | `mask_ratio` |
> |---|---|---|---|---|---|
> | 16 | 60.4% | 15.2% | 8.2% | **2.9%** | 18.0% |
> | 8 | 60.2% | 13.7% | 8.6% | 2.7% | 17.6% |
> | 4 | 63.3% | 18.6% | 9.4% | 4.3% | 18.6% |
> | 3 | 61.3% | 16.8% | 10.7% | 5.5% | 19.7% |
> | **2** | 68.6% | 22.5% | 16.2% | **9.2%** | 24.2% |
>
> **Цена всей оси 16 → 2 бита:**
>
> | политика | 2.0 | | 1.5 | |
> |---|---|---|---|---|
> | `block` | +8.2 | 2.8σ | +7.3 | 2.3σ |
> | `every_n:8` | +7.3 | **3.0σ** | +1.4 | 0.7σ |
> | `every_n:4` | +8.0 | **3.9σ** | +0.6 | 0.4σ |
> | `every_n:2` | +6.3 | **4.3σ** | +1.5 | 1.5σ |
>
> **На MoE разрядность стоит значимо при всех политиках. На плотной — только при
> `block`, а на коротких интервалах не отличается от нуля.**
>
> Резче всего при `every_n:2`, где устаревания почти нет и видна одна разрядность:
> на 2.0 восемь бит бесплатны (0.2σ), четыре стоят 1.2σ, три — 2.1σ, два — **4.3σ**.
>
> **Правило становится двухадресным:** на плотной сжимать до двух бит, на MoE до
> четырёх; три уже на грани, два платят отчётливо.
>
> **Предсказание сбылось по направлению и промахнулось по величине.** Я ожидал
> «десятки пунктов» на MoE, исходя из того, что один бит там двигает `block` на
> 3.5. Вышло 6–8 пунктов на всю четырнадцатибитную ось — значит зависимость
> сильно сублинейна и экстраполяция от одного бита завышала.

#### Четвёртый пункт в §2.5a: чувствительность к разрядности тоже архитектурна

К трём различиям, собранным выше — ось группировки, пол округления, что делает
R4 — добавляется четвёртое. И оно ложится в тот же вывод: **где мерится тензор,
модели совпадают; где мерятся решения, MoE хуже.** Ошибка квантизации `K`
совпадает у них до 4% при любой ширине, а во что она превращается на решениях —
расходится втрое по значимости.

> **R4 там тоже ничего не делает, и квадрат достроен.** По оси токенов 44.7
> против 48.0 при трёх битах, по канальной 48.4 против 48.0 — всё в шуме. Все
> четыре угла на плотной модели сошлись: **ни ось группировки, ни поворот не
> двигают ничего.**
>
> **Первый кандидат опровергнут прогоном обзора.** Предполагалось, что у `K` на
> 1.5 может не быть концентрированной канальной структуры. Она есть, и та же:
>
> | | совпадение старших каналов (маска 0.00/0.50/1.00) | разброс |
> |---|---|---|
> | **1.5 `K`** | 0.64 / 0.60 / 0.70 | 1.33 / 1.35 / 1.44 |
> | **2.0 `K`** | 0.66 / 0.77 / 0.81 | 1.35 / 1.36 / 1.50 |
>
> **Одинаковая структура, противоположное следствие.** Значит «выбросы сидят в
> фиксированных каналах» объясняет, почему ось **может** иметь значение, и не
> объясняет, почему она имеет его **только на MoE**.
>
> **Остаётся GQA, и механизм у него внятный.** При MHA каждая голова считает свой
> `K` со своей независимой ошибкой округления, и по головам ошибки частично
> гасятся. При GQA четыре головы читают **одну** запись и видят **одну и ту же**
> ошибку — коррелированная ошибка не гасится, а складывается. Это снова
> корреляционная структура, но по третьей оси: не между позициями и не между
> каналами, а **между головами**.
>
> **Измерено 3 сентября, и гипотеза опровергнута — в сторону ствола.**
> `--head-sharing`: `K` округляется стохастически на четырёх битах, обе строки
> размножают головы явно, отличается только порядок двух операций. Общий эталон
> (зазоры `3.50 5.84 7.93 10.88`, пол 10.74%), 512 позиций.
>
> | | ущерб | переворотов |
> |---|---|---|
> | `shared` — четыре головы видят одну ошибку | **42.19%** | 216 |
> | `independent` — каждая свою | **48.24%** | 247 |
>
> Ожидалось, что `independent` окажется **лучше**, если веер и есть усилитель.
> Он **хуже** на 6.05 пункта, **1.95σ**.
>
> **Коррелированная ошибка при той же поэлементной величине менее вредна, чем
> независимая.** Значит объединять с «когерентность защищает» не только можно —
> это **третий её случай**, после полной маски (§4.4) и закрепления маршрутов
> (§4.5). Опасение выше было напрасным: складывающиеся вклады ведут себя так же,
> как двигающиеся конкуренты.
>
> **Что это оставляет от GQA.** Корреляционный механизм отпал. Выживает
> **ёмкостный**: у 2.0 четыре головы `K` против тридцати двух у 1.5, то есть
> восьмикратно меньше хранимых строк и восьмикратно меньше шкал на ту же
> развёртку. Это другое утверждение, и оно не проверено.
>
> **Оговорки.** 1.95σ — на грани, и одна строка. Стохастическое округление
> шумнее детерминированного, поэтому уровни 42–48% с рабочими 4.5% (§2.7.8)
> несопоставимы; сравнивать можно только две эти строки между собой, что и
> сделано.
>
> **Итог: разрыв 5–13× не объяснён ни маршрутизацией, ни корреляцией по
> головам.** Оба кандидата проверены и оба отпали.

**Побочно: статика переносится на плотную модель.** Промах калибровки на чужих
полотнах **0.16 бита** у `K` против 0.25–0.29, которые стоит хранить динамическую
шкалу. И корзина по доле маски объясняет там всего **47%** дисперсии против 68%
на MoE — то есть на 1.5 достаточно одной калибровки, бакетирование не нужно.
>
> **Побочно: `hit rate` и `age` на плотном пути отчитываются не о том.** Они
> показывают 99.9% и 15.5 в **каждой** строке — при `block`, при `every_n:2`,
> при `mask_ratio`, — тогда как ущерб гуляет от 46.9% до 2.5%. Колонки читают
> префиксный стор, который политикой не управляется. Дефект отчётности, не
> измерения; колонка ущерба политику отслеживает верно.

### 2.6a. Остаток квантизации низкорангов ровно там, где позиции замаскированы

Первое измерение фазы C. `check_lowrank.py --survey-only`, K вдоль токенов,
группа 128, база 3 бита (3.29 полных бита), 4 полотна.

Поправка сравнивается не со своей базой, а с **тем же бюджетом, потраченным
плоско**. Один лишний плоский бит умножает остаток на некоторый множитель —
это местный обменный курс между битами и ошибкой; тогда любое снижение остатка
переводится в плоские биты, делится на реальную стоимость, и получается одно
число: во сколько раз ранг выгоднее бита.

**Ранг 4, факторы 4 бита (стоит 0.45 бита), × = выгода против плоского бита:**

| доля маски | сл. 0 | 2 | 5 | 8 | 11 | 14 | 17 | 19 |
|---|---|---|---|---|---|---|---|---|
| 0.00 | 1.03 | 0.59 | 1.08 | 0.43 | 0.54 | 0.52 | 0.35 | 0.33 |
| 0.25 | 1.15 | 0.66 | 1.20 | 0.44 | 0.56 | 0.59 | 0.38 | 0.44 |
| 0.50 | 1.37 | 0.69 | 1.26 | 0.45 | 0.58 | 0.58 | 0.40 | 0.47 |
| 0.75 | 1.69 | 0.74 | 1.40 | 0.51 | 0.64 | 0.64 | 0.45 | 0.52 |
| 1.00 | **2.83** | 1.55 | **2.17** | 0.58 | 0.84 | 0.87 | 0.62 | 0.70 |

**По доле маски — монотонно и всюду**, включая слои, где поправка проигрывает.
**По глубине — не монотонно вовсе.** Выигрывают слои **0 и 5**; слой 2 между
ними не выигрывает нигде, кроме полной маски; с восьмого и глубже поправка не
окупается ни в одной клетке. Ранг 1 забирает 36–41% энергии на слоях 0 и 5
против 10–25% на остальных.

**Снято: «структура рассасывается с глубиной».** Так было записано по трём
снятым слоям (0, 10, 19), где это выглядело монотонным спадом. На восьми слоях
видно, что дело не в глубине: слой 2 сидит между двумя выигрывающими и ведёт
себя как глубокие. Три точки не задают тренд, а порядок слоёв соблазняет его
увидеть.

**Второе наблюдение, и оно объясняет часть эффекта.** Один лишний плоский бит
обязан уполовинить остаток. На слоях 8–19 он и уполовинивает (0.474–0.522). На
слоях 0, 2, 5 при полной маске — нет: 0.577, 0.602, 0.589. Бит, который не
уполовинивает, означает, что остаток — **не равномерное округление, а
обрезание**: `clip_ratio` = 0.95 срезает выбросы, и лишний бит от этого не
спасает. То есть часть «победы» поправки при полной маске — это ослабевший
контроль, а не усилившаяся поправка. Обе величины двигаются: на слое 0 остаток
после ранга 4 падает с 0.719 при раскрытом префиксе до 0.496 при полной маске,
а контроль ухудшается с 0.490 до 0.577.

**Факторы в fp16 не годятся.** В лучшей клетке (полная маска, слой 0) ранг 8 в
fp16 даёт 1.09 — безубыточность, при том что в четырёх битах та же клетка даёт
2.34. Работает именно четырёхбитный фактор, который в исходной арифметике плана
подразумевался молча.

**Чего замер не говорит.** Он про энергию остатка, а не про решения; при полной
маске `argmax@k` вырожден (пол 68.75%, §2.5), так что до развёртки с полом
случайности и `slots@k` переносить вывод на качество нельзя.

#### Механизм найден: это один канал, а не якорь

Диагностика геометрии первой компоненты, все восемь слоёв и все три доли маски:

| | несут первую компоненту | согласие по позиции |
|---|---|---|
| каналов | **1.0 – 4.2**, чаще всего 1.0–1.7 | — |
| токенов | 92 – 154 | 6–12% |

Согласие 6–12% — это ровно случайность (одна голова из шестнадцати), и модальная
позиция каждый раз новая: 96, 33, 11, 110, 97, 4, 217, 108. **Якорей здесь
нет.** Доминирует один канал, на каждом слое и при любой доле маски.

Механизм понятен: на оси токенов у каждого канала своя шкала, значит
относительная ошибка у всех одинакова, а **абсолютная пропорциональна размаху
канала**. Самый широкий канал и даёт почти всю энергию остатка. Тогда ранг —
инструмент не той формы: компонента ранга 1 это внешнее произведение, и она
платит ещё и за токенный сомножитель, который не несёт ничего сверх того, что
уже сказал канал. Прямой соперник — **смешанная разрядность**: дать самым
широким каналам лишние биты. Индексы этих каналов — свойство слоя, а не полотна
(та же посылка, на которой выиграла ось), значит они живут в модели и кэшу не
стоят ничего, как статическая шкала.

На синтетике соперник выигрывает у ранга с запасом: «1 канал +4 бита» оставляет
0.846 остатка при цене 0.031 бита, ранг 1 оставляет 0.844 при цене 0.051 — то
же самое за 1.6× больше. И масштабируется так же: k каналов идут вровень с
рангом k.

#### И моя собственная арифметика цены была неверной

Шкалы для фактора A я нарезал **по одной на токен**. При ранге 1 это 224 шкалы
на 352 числа факторов — цена в 0.25 бита на запись, впятеро больше самого
фактора. Обзор мерил стоимость шкал и называл её стоимостью поправки.

Одна шкала на компоненту ранга даёт ту же точность (на синтетике 0.843 против
0.844) и цену **0.051 вместо 0.300**. Значит колонка `x` в снятых таблицах
занижена примерно вшестеро для строк с четырёхбитными факторами.

**Что из снятого выживает:** колонки `captured` и `left` — они мерились точнее и
от нарезки шкал почти не зависят; строки с факторами fp16 — там своих шкал нет
вовсе; рисунок по глубине (слои 0 и 5); монотонность по доле маски; ослабление
контроля при полной маске.

**Что не выживает:** колонка `x` и оба вывода, на ней стоявшие. «При раскрытом
префиксе поправка не окупается» держалось на цене 0.30 бита за ранг 1; при
честной цене 0.051 та же строка даёт ×6.3.

#### При честных ценах плоский бит проигрывает всем

Пересняли с ценами по колонке и с контролем смешанной разрядности. **Ни одной
строки, которая не била бы плоский бит** — ни на одном слое, ни при одной доле
маски. Это и есть главный результат обзора, и он не про низкий ранг:

> Ошибка сидит в одном канале из 128, поэтому лишний бит, выданный **всем**
> каналам, — заведомо худшее употребление бюджета, чем любое прицельное.

Читать «×9.8» как «в девять раз лучше» нельзя. Плоский бит убирает **больше**
ошибки (остаток 0.49 против 0.80 у «1 канал +4 бита») — он просто стоит в
тридцать раз дороже. Это выгода за бит, а не качество.

**Кто выигрывает — предсказывается геометрией, 10 из 10:**

| каналов несут первую компоненту | победитель |
|---|---|
| ≤ 1.4 | расширение канала |
| ≥ 1.7 | ранг |

При раскрытом и наполовину раскрытом префиксе — то есть в том режиме, который
и разворачивается, — каналы выигрывают везде: ×9.8–12.3 при доле 0.00 и
×5.2–14.7 при 0.50, против ×2.8–9.3 у ранга 1. При полной маске первая
компонента размазывается (слой 2: 4.2 канала, слой 19: 3.0) и там ранг
забирает своё: 7.67 против 4.87 на слое 2.

Смысл ровно тот, что и должен быть: **шип хочет точности, размазанное
направление хочет ранга.** И у расширения канала есть преимущество, которого
нет в этой таблице: на чтении оно не требует умножения `A·B`.

**Самые широкие каналы стабильны наполовину:** совпадение четвёрки между
полотнами 78% на слое 0, 63% на слое 5, 51% на слое 11 (случайность — 1.6%).
То есть список индексов строить можно, но он не константа: на глубоких слоях
половина четвёрки меняется от полотна к полотну. Для развёртки это значит, что
контроль надо считать по **калиброванному** списку, а не по текущему полотну,
иначе он остаётся оракулом.

#### Первая развёртка на решениях недействительна — два дефекта

Числа сняты и **выброшены**, обе причины найдены.

**Мощность.** `argmax@k` при `--samples 4` — доля по 16 позициям: разрешение
6.25 пункта, стандартная ошибка 7.5. Выдало «4 бита хуже 3 битов» (75.00%
против 93.75%), чего быть не может. Признак сработал как должен: невозможный
порядок и есть индикатор нехватки мощности. Нужно ≈24 полотна, чтобы ошибка
упала до 3 пунктов. Скрипт теперь печатает `UNDERPOWERED` сам.

**Смешанное сравнение.** Строка «flat 4 bits» расширяла **и K, и V**, а
прицельные строки трогали только K. Поэтому у них `rel. err` почти не двигался
(1.02e-01 против 1.00e-01 у базы): они не могли достать до ошибки, которую
вносил V, а плоская строка её убирала. Теперь V держится на базовой ширине во
всех строках, и лишний бит идёт ровно туда же, куда идёт поправка.

Побочно это уже кое-что говорит: при трёх битах на обеих сторонах **ошибка
логитов упирается в V, а не в K**. Если так и останется при большем числе
полотен, то весь прицел фазы C на K — оптимизация не того слагаемого.

**Что осталось открытым:** доживает ли выгода до решений. Всё измеренное —
энергия остатка. `mixed_precision` умеет писать в кэш (`wide_channels`,
`wide_extra_bits`), пол случайности и `slots@k` в развёртке есть, мощность
теперь проверяется автоматически — не хватает только прогона на ≈24 полотнах.

### 2.6. Асимметрия K/V: прямая снята, зависимость от маски осталась

На оси channel зеркальные пары давали 2.96σ и 4.44σ в пользу K. **На оси token
разрыв схлопывается** до 1.05σ и нуля: K требовал не доплаты битами, а верной
группировки.

А **переворот при полной маске выжил**: там бит V дороже бита K, по обеим
зеркальным парам и обеим информативным колонкам (`rel. err`, `slots@k`).
Механизм: при полной маске ключи почти одинаковы, внимание почти равномерно
независимо от точности K, а V идёт прямо в сумму выхода.

Заявку надо сузить: KIVI уже установил, что K и V не одинаковы, по **оси
группировки**. Наше — что **направление зависит от доли маски**.

---

## 3. Роутер: усиливает норму, но не ущерб

19 роутеров, **9.96M параметров, 0.061% модели**.

### 3.1. По весам

При 4 битах на весах роутера меняется **22% выборов эксперта**, при том что
4-битные веса самих экспертов не стоят ничего (McNemar p = 0.44). Маршруты
устойчивее на раскрытом полотне: 95.1% против 90.7%.

### 3.2. По кэшу

Развёртка по разрядности, группа 128, ось token:

| биты | маршрутов сохранено | массы потеряно | худший слой | argmax |
|---|---|---|---|---|
| 16 (хранение тождественно) | 84.81% | 12.05% | 7 (72.7%) | 87.50% |
| 8 | 83.51% | 12.96% | 8 (69.5%) | 84.38% |
| 4 | 80.86% | 15.35% | 8 (61.3%) | 87.50% |
| 3 | 75.78% | 19.97% | 6 (55.9%) | 87.50% |
| 2 | 66.55% | 28.06% | 6 (47.3%) | **28.12%** |

**Четыре пятых порчи маршрутов — цена окна, а не битов**: при 4 битах потеряно
19.1%, из них 15.2 пункта заплачено до всякой квантизации.

**Сеть эту порчу поглощает.** С 16 бит до 3 маршруты проседают на 9 пунктов,
масса растёт с 12% до 20% — а зафиксированные токены **не меняются вовсе**.
Обрыв между тремя битами и двумя, и он не постепенный.

### 3.3. Закрепление маршрутов: решающее вмешательство

`force_routes` заставляет каждый роутер повторить выбор точного прогона (патч
на `group_limited_topk`, так что сбор оценок, нормировка и
`routed_scaling_factor` продолжают считаться кодом чекпойнта).

Результат: расхождение скрытых состояний падает **вчетверо** (2.65e-01 → 6.9e-02
на пике), а **argmax не меняется вовсе** — 87.50% и с закреплением, и без.

**Значит роутер усиливает норму отклонения, а не ущерб.** Те 12.5% сдвинутых
решений при 16 битах порождены арифметикой окна.

Поглощают, по-видимому, три вещи: общие эксперты (есть, подтверждено), почти
равные веса внутри восьмёрки, и двадцать слоёв остаточного потока — расхождение
пикует на слоях 7–9 и к девятнадцатому падает вдвое.

### 3.4. Избирательность — свойство малости возмущения

Доля последнего, восьмого ранга среди выброшенных экспертов: **35.9%** при 16
битах, 31.4% при четырёх, **22.7%** при двух. Доля первого ранга растёт с 1.1%
до 2.3%.

Слабый толчок переворачивает только близкие ничьи, а близкие ничьи и есть
нижние ранги. Сильный переворачивает всё подряд — и тогда рушится ответ.

**Но ранговый перекос защищает слабее, чем кажется:** отношение «потеряно массы
/ потеряно маршрутов» держится около **0.85**, а не 0.3. Оценки берутся
сигмоидой и нормируются по восьмёрке, поэтому веса внутри неё почти одинаковы —
ранг решает, кого выбьет, и почти не решает, сколько это стоит.

### 3.5. Сорок пять тысяч раз

Один и тот же поворот QuaRot, математически тождественный:

| точность | маршруты | сдвиг выхода |
|---|---|---|
| float32 | 100% | **4e-06** |
| bfloat16 | 83.7–91.6% | **1.8e-01** |

Разница между строками — округление весов в последних битах плюс перевороты
маршрутов. Итог отличается в **сорок пять тысяч раз**.

---

## 4. Дрифт: устаревание против округления

Вопрос, вокруг которого проект и строился — прячет ли одна ошибка другую.

### 4.1. Точность префикса: ноль по построению, но не побитово на MoE

| | `stale(prefix)` |
|---|---|
| **LLaDA2.0**, блочно-каузальная, маршруты закреплены | **0.00000** на всех слоях и задержках |
| **LLaDA2.0**, как есть | 0 только на слое 0, дальше **0.010 → 0.053** |
| **LLaDA-1.5**, двунаправленная | 0 только на слое 0, дальше **0.029 → 0.174** (d=1), до **0.279** (d=4) |

**Средняя строка — новая, и её пришлось объяснять.** Первый прогон
`measure_drift` на LLaDA2.0 дал ноль только на нулевом слое, а дальше 1–6%
относительной ошибки — то есть ровно ту картину, которая ожидается от
двунаправленной модели, а не от блочно-каузальной.

Подпись при этом была не та. Настоящее устаревание растёт с **задержкой**: на
текущем блоке 0.283 → 0.511 при учетверении d. Колонка префикса росла с
**глубиной** и почти не росла с задержкой — 0.0104 → 0.0140 на слое 4. И ноль
стоял ровно на нулевом слое, единственном плотном.

`--pin-routes` разрешает вопрос: при маршрутах, закреплённых по первому шагу,
колонка обнуляется **во всех пятнадцати строках**.

**Механизм — диспетчеризация экспертов.** Закрытая позиция математически не
зависит от текущего блока, маска это гарантирует. Но эксперт получает
собранную роутером выборку токенов, и когда токены текущего блока меняются,
меняются размер и порядок редукции. Скрытое состояние закрытого токена
сдвигается на уровне арифметики, а роутер превращает этот сдвиг в **дискретный
переворот маршрута** — тот же механизм, что в §3.

**Формулировать надо двумя предложениями, а не одним:**

> Под блочно-каузальной маской префикс точен по построению, и в точной
> арифметике это ноль. Но на MoE переиспользование префикса **не побитово
> воспроизводимо**: диспетчеризация зависит от всего полотна, роутер усиливает
> арифметический шум до переворота маршрута, и закрытый блок сдвигается на
> 1–6% относительной ошибки.

Это специфично для MoE и, насколько удалось проверить, не сказано никем. DART
работает на MoE и об этом не упоминает.

**Какие числа куда идут.** Незакреплённые — то, что кэш реально несёт, они и
стоят в таблицах ущерба. Закреплённые — изолирующее измерение, отвечающее на
вопрос, что от маски, а что от роутера. Нужны обе колонки.

Ноль на слое 0 у LLaDA-1.5 — подтверждение другого механизма: там K берётся
прямо из эмбеддинга, а токен раскрытой позиции не менялся. Со **первого** слоя
состояние уже зависит от внимания на меняющиеся замаскированные позиции, и
устаревание **копится вглубь стека**.

Округление при этом **плоское** по глубине (0.055–0.068), потому что
перерисовывается при каждой записи.

**Отличие от DART сохраняется:** блочно-каузальная маска делает префикс точным,
двунаправленное внимание — нет. У них обе модели двунаправленные, значит
«ошибка кэша» — неразделимая смесь, и вопрос «во что обходятся биты» там нельзя
поставить чисто. Оговорка про MoE это не отменяет: 1–6% арифметического сдвига
и 3–28% настоящего устаревания — величины разного порядка.

### 4.1a. Геометрия: две ошибки складываются в квадратуре

Разложение того, что читает внимание, точное:

    Q(K_old) − K_new  =  [Q(K_old) − K_old]  +  [K_old − K_new]
                          r, округление         s, устаревание

`geometry_table` в `measure_drift.py` считает длины, косинус и сравнивает
измеренную длину суммы с квадратурной. Четыре бита, группа 128, ось token,
маршруты закреплены:

| слой | d | \|r\| | \|s\| | cos | \|r+s\| / √(\|r\|²+\|s\|²) |
|---|---|---|---|---|---|
| 0 | 1 | 0.50 | 2.04 | **−0.137** | 0.969 |
| 0 | 4 | 0.55 | 4.17 | **−0.229** | 0.970 |
| 4 | 1 | 3.02 | 9.59 | −0.001 | 0.9995 |
| 4 | 4 | 3.27 | 21.57 | +0.002 | 1.0001 |
| 8 | 4 | 8.25 | 62.67 | −0.014 | 0.998 |
| 12 | 4 | 25.63 | 238.07 | −0.036 | 0.996 |
| 16 | 4 | 98.85 | 899.93 | −0.036 | 0.996 |

**Отношение к квадратуре 0.994–1.000 везде, кроме нулевого слоя.** То есть
композиция — квадратура с поправкой на гашение в пределах полупроцента. Это
объясняет шесть клеток §2.7.2, а не совпадает с ними случайно: в пространстве
такой размерности два не связанных смещения почти ортогональны, длины
складываются в квадратуре, и меньшее почти ничего не добавляет к большему.

**Нулевой слой — исключение, и осмысленное.** Косинус там −0.14…−0.23, и это не
роутер: слой плотный, закрепление его не тронуло. На нулевом слое `K` берётся
прямо из эмбеддинга, и у **всех** замаскированных позиций это одна и та же
строка — значит округление у них структурно одинаково, а устаревание есть
переход от строки маски к строке настоящего токена. Обе ошибки порождены одним
вектором, независимыми быть не обязаны.

Картина связная: **где у двух ошибок разные причины — косинус ноль; где общая
причина — корреляция.** Общая причина есть ровно в одном месте, на входе.

**Без закрепления маршрутов** косинус был вдвое-втрое крупнее по модулю (до
−0.073 на слое 16), то есть треть-половина наблюдавшегося гашения была churn
маршрутов, а не геометрией.

Что остаётся после закрепления, разобрано во второй разрядности — §4.1b: это
детерминированная часть квантователя, а не взаимодействие двух ошибок.

#### 4.1b. Вторая разрядность: прибор проверен, гашение локализовано

Тот же замер при трёх битах, маршруты закреплены.

**Прибор ведёт себя точно как должен.** Убрать бит — удвоить шаг сетки, значит
удвоить длину смещения от округления:

| слой | \|r\|, 4 бита | \|r\|, 3 бита | отношение |
|---|---|---|---|
| 0 | 0.501 | 0.908 | 1.81 |
| 4 | 3.022 | 6.449 | **2.13** |
| 8 | 7.668 | 16.332 | **2.13** |
| 12 | 23.821 | 49.852 | **2.09** |
| 16 | 91.670 | 192.100 | **2.10** |

Это не результат, а проверка, что измеряется заявленное.

**Квадратура держится ещё лучше:** отношение `|r+s|` к `√(|r|²+|s|²)` —
0.964–1.001, на средних слоях 1.0003–1.0012, то есть совпадение до четвёртого
знака.

**А косинус упал вдвое, и это объясняет его происхождение:**

| слой, d | cos, 4 бита | cos, 3 бита |
|---|---|---|
| 0, d=1 | −0.137 | −0.098 |
| 8, d=4 | −0.014 | −0.006 |
| 12, d=4 | −0.036 | −0.014 |
| 16, d=4 | −0.036 | −0.025 |

Возмущение от округления выросло вдвое, а косинус **уменьшился**. Значит у
смещения от округления есть **фиксированная по величине составляющая**,
направленная против устаревания, и она не растёт вместе со случайной частью:
удвоили случайную — доля согласованной упала.

Отсюда механизм проще, чем казалось: **два возмущения ортогональны, а малое
отклонение от квадратуры вносит детерминированная часть квантователя** —
обрезание тянет крупные значения внутрь. Гашение не свойство взаимодействия
двух ошибок, а свойство одной из них.

**И отношение ошибок при трёх битах вдвое меньше:** колонка `ratio` даёт
**2.03–4.44** против 4.14–10.47 при четырёх битах. Один просроченный шаг стоит
там всего вдвое дороже округления. Это ровно то, что объясняет разное
поглощение в §2.7.2 — 90% при четырёх битах и 47% при трёх.

#### Воспроизводимость: до 5% при меняющейся загрузке карты

`|s|` и дрейф латентов **не должны** зависеть от разрядности: траектория
снимается до всякой квантизации, биты применяются к сохранённым тензорам
постфактум. А они отличаются между двумя прогонами на 2–5%: слой 16 при d=4
даёт `|s|` = 899.93 против **857.68**, дрейф латентов 0.2517 против 0.2448.

Значит **эталонная траектория между запусками не воспроизводится побитово**.
Причина той же природы, что в §4.1 — диспетчеризация экспертов, — плюс,
вероятно, выбор ядер cuBLAS под разной загрузкой: между прогонами сервер ушёл
со свободного на полностью занятый.

**Но решения воспроизводятся, и это уточнение важное.** Два прогона GSM8K на
одной конфигурации — четыре бита кэша, полотно 512, двести задач — дали
одинаковую бегущую точность **во всех сорока контрольных точках**, при том что
скорость различалась вдвое (65.9 против 27.2 секунды на задачу), то есть
загрузка карты была совсем разной. Значит недетерминизм живёт в третьей цифре
норм и **не доходит до argmax**.

Отсюда правило чтения: **в решениях доверять можно, в нормах — только порядку.**

Прежняя запись «два независимых запуска дали одинаковые числа до пятого знака»
верна только при постоянной загрузке. Формулировать надо: **воспроизводимо при
неизменной загрузке карты, до 5% при меняющейся.** На выводы это не влияет —
расхождение 2–5% против эффектов в разы, — но числа в таблицах надо считать
снятыми с этой точностью, а не с пятого знака.

### 4.2. На текущем блоке устаревание перекрывает округление в 4–10 раз

Четыре бита, группа 128, K вдоль токенов:

| слой | d | stale(current) | rounding | отношение |
|---|---|---|---|---|
| 0 | 1 | 0.229 | 0.053 | 4.36 |
| 0 | 4 | 0.450 | 0.052 | 8.65 |
| 12 | 4 | 0.608 | 0.060 | 10.10 |
| 16 | 4 | 0.689 | 0.067 | **10.32** |

**Ни в одной строке округление не узкое место.** Значит если распространять кэш
на текущий блок, вкладываться надо в **политику обновления**, а не в биты; и
наоборот, биты там можно резать смело.

На оси channel та же колонка вдвое больше (0.109–0.121) и отношение падает до
1.94–6.31 — ещё одно следствие §2.1.

### 4.3. Пик дрейфа латентов на четвёртом слое — это роутер, а не состояние

Так выглядел профиль, и так было записано как гипотеза:

| | профиль |
|---|---|
| LLaDA2.0 (MoE) | пик **3.65** на слое 4, спад до 0.27 к выходу |
| LLaDA-1.5 (плотная) | U-образный: минимум **0.118** на слое 4, рост до 0.367 к слою 28 |

**Строка LLaDA-1.5 держится на двух точках** — 0.118 и 0.367, — и «U-образный»
описывает форму, а не измеренную кривую. Послойного профиля для неё нет; для
2.0 он есть, и сравнивать пока можно только концы.

Гипотезой было, что маршрутизация заменяет матрицы целиком, а не возмущает их.
**Проверено закреплением маршрутов, и гипотеза подтвердилась с запасом:**

| слой | как есть | маршруты закреплены | во сколько раз |
|---|---|---|---|
| 0 | 0.2526 | 0.2526 | **1.0** — контроль, слой плотный |
| 4 | **3.6460** | **0.1108** | **33×** |
| 8 | 1.1805 | 0.1544 | 7.6× |
| 12 | 0.4632 | 0.1958 | 2.4× |
| 16 | 0.2731 | 0.2517 | 1.1× |

Нулевой слой совпал до последнего знака — закрепление ничего не ломает там, где
роутера нет. А на четвёртом незакреплённый дрейф был **в тридцать три раза**
больше настоящего движения состояния.

С закреплением профиль LLaDA2.0 становится **монотонно растущим** (0.111 → 0.252
по глубине) — то есть таким же по форме, как у плотной LLaDA-1.5, и разница
между архитектурами, которая казалась структурной, оказалась артефактом
маршрутизации.

**Что это значит для чтения прежних таблиц.** Колонка «дрейф латентов» на
MoE-слоях до сих пор измеряла churn маршрутов, а не движение скрытого
состояния. Числа верны как описание того, что происходит при выводе; как
описание того, «насколько уходит состояние», они завышены на порядок.

---

## 5. Числа на задаче

GSM8K, LLaDA2.0-mini, полотно 512, зачёт по явному маркеру ответа.

| конфигурация | задач | точность | с/задачу |
|---|---|---|---|
| FP16, без кэша | 200 | **91.50%** | 294.9 |
| ~~FP16, кэш 16 бит~~ | ~~200~~ | ~~93.00%~~ | ~~82.5~~ |
| **FP16, кэш 16 бит** (переснято) | **200** | **90.50%** | 66–27 |
| **FP16, кэш 4 бита** | **200** | **90.50%** | 66–27 |
| FP16, без кэша | 50 | 92.0% | 423.4 |
| FP16, кэш 16 бит | 50 | 96.0% | 63.0 |
| FP16, кэш 4 бита, ось token | 50 | 90.0% | 53.4 |

> **Две строки «кэш 16 бит, 200» расходятся на 2.5 пункта, и это не будет
> объяснено.** Закрыто как невосстановимое 3 сентября: конфигурация августовского
> прогона нигде не сохранена, восстанавливать нечего, и ссылаться надо на
> переснятые числа. Урок записан в `RUNBOOK.md`: конфиг пишется в шапку выхода,
> иначе прогон непригоден для сравнения. Ниже — что известно.
> Поля конфигурации у переснятого прогона — полотно 512, шагов 256, группа 128,
> ось `token` — но конфигурация августовского прогона в записях не сохранена, и
> сравнить нечего. **Это не недетерминизм:** четырёхбитный и шестнадцатибитный
> прогоны, снятые заново, дали одинаковую бегущую точность **во всех сорока
> контрольных точках**, то есть путь воспроизводится. Значит расходятся
> протоколы, и до выяснения ссылаться надо на переснятые числа.

**База воспроизведена.** 91.50% против опубликованных 94.24 при стандартной
ошибке 2.0 — расхождение внутри полутора сигм. Старые 77.0 были следствием
полотна 256, где ответы не помещались; обрывов при 512 ноль.

#### Сетка на задаче: вся шкала ущерба на зазорах не переводится в точность

Главный отрицательный результат работы, и его надо заявлять первым, а не прятать
в оговорки.

| политика | средний возраст | 16 бит | 4 бита | 3 бита | решений сдвинуто на зазорах |
|---|---|---|---|---|---|
| `every_n:1` | 0 | **90.50** | — | — | 0.0% |
| `every_n:4` | 2 | **90.50** | **90.50** | 89.00 | 11.1% |
| `block` | 16 | **90.50** | **90.50** | **89.00** | **61.9%** |

Прямоугольник закрыт, включая угол — максимальное устаревание при трёх битах,
где по формуле `d ≈ 0.89` и съеден почти весь зазор.

**И совпадают не числа, а сами множества решённых задач:**

```
16 бит:  block против every_n:1   →  0 разошедшихся вопросов
 4 бита: block против every_n:4   →  0
 3 бита: block против every_n:4   →  0
```

**Политика обновления не меняет ни одного вопроса — ни счёта, ни того, какие
именно решены.** Между этими клетками ущерб на зазорах идёт от нуля до
шестидесяти двух процентов.

> **Снято 8 сентября: колонки точности обязаны были совпасть, и это не
> результат.** `--kv-policy` на пути задачи не действовал ни на одной семье.
>
> На MoE `refresh_prefix` вызывается один раз на блок **вне шага** и безусловно
> (`llada2_local.py:457`); `should_refresh` на этом пути не вызывается вовсе. На
> плотной политика читается только внутри `stale = stale_prefix and ...`, а
> `stale_prefix` по умолчанию `False`, и выставить его из `evaluate.py` было
> нельзя. В обоих случаях политика доходит только до `should_refresh_window`, а
> окно кэшируется лишь при `reuse_window=True`, чего `evaluate.py` не передавал.
>
> **Значит три строки таблицы — это три одинаковых прогона.** Ноль
> разошедшихся вопросов в трёх сравнениях подряд получен не потому, что
> устаревание безвредно, а потому, что устаревания там не было ни в одной
> клетке: префикс писался раз на блок везде, окно пересчитывалось каждый шаг
> везде.
>
> **Колонка «решений сдвинуто» настоящая** — она снята `check_block_reuse`,
> который вызывает сэмплер с `reuse=True` и потому политику оживляет. Таблица
> сопоставляет величину, которая менялась, с величиной, которая не могла
> измениться, и читает совпадение как перенос.
>
> **Утверждение не опровергнуто — оно не проверено.** Возможно, политика и
> правда не меняет ответов; на ответах эта ось не варьировалась ни разу.
>
> **Чем чинится.** Флаги `--reuse-window` и `--stale-prefix` добавлены
> (`e54b1f9`). Проверять тезис надо **при включённом `--reuse-window`** — в том
> же режиме, в котором сняты 0.0 / 11.1 / 61.9%. Отдельно от этого стоит
> каноническая точка, где окно свежее и политика мертва по построению: это то,
> что поставляют dInfer и Fast-dLLM, и там утверждение тривиально верно.

#### Проверено 9 сентября: ось переводится в ответы, и отрицательный результат становится положительным

Двадцать два прогона, полотно 512, 256 шагов, 200 задач. Ниже — всё, что
изменилось, и оно меняется в одну сторону.

**Политика при живом окне двигает ответы, и односторонне:**

| кэш 16 бит, `--reuse-window` | `every_n:1` | `every_n:4` | `block` |
|---|---|---|---|
| LLaDA-1.5 | 83.50 | 55.50 | **3.00** |
| LLaDA2.0-mini | 90.50 | 47.50 | **1.50** |

McNemar на переходе `n:4 → block`: на плотной **105 потеряно, 0 приобретено**
(`p = 5e-32`), на MoE **93 против 1** (`p = 1e-26`). Прежняя таблица давала в
этих клетках три одинаковых числа, и это был артефакт неподключённой ручки.

**Значит ущерб на зазорах переводится в точность почти один в один:** 61.9%
сдвинутых зафиксированных решений — это три процента правильных ответов.
Главный отрицательный результат работы отменяется, и на его месте стоит
положительный.

#### Два стора расходятся полностью, и это и есть результат

Одна модель, одна и та же метка политики, противоположный исход — разница
только в том, **какой** из двух сторов стареет:

| что заморожено | точность | разошлось | McNemar |
|---|---|---|---|
| **префикс** (`stale_prefix`, `every_n:1` → `block`) | 85.00 → **83.50** | 12 против 9 | **p = 0.66** |
| **текущий блок** (`reuse_window`, `every_n:1` → `block`) | 83.50 → **3.00** | 105 против **0** | **p = 5e-32** |

**Заморозить префикс — бесплатно. Заморозить текущий блок — смертельно.**
Полтора пункта против восьмидесяти.

И это ровно то, что делают все три работы поля: dKV-Cache, Fast-dLLM и
dLLM-Cache кэшируют префикс и пересчитывают декодируемый блок на каждом шаге
(§1). Раньше это была их конструкция без числа. Теперь число есть, и оно
объясняет, почему конструкция именно такая — а заодно почему `cache_update_iter`
у dInfer выключен по умолчанию и никого это не беспокоит.

#### В канонической точке кэш бесплатен по обоим счётам

Полоса из четырёх прогонов подряд на одной карте, окно свежее:

| | точность | против предыдущей | McNemar |
|---|---|---|---|
| без кэша | 83.00 | — | — |
| кэш 16 бит, `block` | 83.50 | +0.5 | `p = 1.0` |
| **кэш 4 бита, `block`** | **84.50** | +1.0 | `p = 0.83` |

Четыре бита номинально **лучше**, чем отсутствие кэша вовсе. Округление в
поставляемом режиме не стоит ничего, и это подтверждает §2.2 на ответах, а не
на решениях.

#### Расширено 14 сентября: три бита тоже бесплатны, и статическая шкала на K тоже

Шесть прогонов, все в поставляемом режиме (`block`, текущий блок пересчитывается
каждый шаг, префикс на родной стороне RoPE каждой модели), 200 задач. Статические
шкалы калибруются новым `--kv-static` (`7571130`): тот же сэмплер и тот же режим
на 8 задачах GSM8K из **train**, кэш 16 бит пишет шкалы по ровно тем тензорам,
что ложатся в стор. Во всех статических прогонах `fallbacks = 0` — все корзины
доли маски откалиброваны.

**Три бита на кэше бесплатны на обеих моделях:**

| кэш 3 бита, динамика, группа 128 | точность | против 16 бит | против 4 бит |
|---|---|---|---|
| LLaDA-1.5 | 83.00 | 83.50: 14 против 13, **`p = 1.0`** | 84.50: 12 против 9, **`p = 0.66`** |
| LLaDA2.0-mini | 88.50 | 90.50: 10 против 6, **`p = 0.45`** | 89.00: 10 против 9, **`p = 1.0`** |

Шестнадцатибитная клетка LLaDA2.0-mini — прогон с обновлением каждый шаг; на этой
модели он численно совпадает с поставляемым режимом, потому что префикс точен по
построению.

**Статическая шкала на канал для K не стоит точности:**

| | динамика, группа 128 | статика на K | McNemar |
|---|---|---|---|
| LLaDA-1.5, 4 бита | 84.50 | **87.00** | 8 против 13, **`p = 0.38`** |
| LLaDA2.0-mini, 4 бита | 89.00 | **87.00** | 11 против 7, **`p = 0.48`** |
| LLaDA-1.5, 3 бита | 83.00 | **83.50** | 13 против 14, **`p = 1.0`** |
| LLaDA2.0-mini, 3 бита | 88.50 | **92.00** | 3 против 10, **`p = 0.09`** |

`l20_S_k3` досчитан 14 сентября; против кэша 16 бит 92.00 против 90.50 — 8 против 5,
`p = 0.58`. Сетка статики закрыта на обеих моделях.

> **Предупреждение о p между отдельными прогонами.** Та же клетка против статики на
> K при 4 битах (`l20_S_k4`, 87.00) даёт 11 против 1, **`p = 0.006`** — «три бита
> лучше четырёх». Физически так быть не может, значит разброс между прогонами
> (карта, недетерминизм ядер) больше того, что закладывает McNemar при
> независимых расхождениях. Одиночные `p` порядка 0.1 между разными прогонами
> читать как шум; устойчивым считать только то, что держится на нескольких
> клетках сразу.

На одной модели статика номинально выше, на другой ниже — в обе стороны в пределах
шума. Сжатие при этом 3.88× вместо 3.75×: шкалы K уходят из кэша в модель. Это
переносит на ответы и на вторую архитектуру то, что §2.6b показал на тензоре и
решениях 2.0, — то есть воспроизведение раскладки KVQuant, не свой результат. Близко
подошёл и DART (§1): поканальные шкалы, посчитанные на первом шаге и переиспользуемые
на следующих, на LLaDA-8B и LLaDA-MoE. Своё здесь узкое — офлайн-калибровка на
чужих задачах, три бита и блочно-каузальная модель с QK-Norm.

**Статика на K и V вместе при трёх битах — тоже бесплатна** (`l15_S_kv3`,
`l20_S_kv3`, добавлено 14 сентября):

| 3 бита | динамика | статика на K и V | McNemar |
|---|---|---|---|
| LLaDA-1.5 | 83.00 | **86.00** | 14 против 8, **`p = 0.29`** |
| LLaDA2.0-mini | 88.50 | **88.50** | 9 против 9, **`p = 1.0`** |

Для V это не пересказ KVQuant. Статическая книга хранит одну шкалу на канал
(`StaticScaleBook`, `channel_range`), а динамическая V была со шкалой на токен —
значит, у V одновременно поменялись и ось, и способ калибровки, хотя старшие каналы V
устойчивы слабее, чем у K (совпадение 0.19–0.42 против 0.60–0.81). На
ответах разницы нет на обеих моделях — это согласуется с К2 (для V ось
безразлична) и переносит его с тензора на ответы. Все шкалы уходят в модель:
3.00 бита на значение, **сжатие 5.33×** против 5.1× со статикой только на K.

Против кэша 16 бит у LLaDA2.0-mini 88.50 против 90.50: 7 против 11, `p = 0.48`.
Файла шестнадцатибитной клетки LLaDA-1.5 (83.50) локально нет; против прогона
без кэша (83.50) — 17 против 12, `p = 0.46`.

Одно наблюдение без теста: на LLaDA2.0-mini обрезанных по длине ответов 8 против
3 у динамики, все неверные. На точности это не сказалось, но если на 2 битах
статика начнёт проигрывать, смотреть в первую очередь сюда.

> **Оговорки.** Динамические клетки 4 бит сняты на других картах, чем статические;
> межкарточный шум измерен около трёх вопросов (§5, воспроизводимость), здесь
> разницы по пять. Отличить «помогает статика» от «помогает просто крупная группа»
> должна пара `--kv-group-size 4096` (одна группа на весь префикс).

**Следствие для остальной сетки.** Если при трёх битах верная ось уже ничего не
стоит, то R4 и статике там нечего возвращать: клетки «шкала на канал ± R4» при трёх
битах ожидаемо дадут ноль. Информативными остаются клетки со шкалой на токен —
они проверяют, бьёт ли неверная ось по ответам. Место, где у статики и R4 может
появиться эффект на ответах, — **два бита**.

#### Сводка 14 сентября: лучшая схема кэша и что в ней доказано

На трёх–четырёх битах точность решают две ручки — какой стор стареет и ось
группировки K. Остальное — экономия памяти.

| ручка | выбор | доказано |
|---|---|---|
| политика | префикс раз на блок, текущий блок каждый шаг | ответы, обе модели |
| разрядность | **3 бита** | ответы, обе модели |
| ось K | шкала на канал | тензор и решения; сетку осей на ответах решено не снимать |
| шкала K | **статическая** | ответы: 3 и 4 бита, обе модели |
| шкала V | **статическая, на канал** | ответы, 3 бита, обе модели (`p = 0.29`, `1.0`) |
| размер группы | не важен | решения; ответы ждут `onegroup` |
| R4 | выключен | ответы на 2.0, `p = 1.0`; 1.5 ждёт `axis3` |
| QuaRot R1–R3 | для кэша не важен | до K не доходит (§2.8) |

**Сжатие:** при 4 битах K 4.00, V 4.25, в среднем 4.125 бита — **3.88×**; при 3
битах K 3.00, V 3.25, в среднем 3.125 — ≈5.1×; при 3 битах со статикой и на V —
3.00 бита, **5.33×** без измеримой потери точности на обеих моделях.

**Ручка за ручкой между моделями.** Всё, что касается точности на ответах в
поставляемом режиме, переносится: политика (−80.5 и −89.0 п. за заморозку блока),
разрядность (`p` от 0.45 до 1.0 на 3 и 4 битах), статика на K (`p = 0.38` и
`0.48`). Не переносятся две величины, обе про чувствительность к ключам: цена
неверной оси (1.75–1.99× против 4.09–4.78×, объяснено QK-Norm, §2.5a) и пол
округления на решениях (0.4% против 5.4%, механизм открыт, до ответов не доходит).

**Что может изменить схему:** два бита — единственное место, где способ округления
может начать решать на ответах. Сетка на два бита не запускалась.

#### Статистика К3 и К4 (14 сентября)

`check_key_error --dump` на 32 полотнах × 6 слоёв, обе маски, для LLaDA2.0-mini
ещё и с `--skip-qk-norm`; `check_block_reuse --dump-margins` на 16 промптах, обе
модели хранят K **после** RoPE (`--kv-rope post` на 1.5). Разбор —
`scripts/stats_theses.py`: бутстрэп по полотнам (слои фиксированы), тест по промптам.

**К3 держится, и с большим запасом.** Отношение «шкала на токен / шкала на канал», 4
бита, группа 128:

| | маска 0.50 | маска 0.00 |
|---|---|---|
| K, LLaDA2.0-mini | 4.71× [4.68–4.73] | 4.06× [4.04–4.07] |
| K, LLaDA2.0-mini без QK-Norm | 2.03× [2.02–2.04] | 1.81× [1.81–1.82] |
| K, LLaDA-1.5 | 1.99× [1.99–2.00] | 1.75× [1.75–1.75] |
| V, LLaDA2.0-mini (с нормой и без — тождественно) | 1.25× | 1.15× |
| V, LLaDA-1.5 | 1.20× | 1.12× |
| **K: 2.0 / 1.5** | **2.36× [2.35–2.38]** | **2.32× [2.30–2.33]** |
| **K: 2.0 с нормой / без, те же полотна** | **2.32× [2.30–2.33]** | **2.24× [2.23–2.25]** |
| K: 2.0 без нормы / 1.5 | 1.02× | 1.03× |
| V: 2.0 / 1.5 | 1.03× | 1.03× |

Без QK-Norm ключи двух моделей расходятся на 2–3% — ровно столько же, сколько V,
на который нормировка не действует. Бутстрэп отличает эти 2–3% от нуля, но
величина та же, что у V, так что это общий фон двух моделей, а не остаток эффекта.
Диапазоны по слоям не пересекаются: K у 2.0 3.05–6.13×, у 1.5 1.30–2.32×. Проверка
прибора: V с нормой и без совпадает до третьего знака (частное 1.00, `p = 1`) —
флаг трогает только Q/K.

**Попутно: QK-Norm ещё и уменьшает ошибку на верной оси на треть.** K 2.0 против
1.5 — 0.67× [0.66–0.67], с нормой против без — 0.69×. Нормировка одновременно
делает верную ось точнее и неверную дороже: оба следствия одного механизма —
устойчивых выбросов в фиксированных каналах.

Оговорка к ширине интервалов: бутстрэп по полотнам ловит только разброс текста.
Слои, чекпойнт и выбор модели он не ресэмплит, так что «2.32 [2.30–2.33]» — точность
замера на этой паре, а не обобщение на семейства. Для него нужна третья пара.

**К4 держится, но разрыв втрое, а не в 13 раз.** Доля решений, изменённых
округлением, `every_n:1`:

| | LLaDA-1.5 | LLaDA2.0-mini | 2.0 / 1.5 | по промптам |
|---|---|---|---|---|
| 4 бита | 1.12% [0.54–1.86] | 3.32% [2.49–4.20] | 3.0× [1.6–6.5] | парный `p = 0.007`, без пар `0.0012` |
| 3 бита | 1.66% [0.73–2.98] | 4.88% [3.71–6.25] | 2.9× [1.5–6.8] | парный `p = 0.004`, без пар `0.0011` |

Прежние числа §2.7 (0.7% / 0.4% против 3.7% / 5.4%, «в 5–13 раз») сняты на 8
промптах, и LLaDA-1.5 тогда хранила K **до** RoPE. На тех же первых 8 промптах
новая строка 2.0 воспроизводится (3.5% и 5.0% против 3.7% и 5.4%), а строка 1.5 —
нет: 1.1% и 1.2% против 0.7% и 0.4%. Изменился только тензор стора, значит на 1.5
округление K после RoPE стоит дороже, чем до. Это согласуется с KVQuant (квантовать
K до RoPE лучше): RoPE вращает пары каналов на угол, зависящий от позиции, и
выброс фиксированного канала расползается по паре. Честное сравнение моделей —
на одном тензоре, после RoPE, и там разрыв ≈3×. Заодно ушла немонотонность прежней
строки 1.5 (3 бита лучше 4): теперь 1.66% > 1.12%.

У 1.5 один промпт выбивается (10.2% при 3 битах, 5.5% при 4) — без него пол 1.5
был бы 0.8–1.1%, а разрыв 4.0–4.4×. Это единственная пара из 16, где 1.5 хуже 2.0;
ещё одна — ничья, в остальных 14 (на обоих битах) выше пол у 2.0.

**Одна группа на весь префикс, LLaDA-1.5** (`l15_G1_*`, динамическая шкала на
канал с группой 4096 — по сути одна шкала на канал на весь стор): 86.0% при 4
битах и 82.5% при 3, против 87.0% и 83.5% у статики на K и 83.0% у динамики с
группой 128. Всё в пределах шума повторного прогона. На ответах 1.5 «статика» и
«крупная группа» не различаются — обе бесплатны; значит, выигрыш статики не в
точности, а в том, что шкалы не надо считать и хранить. Пара для 2.0
(`l20_G1_*`) ещё идёт.

#### Два бита на ответах: ось ключей решает — и только на модели с QK-Norm (16 сентября)

Восемь прогонов `queue2.sh`, поставляемый режим, 200 задач GSM8K, 2 бита. Между
первыми тремя клетками модели меняется только схема для `K`; `V` везде
динамический, шкала на токен. LLaDA-1.5 во всех клетках хранит `K` после RoPE.

| 2 бита | LLaDA-1.5 | LLaDA2.0-mini |
|---|---|---|
| шкала на канал | 80.50 | **84.50** |
| шкала на токен | 82.00 | **1.50** |
| QuaRot (R4 → шкала на токен) | 84.00 | **1.50** |
| статика на K и V | 73.50 | 55.00 |
| эталон без сжатия | 83.50 | 90.50 |

**Тезис К3 дошёл до ответов, и разрыв не количественный, а качественный.** На
LLaDA2.0-mini неверная ось и QuaRot уничтожают модель: 166 задач против 0,
`p = 2·10⁻⁵⁰` в обеих парах. На LLaDA-1.5 ни ось, ни поворот не решают ничего:
ось 15 против 18 (`p = 0.73`), QuaRot против шкалы на канал 11 против 18
(`p = 0.27`), QuaRot против эталона 13 против 12 (`p = 1.0`).

Предсказание тензорного замера подтвердилось: там при 2 битах отношение осей было
2.95× у 2.0 против 1.88× у 1.5, и именно на модели с QK-Norm запас кончился
первым. Это первый раз, когда QK-Norm видна на точности задач, а не на тензоре.

**Как именно разваливается 2.0.** Не бессвязный текст, а отказ рассуждать: вместо
решения модель печатает заглушку («The answer is N.», «The correct answer is 2.»),
и 20–26 ответов из 200 обрываются по длине. Пол округления (§выше) предсказывал
чувствительность решений, и здесь она бьёт по самой способности разворачивать
рассуждение.

**Статика при 2 битах перестаёт работать — на обеих моделях.** 73.50 против 80.50
на 1.5 (29 против 15, `p = 0.049`) и 55.00 против 84.50 на 2.0 (62 против 3,
`p = 2·10⁻¹⁵`). Прежнее «статика бесплатна» верно только при 3–4 битах: пять
корзин по доле маски и максимум по восьми задачам не удерживают диапазон, когда
уровней всего четыре. Статика на 2.0 при этом всё равно много лучше неверной оси
(55.0 против 1.5, `p = 5·10⁻³⁰`) — то есть шкала на канал спасает даже
откалиброванная заранее.

**Два бита не бесплатны и при верной оси.** У 2.0 84.50 против 90.50 — 5 задач
против 17, `p = 0.017`, это первый значимый проигрыш разрядности во всей работе.
У 1.5 80.50 против 83.50 — 13 против 19, `p = 0.38`, в пределах шума.

**Что это меняет в рецепте.** При 2 битах: только динамическая шкала на канал для
`K`; статика не годится; QuaRot противопоказан на моделях с QK-Norm. При 3–4 битах
всё по-прежнему.

#### Закон по весам: разброс γ предсказывает цену оси по слоям (16 сентября)

`check_key_error.py --all-layers --split-rope --rotate`, 32 полотна, маска 0.5,
группа 128; `gamma_law.py` — Спирмен по 20 слоям, перестановочный `p`, бутстреп по
слоям. «Добавлено нормой» — отношение цены оси с нормой к цене без неё
(`--skip-qk-norm`), то есть вклад самой нормы в том же слое.

| вся голова | 4 бита | 3 бита | 2 бита |
|---|---|---|---|
| ρ(max/median γ, цена оси) | **+0.87** [+0.67, +0.96], p = 10⁻⁴ | **+0.81**, p = 10⁻⁴ | +0.08, p = 0.74 |
| ρ(max/median γ, добавлено нормой) | **+0.75** [+0.40, +0.92], p = 10⁻⁴ | **+0.65**, p = 0.003 | −0.10, p = 0.68 |
| ρ(max/median γ, цена QuaRot) | +0.67, p = 0.002 | +0.88, p = 10⁻⁴ | **+0.85**, p = 10⁻⁴ |

**Это не тренд глубины.** И разброс γ, и цена оси убывают с глубиной (ρ = −0.66 и
−0.58), но частная корреляция при поправке на номер слоя держится: +0.79 для цены
оси на 4 битах, +0.59 для добавленного нормой, +0.63 на 3 битах, +0.71 для QuaRot
на 2. Перестановочный тест, сохраняющий тренд (меняются только соседние слои):
p = 0.027, 0.044, 0.010, 0.007. Посчитано по таблицам, в `gamma_law.py` пока не
встроено.

**На 2 битах закон для оси пропадает.** Отношения сжимаются до 2.3–3.5× против
3.5–6.5× на 4 битах: при четырёх уровнях ошибается и шкала на канал. Вероятно,
насыщение, не проверено. Развал на ответах от этого не зависит: цена 2.3–3.5× есть
в каждом слое.

**Контроль RoPE в задуманном виде не работает — ошибка постановки.** Требование
было: закон должен держаться в невращаемой половине головы. Там он не значим
(+0.29, p = 0.21; добавлено нормой +0.35, p = 0.12), но по причине, которая
контроль обессмысливает: **γ в невращаемой половине почти плоский** (max/median
1.14–2.25 против 2.3–6.5 у пар во вращаемой). Все громкие множители сидят на
вращаемых каналах. При этом невращаемая половина платит за ось **больше всех**
(3.2–13.1× на 4 битах): шкала на токен общая на голову, её задают громкие
вращаемые каналы, тихие округляются в ноль. Это жертвы, а не источник, и цена
жертв следует разбросу γ **всей головы**: ρ = +0.88 (p < 10⁻⁴), добавлено нормой
+0.85. Механизм подтверждается, но разделить γ и частоты RoPE на весах нельзя —
это делает только перенос ниже.

#### Перенос γ: неравномерность γ достаточна для развала — показано вмешательством (16–17 сентября)

`dllmquant/algos/smooth_qk.py`, `--migrate-qk α`. Множитель нормы ключей делится
на `s`, нормы запросов умножается на `s`. Внимание видит только произведение, так
что модель та же. Два ограничения: `s` общий на пару RoPE (i, i + rd/2) — то же,
что в SmoothAttention у QServe, — и `s` округлён до степени двойки, иначе в
bfloat16 произведение теряет младшие биты мантиссы.

**Первая версия провалилась на воротах.** Выравнивание к среднему геометрическому
головы (`mode="geomean"`) было точным (сдвиг внимания 0), но пики не убрало:
громкий канал с тихим партнёром по RoPE сохраняет √(громкий/тихий) пика, а пары
ниже среднего получают `s < 1` и **усиливаются**. Слой 19: \|K\| max 24 → 48.5,
crest 9.1 → 10.4; слой 10: crest 13.1 → 12.6. Прогоны с этим переносом на ответах
не считались. Рабочая версия (`mode="shrink"`, коммит `5786b71`): `s` — больший γ
пары, делённый на медиану головы, обрезан снизу единицей. Ни один канал ключей не
становится громче.

**Ворота на чекпойнте** (`check_migration.py`, все 20 слоёв, 128 токенов):

| | до | после |
|---|---|---|
| сдвиг вероятностей внимания | — | **0.00** во всех слоях |
| crest ключей, среднее по слоям | 11.47 | **6.30** |
| \|K\| max, диапазон по слоям | 18.8–85.0 | 9.8–17.8 |
| разброс γ ключей (max/median) | 3.4–9.2 | 1.3–1.4 |

Пики упали **во всех 20 слоях** при нетронутом RoPE.

**Ответы**, 200 задач GSM8K, 2 бита, поставляемый режим, LLaDA2.0-mini:

| кэш | без переноса | с переносом | спасено / потеряно | McNemar |
|---|---|---|---|---|
| 16 бит | 90.50 | **90.50** | — | — |
| шкала на канал | 84.50 | **84.50** | 0 / 0, **200 из 200 ответов совпали слово в слово** | 1.0 |
| шкала на токен | 1.50 | **57.50** | **113 / 1** | **1.1·10⁻³²** |
| шкала на токен, α = 0.5 | 1.50 | **7.50** | 14 / 2 | 0.004 |
| QuaRot | 1.50 | **0.00** | 0 / 3 | 0.25 |

От α = 0.5 к α = 1: 102 / 2, p = 5.4·10⁻²⁸. Обрывов по длине: 21 у шкалы на токен
при α = 1, 36 при α = 0.5, 82 у QuaRot, 4 у шкалы на канал.

**Что установлено.**

1. **Перенос — тождество не только на тензоре, но и на генерации.** Шкала на канал
   не должна его замечать (каналы `K` умножены на степени двойки, коды те же, `Q`
   компенсирует), и 200 ответов из 200 совпали побуквенно при 2-битном кэше.
   16-битный контроль совпал с эталоном.
2. **Пиков, которые вносит неравномерность γ, достаточно для большей части
   развала.** Единственное вмешательство, не меняющее модель, возвращает шкале на
   токен 113 задач и отнимает одну. Возвращено 56 из 83 пунктов разрыва с верной
   осью, около двух третей. Формулировать именно так, а не «γ — причина»: перенос
   в режиме `shrink` не убирает γ, а опускает громкие пары к медиане, и опыт не
   отличает «важен именно γ» от «важны пики такой величины» — сглаживание по
   активациям (SmoothAttention), скорее всего, спасло бы тоже. Корректное
   причинное утверждение контрфактическое: без неравномерной части γ модель не
   разваливается.
3. **Эффект идёт с дозой**, оба шага значимы: 1.5 → 7.5 → 57.5. Форма
   нелинейна — первая половина по α даёт 6 пунктов, вторая 50, — **но α не
   линейная мера дозы.** Множитель округляется до степени двойки, и при α = 0.5
   огрубление сильнее: канал с γ в 1.6 медианы при α = 1 делится на 2, при
   α = 0.5 не делится вовсе (проверено на `gain_scales`: множители громких
   каналов 1/2/2/4/4/4/8/16 против 1/1/2/2/2/2/4/4). Откладывать надо не α, а
   достигнутый crest ключей. **Снято 18 сентября** (`check_migration.py --alpha
   0.5`, все 20 слоёв, CPU): средний crest 11.53 → **7.73** при α = 0.5 против
   11.47 → 6.30 при α = 1. В единицах crest переход тот же резкий: 11.5 → 1.5%,
   7.7 → 7.5%, 6.3 → 57.5%, — снижение пиков на треть почти ничего не даёт, а
   следующее небольшое возвращает 50 пунктов. Нелинейность не артефакт
   округления α. Порог по-прежнему по трём точкам, но форма перехода теперь
   читается в физической величине. Практическое следствие держится: половинный
   перенос, стандартный в SmoothQuant и SmoothAttention, здесь почти ничего не
   даёт.
4. **Меняется подпись отказа.** Из 59 задач, где шкала на токен после переноса
   ошибается, а шкала на канал верна, заглушек («The answer is N.») всего 2;
   остальное — сбившиеся рассуждения с повторами. Развал снят, осталась обычная
   деградация от шума.
5. **QuaRot перенос не спасает** — 0 / 3, различие с исходным 1.5 в пределах шума.
   Само по себе это не говорило, что механизм отказа у поворота другой: так же
   выглядело бы «помог, но не опустил ниже порога». Различено ошибкой логитов —
   следующий подраздел: перенос отвечает у двух квантователей **с противоположным
   знаком**.

**Что не установлено.**

* **Остаток в 27 пунктов.** Кандидат — выбросы активаций до нормы: crest после
  переноса 6.3, не ~1. Не проверено.
* **Почему именно у QuaRot ошибка растёт.** Направление измерено (подраздел ниже),
  разложение — нет. Кандидат: поворот размазывает шум по голове пропорционально
  длине ключа, перенос длину снижает слабо, а после обратного поворота шум ложится
  и на каналы, где вырос `Q`. Проверка — отдельно мерить ‖q‖ и ‖k‖ по каналам до и
  после.
* **Перенос на другие модели.** Только LLaDA2.0-mini. Развал того же вида на
  Qwen3 уже виден в чужих таблицах (KVLinC, OptR, OScaR — §1), так что прямая
  проверка — тот же перенос на Qwen3-8B.
* **Диффузионность.** В механизме нет ничего диффузионного; это результат про кэш
  моделей с QK-Norm, снятый на диффузионной модели. Связь с политикой обновления
  (§2.9) не мерилась.

**Как заявлять.** «Развал поворотных и по-токенных схем кэша при 2 битах на
моделях с QK-Norm наблюдался (KVLinC, OptR, OScaR), но не объяснялся. Мы
показываем, что пиков, которые вносит неравномерность множителя QK-Norm,
достаточно для большей части развала: бит-точный перенос этой части из ключей в
запросы оставляет модель неизменной (200/200 одинаковых ответов) и возвращает
по-токенной схеме 113 задач из 200 при одной потерянной. Поворот тот же перенос не
спасает: перенос уменьшает ошибку логитов по-токенной схемы в 2.4 раза, а у
поворотной увеличивает в 1.5 раза, на всех трёх разрядностях». Не заявлять:
открытие развала; операцию
переноса и ограничение на пары RoPE (QServe); «γ — единственная причина» (остаток
27 пунктов); порог по дозе (α огрублена округлением).

#### Почему QuaRot не спасается: ошибка логитов отвечает на перенос с другим знаком (17 сентября)

`check_key_error.py --logit-error`: относительная ошибка логитов
‖q·(K − K̂)ᵀ‖ / ‖q·Kᵀ‖ на разрешённых маской позициях, 32 полотна × 20 слоёв, маска
0.5, группа 128, LLaDA2.0-mini без переноса (`ke_l20_base_L`) и с полным переносом
(`ke_l20_mig_L`); разбор `scripts/logit_compare.py`. Знаменатель при переносе бит в
бит тот же, так что ошибка сравнима до и после — в отличие от ошибки в
пространстве `K`, где громкие каналы после переноса переехали в `Q`.

| ошибка логитов | 2 бита: до → после | 3 бита | 4 бита |
|---|---|---|---|
| шкала на канал | 0.145 → 0.145, **1.00×** | 1.00× | 1.00× |
| шкала на токен | 0.709 → 0.295, **0.42×** | 0.44× | 0.60× |
| QuaRot | 0.446 → 0.688, **1.54×** | 1.62× | 1.61× |

**Прибор исправен:** шкала на канал ровно 1.00× — то же, что 200/200 одинаковых
ответов.

**Три гипотезы, и какая выжила.**

* **H1 — поворот отказывает по другой величине**, перенос ему вредит. Предсказание:
  ошибка QuaRot после переноса не меньше, чем до. **Держится** на всех трёх
  разрядностях.
* **H2 — перенос помог, но не опустил ниже порога**, механизм тот же.
  Предсказание: ошибка QuaRot падает, но остаётся выше спасённой клетки.
  **Отпадает**: ошибка растёт.
* **H3 — дело в хрупкости модели**, не в повороте. Требует LLaDA-1.5
  (`ke_l15_base_L`), не снято.

**Что установлено.** Одно и то же вмешательство уменьшает ошибку внимания
по-токенной схемы в 2.4 раза и увеличивает у поворотной в 1.5 раза. Отклик
противоположного знака — значит, две схемы отказывают по разным величинам:
по-токенная — от пиков ключей, которые вносит γ; поворотная — не от них. **Какая
именно величина у поворота — измерено 19 сентября, см. следующий подраздел.**

**Попутно: пять клеток ложатся на одну монотонную кривую точности по ошибке
логитов,** независимо от схемы и переноса:

| ошибка логитов, 2 бита | 0.145 | 0.295 | 0.446 | 0.688 | 0.709 |
|---|---|---|---|---|---|
| точность | 84.5 | 57.5 | 1.5 | 0.0 | 1.5 |

Развал — где-то между 0.30 и 0.45. Это объясняет оба исхода одной картиной: перенос
опустил по-токенную схему ниже порога, а поворотную поднял выше. **Оговорки:**
ошибка снята на полотнах из текстов вопросов при маске 0.5 одним проходом, а
развал происходит на генерации. Порог — наблюдение, не закон.

**H3 проверена 18 сентября — отпадает.** `ke_l15_base_L`, тот же протокол на
LLaDA-1.5 (32 слоя, маски внимания нет). Ошибка логитов при 2 битах:

| LLaDA-1.5, 2 бита | ошибка логитов | точность |
|---|---|---|
| шкала на канал | 0.196 | 80.5 |
| QuaRot | 0.201 | 84.0 |
| шкала на токен | 0.259 | 82.0 |

QuaRot у 1.5 даёт **0.20** против 0.45 у 2.0: поворот у 1.5 выживает не потому, что
модель терпимее, а потому, что ошибка вдвое меньше. Все три клетки 1.5 лежат ниже
0.30 и все выживают — это **согласуется** с общей кривой для двух моделей (восемь
точек, ни одна не противоречит), но её **не доказывает**: у 1.5 нет ни одной точки
выше 0.30, так что терпимость моделей к одной и той же ошибке не сравнена. Строгая
проверка — клетка 1.5 с ошибкой логитов около 0.3–0.5. Сравнивать абсолютные
точности осторожно: эталоны 83.5 и 90.5.

#### Устаревание × разрядность на ответах: ошибки складываются сверхаддитивно (18 сентября)

`scripts/pod/policy.sh`: `--reuse-window`, текущий блок хранится квантованным и
между обновлениями читается устаревшим; префикс раз на блок, точен по маске. 200
задач GSM8K, полотно 512, 256 шагов (16 на блок), ось `K` — на канал. Разбор
`scripts/policy_table.py`.

| обновление текущего блока | 16 бит | 3 бита | 2 бита | цена 3 бит | цена 2 бит |
|---|---|---|---|---|---|
| каждый шаг | **90.5** | 89.5 | 83.5 | 1.0 (8 / 6, p = 0.79) | **7.0** (19 / 5, p = 0.0066) |
| раз в 2 шага | 84.5 | — | 60.0 | — | **24.5** (54 / 5, p = 1.9·10⁻¹¹) |
| раз в 4 шага | 47.5 | 37.0 | 16.5 | **10.5** (50 / 29, p = 0.024) | **31.0** (71 / 9, p = 4.4·10⁻¹³) |
| раз на блок | 1.5 | 1.0 | 1.0 | пол | пол |

Цена устаревания при 16 битах: раз в 2 шага −6.0 (19 / 7, p = 0.029), раз в 4 шага
−43.0 (91 / 5, p = 1.6·10⁻²¹), раз на блок −89.0 (178 / 0, p = 5.2·10⁻⁵⁴).

**Контроль прошёл:** 16 бит с обновлением каждый шаг — 90.5, как поставляемый кэш.

**1. Цена разрядности растёт с устареванием, а не падает.** 7 пунктов при свежем
кэше, 31 — при обновлении раз в 4 шага. На решениях (§2.9) было обратное:
разрядность тонула в устаревании. **Пункт 1 тезиса в форме «цена бит исчезает с
устареванием» на ответах опровергнут.** Нуль при заморозке на блок — не
поглощение, а пол: модель мертва и без квантования (1.5%).

**2. Ошибки складываются сверхаддитивно — уже при самом слабом устаревании.** Раз в
2 шага: 2 бита стоят 7, устаревание 6, сумма 13; вместе — 30.5 (90.5 → 60.0). Раз в 4
шага: 7 и 43 против 74. **И три бита, бесплатные при свежем блоке (1.0, p = 0.79),
стоят 10.5 при обновлении раз в 4 шага (p = 0.024)**: разрядность, которую при
поставляемом режиме можно не считать, перестаёт быть бесплатной, как только блок
устаревает. Цена каждой разрядности растёт с возрастом блока, пока обе клетки не
упираются в пол.

**3. Устаревание текущего блока на ответах огромно.** Переиспользовать блок хотя бы
три шага из шестнадцати — минус 43 пункта; заморозить — развал. Это и есть причина,
по которой dInfer и Fast-dLLM текущий блок не переиспользуют; здесь она измерена.

**Как это согласовать с §2.9.** Решения и ответы — разные величины. Зазоры решений
субаддитивны (§2.7.7), а точность — резкая функция суммарного ущерба: пока модель
далеко от развала, лишняя ошибка стоит мало, у края — очень много, за краем —
нечего терять. Та же картина, что у ошибки логитов в подразделе выше (развал между
0.30 и 0.45) и у дозы переноса в единицах crest (переход между 7.7 и 6.3). **Это
гипотеза, а не вывод:** ошибка логитов у клеток с устареванием не мерилась —
`check_key_error` видит только округление, — так что лягут ли они на ту же кривую,
неизвестно.

**Оговорки.** Режим не поставляемый: ни dInfer, ни Fast-dLLM текущий блок не
переиспользуют, это верхняя граница устаревания на этой модели. В 2-битных клетках
квантуется и текущий блок (83.5 против 84.5 у `l20_B2_ch`, где только префикс).
Одна модель, одна задача. Фиксированное расписание, 2 токена за шаг; при пороге
уверенности — подраздел ниже.

#### Оба пункта при декодировании по порогу уверенности (19 сентября)

`scripts/pod/thresh.sh`: `--threshold 0.95` — фиксируются все замаскированные позиции с
уверенностью ≥ 0.95, минимум одна за шаг, как в Fast-dLLM-v2 и BitSieve (подблоков
по 8 нет). Проверка, что результаты не созданы фиксированным расписанием.

| клетка, 2 бита | фиксированное расписание | порог 0.95 | шагов на блок |
|---|---|---|---|
| эталон, 16 бит | 90.5 | **93.5** | 8.1 |
| ключи на канал | 84.5 | **90.0** | 10.4 |
| ключи на токен | 1.5 | **2.0** | 22.9 |
| на токен + перенос γ | 57.5 | **73.0** | 17.5 |
| блок переиспользуется, каждый шаг | 83.5 | **84.5** | 11.4 |

**Пункт 2 держится.** Развал оси повторяется (2.0), перенос спасает, и даже сильнее:
73.0, то есть возвращено 71 из 88 пунктов разрыва с верной осью (около 80% против
двух третей при фиксированном расписании). **У развала появилась вторая подпись — в
самом сэмплере:** блок с по-токенными ключами требует 22.9 шага против 8.1 у точного
кэша. Порог ждёт уверенности, которую испорченные ключи не дают; перенос возвращает
часть (17.5).

**Пункт 1 — первая клетка:** 2 бита при переиспользуемом блоке с обновлением каждый
шаг стоят 9.0 пункта (93.5 → 84.5) против 7.0 при фиксированном расписании. Клетки
«раз в 2 шага» при пороге досчитываются.

Абсолютные числа выше, чем при фиксированном расписании (93.5 против 90.5): порог
тратит шаги там, где модель не уверена. Сравниваются контрасты внутри колонки.
McNemar для этих клеток не посчитан.

#### Отказ QuaRot объясняется геометрией q и k, а остаток переноса — полом в громких каналах (19 сентября)

`scripts/pod/diag.sh`: 16 полотен, все слои, 2 и 3 бита, маска 0.5, дозы переноса
0 / 0.25 / 0.5 / 0.75 / 1 в режиме `shrink`, плюс `geomean` и LLaDA-1.5. Разбор
`scripts/diag_compare.py`. Кроме ошибки логитов пишутся две величины геометрии:
средний `|cos(q, k)|` и **изотропный множитель**
`√(Σ‖q‖²‖k‖²) / √(Σ(q·k)²)` — предсказание относительной ошибки логитов для шума,
который изотропен и пропорционален длине ключа. Именно такой шум делает поворот с
последующей шкалой на токен.

| прогон | ошибка логитов: на канал / на токен / QuaRot | \|cos\| | iso | QuaRot / iso |
|---|---|---|---|---|
| 2.0, без переноса | 0.144 / 0.713 / 0.447 | 0.090 | 12.6 | 0.0353 |
| 2.0, α = 0.25 | 0.144 / 0.526 / 0.409 | 0.101 | 10.4 | 0.0394 |
| 2.0, α = 0.5 | 0.144 / 0.411 / 0.472 | 0.091 | 11.7 | 0.0402 |
| 2.0, α = 0.75 | 0.144 / 0.343 / 0.545 | 0.081 | 13.2 | 0.0412 |
| 2.0, α = 1 | 0.144 / **0.295** / 0.687 | 0.068 | 16.5 | 0.0416 |
| 2.0, geomean | 0.144 / 0.842 / 1.934 | 0.067 | 52.8 | 0.0366 |
| LLaDA-1.5 | 0.195 / 0.259 / 0.201 | 0.192 | 4.8 | 0.0421 |

**1. Причина отказа поворота названа.** Отношение «ошибка QuaRot / изотропный
множитель» лежит в 0.0353–0.0421, то есть меняется в **1.19 раза**, тогда как сама
ошибка QuaRot по этим семи прогонам меняется в **9.6 раза**. Множитель объясняет
поворот почти полностью, и это уже не гипотеза: он посчитан из `q` и `k` без
всякого квантования.

> Поворот делает ошибку изотропной, а её цена в логитах обратна тому, насколько
> запрос и ключ сонаправлены. У LLaDA2.0-mini `|cos|` = 0.090 против 0.192 у
> LLaDA-1.5, то есть логит собирается из почти ортогональной пары, и изотропный шум
> бьёт по нему в 2.6 раза сильнее (iso 12.6 против 4.8). Поэтому QuaRot губит одну
> модель и не трогает другую.

**2. Почему перенос делает QuaRot хуже.** Перенос сохраняет каждое `q·k` бит в бит,
но переносит громкость в `Q`, отчего ‖q‖·‖k‖ растёт, а `|cos|` падает: 0.090 → 0.068,
iso 12.6 → 16.5, ошибка QuaRot 0.447 → 0.687. Это **не** свойство переноса вообще:
тест `test_qk_geometry.py` показывает, что множитель может двигаться в обе стороны,
и на этой модели он растёт. Ещё резче это видно на `geomean`, который вдобавок
усиливает тихие каналы: iso 52.8, ошибка QuaRot 1.93, crest ключей 13.4 против 11.5
без переноса. Выбор режима `shrink` этим и подтверждён.

**3. Остаток переноса — пол в громких каналах.** Ошибка логитов по-токенной схемы,
разложенная на каналы, которые γ делает громкими, и на остальные:

| α | громкие | остальные | всего |
|---|---|---|---|
| 0 | 0.199 | 0.672 | 0.713 |
| 0.5 | 0.175 | 0.364 | 0.411 |
| 1 | **0.169** | **0.225** | 0.295 |

Вклад остальных каналов падает втрое, вклад громких **не падает вовсе**. Это и есть
кандидат 1 из разбора остатка: перенос убирает пики из `K`, но ставит те же
множители в `Q`, и шум шкалы на токен в этих каналах умножается на выросший запрос.
Сумма сходится в квадратуре: √(0.169² + 0.225²) = 0.281 против измеренных 0.295.
**Следствие:** дозой этот остаток не лечится — лучшая доза в диапазоне это α = 1, —
лечится только тем, что у этих каналов будет своя шкала, то есть верной осью.

**4. Доза и порог.** Ошибка логитов падает монотонно с дозой: 0.713 → 0.526 → 0.411
→ 0.343 → 0.295. Известные точности: α = 0 даёт 1.5%, α = 0.5 — 7.5%, α = 1 — 57.5%.
То есть 0.411 ещё в развале, а 0.295 уже нет, и порог сужается до **0.30–0.41** — в
согласии с тем, что было видно по клеткам оси. Резкая нелинейность дозы объясняется
не формой переноса, а этим порогом.

**5. Crest после переноса совпадает с LLaDA-1.5**, теперь в одном протоколе: среднее
по слоям 6.49 против 6.76, максимум 7.44 против 8.63. По пикам ключи 2.0 после
переноса не хуже модели, где шкала на токен работает; остаточная разница в ошибке
логитов идёт от `Q`, а не от пиков `K`.

**Оговорки.** Геометрия и ошибки сняты на полотнах из текстов вопросов при маске
0.5 одним проходом, а ответы — на генерации. Отношение QuaRot/iso постоянно с
точностью 19%, то есть множитель объясняет главное, но не всё. Точности при
α = 0.25 и 0.75 не мерились.

#### Fast-dLLM-v2: тот же механизм с другим параметром — bias в k_proj (21–22 сентября)

Третья блочная модель, в обвязке таблиц BitSieve. `Efficient-Large-Model/Fast_dLLM_v2_7B`,
основа Qwen2.5-7B: плотная, **QK-Norm нет, у `q_proj`/`k_proj`/`v_proj` есть bias**,
полный RoPE. Код модели вшит (`dllmquant/vendor/fast_dllm_v2`, ревизия `0661abf`),
декодирование — её собственный `generate`: блоки 32, подблоки 8, порог 0.95, до 2048
токенов. Квантуется только префикс: подменяется класс `DynamicCache`, который модель
строит сама (`dllmquant/models/fast_dllm_v2_local.py`). `scripts/evaluate_fdv2.py`,
очереди `fd*.sh`, 200 задач GSM8K.

**Контроль:** кэш 16 бит — 82.5 против опубликованных 85.0 (их стандартная ошибка 2.5),
обрезанных и нераспознанных ответов нет.

**Сравнение с LLaDA2.0-mini** (обе при пороге 0.95; эталоны 93.5 и 82.5):

| ключи | LLaDA2.0-mini: 4 / 3 / 2 бита | Fast-dLLM-v2: 4 / 3 / 2 бита |
|---|---|---|
| по каналам | 92.0 / 93.0 / 90.0 | 74.0 / 58.0 / 18.0 |
| по токенам | 91.5 / 59.5 / 2.0 | **0.0** / 0.0 / 0.0 |
| QuaRot | 92.5 / 89.0 / 2.5 | **1.5** / 0.5 / 1.0 |

**1. Неверная ось губит обе модели, но модель с bias — раньше:** у Fast-dLLM-v2 по
токенам и QuaRot мертвы уже при **4 битах**, у LLaDA2.0-mini при 4 битах все оси живы, а
неверная начинает бить с 3 бит. QuaRot у MoE-модели при 3 битах жив (89.0) и заметно
лучше шкалы на токен (59.5) — в согласии с ошибкой логитов.

**2. Вмешательство: bias вне кэша.** RoPE линеен, ключ = `RoPE(Wx) + RoPE(b)`; второе
слагаемое — один и тот же вектор до поворота во всех позициях. `--pre-bias` вычитает
повёрнутый bias каждого слоя перед квантователем и прибавляет после — точно при любой
разрядности. Это аналог переноса γ для модели без QK-Norm. **Сам приём не новый** (§1,
таблица занятых заявок): квантование до bias — KVQuant* в «Quantization Hurts Reasoning?»,
вычитание общей для ключей составляющей — «smooth K» в SageAttention. Наша форма — точное
вычитание `RoPE(b)` (или `RoPE(b)·H` при R4) из кэша, хранящего ключи после RoPE.

| Fast-dLLM-v2, 4 бита | без | bias вне кэша | выиграно / потеряно | McNemar |
|---|---|---|---|---|
| 16 бит (контроль) | 82.5 | **82.5** | 0 / 0, **199 из 200 ответов слово в слово** | 1.0 |
| по токенам | 0.0 | **84.0** | **168 / 0** | **5.3·10⁻⁵¹** |
| по каналам | 74.0 | **85.0** | 32 / 10 | 0.001 |
| QuaRot | 1.5 | ~~0.0~~ **недействительно** | — | — |

> **Поправка 22 сентября.** Клетка «QuaRot + bias вне кэша» была посчитана с ошибкой: при
> R4 в кэш приходит `RoPE(Wx + b)·H`, а вычиталось `RoPE(b)` без `H`. Это тоже тождество (16
> бит не могли его поймать), но bias оставался в диапазоне квантователя, то есть клетка
> мерила обычный QuaRot. Исправлено в `93784b9`, пересчёт — `fd9.sh`. На тензоре, где порядок
> был верным, центрированная ошибка QuaRot при 4 битах падает от вмешательства с 0.271 до
> 0.093 — ниже клетки «на токен», которая даёт 84.0. **Спасается ли поворот на этой модели —
> снова открытый вопрос.**
>
> **Пересчитано:** QuaRot при 4 битах с bias вне кэша — **1.5 → 79.0** (157 : 2,
> `p = 3.5·10⁻⁴⁴`), при 3 битах — **0.5 → 72.5** (145 : 1, `p = 3.3·10⁻⁴²`). **Поворот на
> Fast-dLLM-v2 спасается.**

**Почему поворот спасается здесь и не спасается на LLaDA2.0-mini.** Это не другой механизм
отказа, а другое вмешательство. Перенос γ не убирает громкость каналов, а **перекладывает её
в запросы** — модель та же, `q·k` те же, но ‖q‖·‖k‖ растёт, и изотропный шум поворота
умножается на выросший `Q` (ошибка QuaRot от переноса растёт в 1.5 раза). Вычитание bias
громкость **убирает совсем** и запросов не трогает: центрированная ошибка QuaRot падает
втрое (0.271 → 0.093), и ответы идут следом. **Для bias этим показано, что отказ поворота
вызван тем же выбросом.** Для γ этого не показано и этим вмешательством показать нельзя:
перенос не убирает громкость, а перекладывает её в запросы, и то, что у LLaDA2.0-mini
запросы и ключи почти ортогональны (|cos| = 0.09), может быть и собственным свойством
модели. Оба наблюдения следуют из одной оценки: ошибка поворота ≈ (длина квантуемого ключа)
× (изотропный множитель ‖q‖‖k‖ / |q·k|). Вычитание bias уменьшает первый множитель, не
трогая второй; перенос γ уменьшает первый, но портит второй.

**Точное вычитание `RoPE(b)` против среднего по SageAttention — разницы нет** (22 сентября,
`fd_sage.sh`). Вариант SageAttention на хранении (`--key-mean`): из ключей каждой записи
вычитается их среднее по токенам в каждом канале, остаток квантуется, среднее возвращается
при чтении.

| Fast-dLLM-v2 | без вмешательства | точное `RoPE(b)` | среднее (SageAttention) | McNemar |
|---|---|---|---|---|
| 4 бита, по каналам | 74.0 | **85.0** | 80.5 | 0.15 (20 : 11) |
| 4 бита, по токенам | 0.0 | 84.0 | 83.0 | 0.80 |
| 3 бита, по каналам | 58.0 | 80.5 | 79.5 | 0.87 |
| 3 бита, по токенам | 0.0 | 76.5 | 70.0 | 0.079 |
| 4 бита, QuaRot | 1.5 | 79.0 | **83.0** | 0.20 (11 : 19) |
| 2 бита, по каналам | 18.0 | **43.5** | 28.0 | **0.0000** (45 : 14) |

**Систематического победителя нет, и разброс в обе стороны около четырёх пунктов**
(23 сентября): на QuaRot среднее лучше — 83.0 против 79.0, вровень с эталоном 82.5, — а при
4 битах по каналам лучше точное вычитание, 85.0 против 80.5. На тензоре среднее чуть точнее
(центрированная ошибка при 4 битах по токенам 0.109 против 0.130, QuaRot 0.086 против 0.093),
хотя crest хранимых ключей у него выше — 9.1 против 6.8: вычитание среднего опускает RMS
сильнее, чем пик. Парные тесты: при 4 битах обе расходящиеся клетки незначимы — 20 : 11, `p = 0.15` там, где
впереди точная форма, и 11 : 19, `p = 0.20` там, где впереди среднее. **А при 2 битах точная
форма выигрывает значимо: 43.5 против 28.0, 45 : 14, `p = 2·10⁻⁵`.**

Это согласуется с механизмом и с тензором. После RoPE bias поворачивается вместе с позицией,
поэтому среднее по токенам ловит только ту его часть, которая не вращается, и оставляет
остальное в хранимом тензоре: crest ключей на хранении 9.14 против 6.80. При 4 битах этот
остаток тонет в шаге квантователя, при 2 — нет. Значит, **точная форма оправдана именно там,
где бюджет самый узкий**, а это как раз режим работы коллег (1 бит с отбором).

**Вывод (уточнён 23 сентября).** Сам приём — удаление общей для ключей составляющей — чужой,
и при 4 и 3 битах брать надо среднее из SageAttention со ссылкой: точная форма там ничего не
добавляет. Своего здесь два: (1) при 2 битах точное вычитание `RoPE(b)` измеримо лучше
среднего (45 : 14, `p = 2·10⁻⁵`), потому что после RoPE среднее по позициям физически не может
представить вращающийся bias; (2) это удаление **на хранении** спасает по-токенную схему и
поворот в кэше блочной модели, чего у SageAttention (внимание, не хранение) и у pre-bias (до
RoPE, «для QuaRot неприменимо») нет.

**2 бита: остаток — ключи, не значения** (`fd10.sh`). Только V в 2 битах — **85.0** (эталон
82.5, то есть бесплатно; с группой V по 32 канала 82.5), только K в 2 битах с bias вне кэша —
**56.0**; разница 64 : 6, `p = 2.4·10⁻¹³`. Значит, при двух битах ключи теряют около 27
пунктов **сверх** параметрического выброса, а значения ни при чём.

**2 бита:** по каналам с bias вне кэша 18.0 → **43.5** (61 : 10, `p = 4.6·10⁻¹⁰`) — заметно, но не до эталона: при двух
битах bias — не единственная потеря. По токенам (`fd_2_tok_pb`) — 0 из первых 5 задач при
166 с на задачу, остановлено как бесперспективное.

**3 бита:** по каналам 58.0 → **80.5** (55 : 10, `p = 1.2·10⁻⁸`), по токенам 0.0 → **76.5**
(153 : 0, `p = 1.8·10⁻⁴⁶`).

Вмешательство не меняет модель, развал по токенам снимает **полностью** (до уровня без
квантования), и даже потеря 8.5 пункта на правильной оси оказалась от bias. Про QuaRot —
см. поправку выше: клетка была недействительна. **Прежнее «Fast-dLLM-v2 хрупче в
целом» было неверно:** вся её хрупкость при 4 битах — выброс, который создаёт bias.

**Закона по слоям, как у γ, нет** (`scripts/bias_law.py`). Ни форма bias (концентрация в
паре, доля энергии в верхних парах, max |b|), ни его доля в ключе ‖RoPE(b)‖ / ‖k‖ не следуют
цене оси исходных ключей по 28 слоям: ρ от −0.44 до +0.27, лучший положительный — 0.19 при
`p = 0.34`; у γ на LLaDA2.0-mini было +0.87. Bias действует через несколько слоёв, а не
плавно: в слоях 2, 3, 13, 19 он составляет 91–97% ключа, и там цена оси самая высокая
(5.2–5.6×), а без bias падает в 2.5–3 раза. В слоях 0, 1, 27 ключ почти целиком bias (≈ 1.00),
почти постоянен и дёшев для обеих осей (1.8–2.9×), — поэтому связь не монотонна.
**Ворота вмешательства проходят:** без bias цена оси ниже в 27 из 28 слоёв (в среднем
3.40× → 2.14× в пространстве K, 2.33× → 1.57× по центрированным логитам), и падение тем
больше, чем больше доля bias (ρ = +0.50, `p = 0.007`). Внимание при чтении `bias_law.py` на
дампе с `--pre-bias`: колонки цены оси там — для ключей уже без bias; для закона их надо брать
из дампа без вмешательства.

**3. Тензор** (`fd_diag.sh`, `cdiag.sh`, та же сетка): цена неверной оси 2.3× — между
LLaDA-1.5 (1.3×) и LLaDA2.0-mini (4.9×); crest 7.4; |cos(q,k)| = 0.179. **Закон для QuaRot
держится на третьей модели:** ошибка QuaRot / изотропный множитель = 0.041, внутри
прежних 0.035–0.042.

**4. Общий порог по ошибке логитов опровергнут, и центрирование его не спасает.** По
нецентрированной ошибке Fast-dLLM-v2 выглядела устойчивее LLaDA2.0-mini, а на ответах
вышло обратное. Причина метрики найдена: bias добавляет почти один и тот же вектор ко
всем ключам, `q·b` — почти константа на запрос, softmax её не видит, а знаменатель
`‖q·Kᵀ‖` ею раздут. Центрированная ошибка (из строки логитов вычитается её среднее по
разрешённым ключам) это подтверждает: при 2 битах по токенам она поднимает ошибку
Fast-dLLM-v2 в 2.7 раза (0.345 → 0.940), LLaDA2.0-mini — в 1.3 (0.713 → 0.940). Но и
после центрирования при 4 битах по токенам ошибка у двух моделей одинакова — 0.351 и
0.346, — а точность 91.5 и 0.0. **Модели различаются чувствительностью к одной и той
же ошибке внимания;** правдоподобная причина — длина генерации (2048 против 512, ошибка
копится сотнями блоков собственного текста), не проверена. Внутри модели связь ошибки
и точности остаётся монотонной. Закон «QuaRot / изотропный множитель» относится к
нецентрированной ошибке; в центрированной он не держится (разброс 3–4×).

**Как заявлять:** «Фиксированный выброс ключей, созданный параметром в пути ключа, —
причина развала по-токенной схемы кэша на двух моделях с разными механизмами: множитель
QK-Norm у LLaDA2.0-mini и bias в `k_proj` у Fast-dLLM-v2. Точное вмешательство, которое
убирает этот выброс из диапазона квантователя, не меняя модель, развал снимает (113 : 1
и 168 : 0). Если вмешательство убирает громкость, а не перекладывает её в запросы, оно
спасает и поворотную схему (1.5 → 79.0 на Fast-dLLM-v2).» Не заявлять: закон по слоям для
bias (его нет) и общий для моделей порог ошибки внимания. Не заявлять: общий для моделей порог ошибки внимания.

**Практическое следствие для BitSieve:** на их собственной модели плотный 4-битный кэш
по каналам теряет 8.5 пункта, а хранение ключей без bias возвращает их полностью
(85.0), бесплатно и точно. Оговорка: у них отбор top-k, мерили мы плотный кэш.

#### Тезис о кэше диффузионной модели: текущая формулировка (17 сентября)

Рамка «цену кэша задают три решения — частота обновления, ось, архитектура нормы —
которых у авторегрессии нет или они независимы» **не доказана и снята**: связь
между решениями (пункт 3) не мерилась, ось и γ работают и у авторегрессии (Qwen3,
§1), а при 2 битах разрядность стоит и на верной оси (84.5 против 90.5, `p = 0.017`).
Что держится — два отдельных утверждения с адресами:

> **1.** В режиме, где текущий блок переиспользуется, округление и устаревание
> почти ортогональны на тензорах и субаддитивны на зазорах решений (§2.7.7, §2.9),
> **но на ответах складываются сверхаддитивно**: 2 бита стоят 7 пунктов при
> обновлении каждый шаг и 31 при обновлении раз в 4 шага; по отдельности 7 и 43,
> вместе 74. *Обновлено 18 сентября; прежняя форма «цена бит исчезает с
> устареванием» верна только для решений.*
>
> **2.** На LLaDA2.0-mini при 2 битах по-токенный и поворотный квантователи ключей
> обрушивают модель (84.5 → 1.5). Бит-точный перенос неравномерной части γ из
> ключей в запросы возвращает по-токенному квантователю 56 из 83 пунктов (113
> задач против 1) и не спасает поворотный. Поворотный отказывает по другой
> величине: его ошибка логитов следует изотропному множителю ‖q‖‖k‖ / |q·k| (с
> точностью 19% на девятикратном размахе), а у LLaDA2.0-mini запросы и ключи почти
> ортогональны, |cos| = 0.09 против 0.19 у LLaDA-1.5. Перенос эту величину
> ухудшает, потому что переносит громкость в запросы. Остаток по-токенной схемы
> после переноса — пол, который создают те же перенесённые множители: вклад громких
> каналов в ошибку логитов от дозы не зависит.
>
> **2′ (22 сентября).** Тот же механизм на второй модели с другим параметром: у
> Fast-dLLM-v2 выброс ключей создаёт bias в `k_proj`, и точное хранение ключей без
> него поднимает шкалу на токен при 4 битах с 0.0 до 84.0 (168 : 0), не трогая модель
> (199/200 одинаковых ответов при 16 битах); при 3 битах — с 0.0 до 76.5 (153 : 0). Здесь
> вмешательство спасает и QuaRot (1.5 → 79.0, 157 : 2): вычитание bias убирает громкость, а
> перенос γ лишь перекладывает её в запросы, что поворот ухудшает (ошибка QuaRot ×1.5).
> Вызван ли отказ поворота на LLaDA2.0-mini именно γ — не установлено. Утверждение —
> про параметрический выброс ключей вообще; QK-Norm — один из двух проверенных источников.

**Кандидат в объединяющий тезис (18 сентября) — в межмодельной форме опровергнут 22
сентября:** при одинаковой центрированной ошибке внимания LLaDA2.0-mini жива, а
Fast-dLLM-v2 мертва (подраздел про Fast-dLLM-v2). Ниже — исходная формулировка, верная
только внутри одной модели.

**Исходная формулировка (18 сентября).** Округление,
устаревание и неверная ось — источники одной ошибки внимания, а точность — резкая
пороговая функция от неё. Цена любого источника задаётся не им самим, а тем,
насколько близко к порогу модель уже стоит; у диффузионного кэша к порогу её
двигает устаревание, которого у авторегрессии нет. За: пять клеток оси и переноса
на одной кривой по ошибке логитов (развал между 0.30 и 0.45), три клетки LLaDA-1.5
ниже 0.30 и живые, переход по crest между 7.7 и 6.3, сверхаддитивность устаревания
и разрядности на ответах. Не хватает: ошибки логитов у клеток с устареванием —
нужен прибор, который меряет её при переиспользованном блоке.

**Логические оговорки, найденные при проверке.**

* **Спад цены бит на переворотах отчасти задан метрикой.** Переворот бинарен:
  когда устаревание уже перевернуло 12% позиций, округлению остаётся меньше что
  переворачивать. Опираться надо на непрерывные зазоры (§2.7.7, κ/d_r = 0.7–1.2),
  перевороты — иллюстрация.
* **Адрес пункта 1.** На LLaDA2.0-mini в поставляемом режиме устаревания нет
  структурно (§5); закон описывает режим с переиспользованием блока и
  двунаправленные модели, где на LLaDA-1.5 переносится структура, но не
  коэффициенты (§2.7.8).
* **Пункт 1 на ответах** снят очередью `scripts/pod/policy.sh` (подраздел выше).
  Возраст там считается на 16 шагов на блок против 32 в §2.9, так что сравнима
  форма, а не точки. Цена бит читается против 16 бит при той же политике.
* **Пункт 3 (ось × политика) этой очередью не проверяется** — ось в ней везде на
  канал. Нужна отдельная очередь с масштабом на токен, с переносом и без.

#### Сравнение LLaDA-1.5 и LLaDA2.0-mini: что переносится, а что нет (14 сентября)

Сводный график — страница «LLaDA-1.5 против LLaDA2.0-mini», семь панелей. В каждой
строке одно действие с кэшем и две точки, по одной на модель: рядом — переносится,
врозь — нет. Ниже то, что показывает каждая панель, и вывод.

**Т1, политика кэша (сдвиг точности, п.).** Кэш 16 бит против прогона без кэша на
1.5: +0.5, `p = 1.0`. Держать префикс весь блок: 1.5 −1.5 (85.0 → 83.5, `p = 0.66`,
парный интервал −6.0…+3.0), 2.0 — точно по построению. Заморозить текущий блок:
−80.5 и −89.0 п. (`p = 3·10⁻⁴⁷` и `5·10⁻⁵⁴`). **Переносится.** Кэш — два стора с
разным допуском: префикс меняется медленно, текущий блок — каждый шаг.

**К1 · К5, разрядность и статика (сдвиг точности, п.).** Десять клеток, все внутри
разброса повторного прогона (±3.7 п.), знак между моделями прыгает: статика K при 4
битах +3.5 на 1.5 и −3.5 на 2.0, при 3 битах 0.0 и +1.5. Эталон 1.5 — прогон без
кэша, 2.0 — кэш 16 бит. **Переносится:** 3 бита со статическими шкалами K и V дают
5.33× без измеримой потери на обеих. На 3–4 битах способ квантизации ничего не
решает; разница между схемами может появиться только при 2 битах.

**R4 (сдвиг против той же клетки без поворота).** 2.0: −0.5 п., `p = 1.0`; 1.5: −1.0 п.
(пара `l15_R4pair`, 3 бита, стор после RoPE). **Переносится:** поверх верной оси не нужен.

**К2 · К3, цена неверной оси (ошибка тензора, во сколько раз).** V: 1.12 / 1.16 и
1.22 / 1.26 — совпадает. K: 1.75 / 4.09 и 1.99 / 4.78 — вдвое; ключи 2.0 без QK-Norm
дают 2.04, рядом с 1.5. **Выбор оси переносится** (K — шкала на канал, V — любая),
**цена ошибки не переносится,** причина — QK-Norm, показано вмешательством внутри
модели. На ответах не проверено.

**К3, выбросы (crest factor).** V: 6.45 / 6.24 и 7.47 / 7.44. K: 6.37 против
12.07 и 11.83; без QK-Norm у 2.0 — 6.10. **Не переносится, тот же механизм:**
нормировка умножает каналы ключа на общие для всех токенов множители, и выбросы
закрепляются в фиксированных каналах.

**К4, ошибка ключей на верной оси (×10⁻²) и пол округления (% решений).** Ошибка у
2.0 на треть меньше: 4.06 против 6.08 при 4 битах (32 полотна), 7.86 против 12.30 при
3. Пол при этом выше: 3.32% против 1.12% и 4.88% против 1.66% — около 3×, `p = 0.007`
и `0.004` по промптам (см. «Статистика К3 и К4»). **Не переносится, механизм открыт.** Не
тензор (ключи 2.0 точнее) и не роутер (K и V вредят поровну, 0.09σ). До ответов в
поставляемом режиме не доходит.

**QuaRot против масштаба на канал для ключей — снято 15 сентября.** `check_key_error
--rotate`: 32 полотна × 6 слоёв, маска 0.50, группа 128, поворот — случайная матрица
Адамара по голове (та же, что `--rotate-qk`). Разбор `stats_theses.py rotate`.

| ошибка K: QuaRot (R4 → шкала на токен) / шкала на канал | 4 бита | 3 бита | 2 бита |
|---|---|---|---|
| LLaDA-1.5 | 1.58× | 1.65× | 1.68× |
| LLaDA2.0-mini без QK-Norm | 1.65× | 1.69× | 1.72× |
| **LLaDA2.0-mini** | **2.00×** | **2.16×** | **2.22×** |
| 2.0 / 1.5 | 1.27× [1.26–1.27] | 1.31× [1.30–1.32] | 1.32× [1.32–1.33] |
| 2.0 с QK-Norm / без, те же полотна | 1.21× [1.21–1.22] | 1.28× [1.27–1.28] | 1.29× [1.29–1.30] |
| V, та же величина: 1.5 / 2.0 | 1.07× / 1.10× | 1.08× / 1.11× | 1.10× / 1.13× |

Три вывода.

1. **Направление переносится:** для K шкала на канал точнее QuaRot на обеих моделях и
   всех разрядностях, разрыв растёт с уменьшением бит. Это KVLinC (Llama, Qwen2.5,
   Qwen3, 2 бита), теперь на диффузионных моделях и при 3–4 битах.
2. **Размер не переносится, и его задаёт QK-Norm:** с нормировкой разрыв в 1.21–1.29
   раза больше; без неё 2.0 от 1.5 отличается на 2–4% — столько же, сколько V. Та же
   картина, что у цены оси (К3), — это второй независимый замер одного механизма.
   KVLinC роль QK-Norm не анализировал.
3. **Поворот сильно помогает шкале на токен именно на 2.0** (в 2.35 раза при 4 битах
   против 1.26 на 1.5 и 1.23 на 2.0 без нормы), но с падением разрядности помощь
   тает: 2.02 при 3 битах, 1.33 при 2. Поворот поверх шкалы на канал ошибку не
   уменьшает, а слегка увеличивает (+3…+11%; единственное исключение — 2.0 при 4
   битах, −1.7%) — согласуется с нулём на ответах в паре `l15_R4pair`.

Для V шкала на канал тоже немного точнее, чем R4 со шкалой на токен (7–13%, одинаково
на обеих моделях). Это расходится с рекомендацией KVLinC для V (повёрнутые значения
по токенам); сравнивать осторожно — у KVLinC другие модели и оценка на перплексии, а
не на ошибке тензора.

**R4 поверх шкалы на канал на LLaDA-1.5 — снято 14 сентября.** Пара `l15_R4pair`:
3 бита, поставляемый режим, стор после RoPE (`--kv-rope post`), шкала на канал —
82.5% без поворота, 81.5% с поворотом. Разница в две задачи: при таком сдвиге
McNemar не опускается ниже 0.5 ни при каком раскладе. Вместе с LLaDA2.0-mini (−0.5,
`p = 1.0`) — **переносится: поверх верной оси R4 не нужен.** Вопрос «R4 или шкала
на канал» это не закрывает — для него `check_key_error --rotate`.

**Одна группа на весь префикс на LLaDA2.0-mini** (`l20_G1_*`): 90.0% при 4 битах и
89.0% при 3, против 90.5% у кэша 16 бит и 88.5% у динамики с группой 128. Вместе с
1.5 (86.0 и 82.5) — переносится, на обеих бесплатно.

| действие | LLaDA-1.5 | LLaDA2.0-mini | итог |
|---|---|---|---|
| держать префикс весь блок | −1.5 п., `p = 0.66` | 0, точно | переносится |
| заморозить текущий блок | −80.5 п. | −89.0 п. | переносится |
| кэш 4 и 3 бита | `p = 0.83`, `1.0` | `p = 0.61`, `0.45` | переносится |
| статика K; K и V | 0…+3.5 п. | −3.5…+1.5 п. | переносится |
| ось V | 1.12–1.22× | 1.16–1.26× | переносится |
| QuaRot вместо шкалы на канал, ошибка K | 1.58–1.68× хуже | 2.00–2.22× хуже | направление переносится, размер — QK-Norm (без неё 1.65–1.72×) |
| R4 поверх шкалы на канал (не QuaRot: тот группирует по токенам) | −1.0 п. (3 бита), `p ≥ 0.5` | −0.5 п. (4 бита), `p = 1.0` | переносится |
| одна группа на весь префикс | 86.0 / 82.5 | 90.0 / 89.0 | переносится |
| ось K | 1.75–1.99× | 4.06–4.71× | не переносится — QK-Norm, частное 2.32× [2.30–2.33] |
| crest K | 6.37 | 11.83–12.07 | не переносится — QK-Norm |
| пол округления | 1.12–1.66% | 3.32–4.88% | не переносится — открыто, ≈3×, `p = 0.004` |

**Итог.** Всё, что касается точности на ответах, переносится, и рецепт один на обе
модели. Не переносится чувствительность к ключам: цена оси объяснена QK-Norm, пол
округления нет. Механизм К3 (множитель QK-RMSNorm создаёт выброс K) описан в HiFA4
на Qwen3-8B; своё здесь — цена для оси кэша, вмешательство внутри модели и все слои. Интервалы для оси и тест для пола посчитаны выше; одиночные `p` между
отдельными прогонами на ответах занижены — см. предупреждение выше.

#### R4 на ответах: на MoE меряно, на плотной — нет

> **Отозвано 11 сентября, в тот же день, что записано.** Здесь стояло «R4 до
> ответов не доходит — ноль расхождений из двухсот», по паре `l15_C4` против
> `l15_C4_r4`. Ноль оказался не результатом, а признаком того, что **поворот на
> плотном пути не применялся вовсе**. Две независимые причины, каждой хватает:
>
> - R4 живёт в хуке `sdpa` (`llada_local.py:189`), а хук ставит
>   `install_window_store`, который вызывается только при `reuse_window=True`
>   (`llada_local.py:411`). В канонической точке хука нет, а `evaluate.py` всё
>   равно печатает «R4 installed»;
> - даже с хуком префикс поворота не получает. Порядок в `LLaDABlock.attention`
>   такой: `k = cat(past_key, k)`, затем `present = (k, v)`, и **только потом**
>   `q, k = self.rotary_emb(q, k)`. Стор держит K **до RoPE**, а поворот
>   накладывается после деквантизации: округление он изменить не может, а `q·k`
>   при ортогональном повороте не меняется.
>
> Та же оговорка задевает строку «что делает R4 — **ничего**» для LLaDA-1.5 в
> §2.5a: там зонд шёл с `reuse=True`, значит окно поворот получало, а префикс —
> нет. На двунаправленной модели стареет как раз префикс, так что замер был
> применён наполовину и требует пересъёмки.

**На LLaDA2.0-mini замер настоящий, и он отрицательный.** `l20_C4` против
`l20_C4_r4`, обе кэш 4 бита, `block`, окно свежее, снятые подряд на одной
карте: **89.00 против 88.50**, 10 против 9, `p = 1.0`.

Девятнадцать ответов сдвинулись, в сумме ноль. То есть падение разрыва осей с
+41 до +5 и уполовиненный пол округления (§2.8a) живут на решениях и выигрыша
на задаче не дают. Объяснение то же, что и раньше: при оси `token` и группе 128
округление и так бесплатно, а R4 чинит режим, в который мы модель не ставим.

**Предписание: R4 нужен там, где выбрана плохая ось, а на верной не нужен.**
На плотной модели это утверждение пока не проверено — проверять надо после
того, как поворот туда действительно доедет.

#### Взаимодействие осей измерено, и оно есть

Цена четырёх бит зависит от того, присутствует ли устаревание:

| режим | 16 → 4 бита на кэше | McNemar |
|---|---|---|
| канон, окно свежее | 83.50 → **84.50** (лучше) | `p = 0.83` |
| живое окно, `every_n:4` | 55.50 → **45.00** | **`p = 0.015`** |

**Округление бесплатно в одиночку и стоит десять пунктов в присутствии
устаревания.** Это ровно тот механизм, который §5 объявлял непроверенным:
запас зазора съеден первым возмущением, и второе достаёт до ответов. На
LLaDA2.0-mini то же сравнение даёт 2 пункта при `p = 0.73` — эффект плотной
модели, не общий.

#### Воспроизводимость: прогоны совпадают поответно, один старый выбивается

Три прогона FP16 без кэша с **идентичным** конфигом (полотно 512, 256 шагов,
200 задач):

| файл | дата | верных | против соседей |
|---|---|---|---|
| `l15_A_nocache` | 8 сен | 166 | — |
| `l15_fp16` | 7 сен | 166 | **0 расхождений**, 2 разных `pred` из 200 |
| `l15_nocache_fp16` | 1 сен | 167 | 7 расхождений, 17 разных `pred` |

**Два прогона за разные дни совпали поответно**, и это же видно на других
парах: `l20_C4_r4` на карте 4 против того же конфига на карте 1 — ноль
расхождений; `l15_I` против `l15_I4` — ноль.

> **Прежде здесь стояло «шумовой пол семь вопросов между картами». Снято.**
> Аргументом было, что оба свежих прогона расходятся с сентябрьским на одном и
> том же наборе из семи вопросов. Аргумент пустой: раз два прогона совпадают
> между собой, их различия с третьим совпадают по построению.

Остаётся факт: сентябрьский первый отличается на семи вопросах, и **карта это
или смена кода — не установлено**. Коммит, на который падало подозрение
(`b70f5f2`), ни при чём: он трогает `LLaDAAttentionProbe`, а зонд в генерации не
участвует. Решает это прямой повтор `l15_A_nocache` на другой карте тем же
кодом; до него пары, снятые на разных картах, надо нести с оговоркой, а пары на
одной карте можно читать буквально.

#### Нарезка активаций на 1.5 переснята при совпадающем протоколе

Калиброванный путь, без поворота, кэш 16 бит в канонической точке, 200 задач —
то есть ровно протокол 2.0. Это закрывает клетки, которые §6.1 объявил
невалидными.

| | группа 128 | по токенам | разрыв между нарезками | база → g128 |
|---|---|---|---|---|
| LLaDA-1.5 | 75.50 | **0.00** | **75.5 п.** | −8.0, `p = 0.020` |
| LLaDA2.0-mini | 84.50 | 5.50 | **79.0 п.** | −6.0, `p = 0.023` |

**«MoE хуже переносит поканальную нарезку» не подтверждается.** Прежняя форма
опиралась на 52 пункта у плотной против 79 у MoE, но 52 были сняты **с
включённым QuaRot** и на ста задачах. При совпадающем протоколе разрыв 75.5
против 79.0 — разница в 3.5 пункта, и обе модели при нарезке по токенам
разваливаются одинаково.

**А «при верной нарезке теряют одинаково» подтверждается независимым
измерением:** 8.0 пункта против 6.0, обе значимы, обе на двухстах задачах.

#### QuaRot на плотной модели чинит группу 128 целиком

| LLaDA-1.5, `W4A4`, кэш 16 бит | без поворота | с QuaRot | McNemar |
|---|---|---|---|
| группа 128 | 75.50 | **83.00** | `p = 0.024` |
| по токенам | 0.00 | **26.00** | `p = 4e-16` |

83.00 против базы 83.50 даёт `p = 1.0`: **поворот возвращает всю потерю от
четырёхбитных активаций при верной нарезке**. Нарезку по токенам он поднимает с
нуля до двадцати шести, то есть чинит частично.

Число 26.00 совпадает с прежней клеткой §6.1 (26.0), снятой с поворотом на ста
задачах, — та строка воспроизвелась на двухстах.

**На LLaDA2.0-mini пары пока нет.** `l20_J`: `W4A4`, группа 128, QuaRot, кэш 16
бит — **87.50%**, против 84.50% без поворота и базы 90.50%. Номинально поворот
возвращает половину потери. Прочитать это нельзя по двум причинам: база без
поворота снята кодом от 3 сентября (смена кода на 1.5 давала до десяти вопросов
расхождения, а здесь разница в шесть), и обе клетки калибровались с IA-AQ по
матрице внимания без QK-Norm (поправка к §2.5a). Честная пара — все четыре клетки
2.0 заново, исправленным кодом.

> **14 сентября: первая клетка переснята исправленным кодом, и она ниже.**
> `l20_E_g128` — `W4A4`, группа 128, без поворота, IA-AQ с QK-Norm в зонде, кэш 16
> бит в поставляемом режиме: **80.00%**, против 84.50% у сентябрьской клетки старым
> кодом. Поответно сравнить нельзя — сентябрьского файла с ответами нет. Против
> `l20_J` (87.50%, QuaRot, старый зонд) пара даёт 10 против 25, `p = 0.017`, **но
> читать это как эффект QuaRot нельзя**: клетки различаются и поворотом, и версией
> зонда. Чистая пара требует `l20_G_g128_rot` на текущем коде.
>
> По строке «при верной нарезке модели теряют одинаково» теперь: LLaDA2.0-mini
> 90.5 → 80.0, **−10.5 п.**; LLaDA-1.5 83.5 → 75.5, **−8.0 п.** Порядок тот же,
> межмодельного парного теста нет — задачи одни, но модели разные.

#### Угол «веса плюс кэш» в канонической точке не кусается

| LLaDA-1.5 | | McNemar |
|---|---|---|
| кэш 16 → 4 бита при весах `W4` | 82.50 → 81.00 | `p = 0.72` |
| веса 16 → 4 бита при кэше 4 бита | 84.50 → 81.00 | `p = 0.21` |

Композиция поперёк осей в поставляемом режиме следа не оставляет — как и на
2.0, где угол давал 90.00 против 89.00. Взаимодействие появляется только при
живом окне (см. выше, `p = 0.015`).

#### Хрупкость вопроса — это длина рассуждения

Сравнение задач, потерянных при `every_n:4`, с теми, что выжили (перестановочный
тест, 20 000 перестановок):

| | длина условия | длина полного ответа |
|---|---|---|
| LLaDA2.0-mini | +43 симв., **`p = 0.006`** | +112 симв., **`p = 5e-5`** |
| LLaDA-1.5 | +31, `p = 0.07` | +63, `p = 0.12` |

На MoE эффект уверенный, на плотной — тот же знак без значимости. Устаревание
снимает **длинные цепочки рассуждения**, и это согласуется с тем, что потери
строго вложены по возрасту: при `n:4` на плотной потеряно 63 задачи, и все 63
потеряны и при `block`; на MoE — 90 из 91. Чем длиннее вывод, тем раньше он
теряет запас.

#### У двух источников ущерба разные подписи отказа

| источник | что в хвостах неверных ответов |
|---|---|
| **устаревание** | петли повторов: «Janet Janet Janet», «white white white»; при `block` на 2.0 — 172 вырожденных хвоста из 197 неверных |
| **активации по токенам** | мусор из знаков: «1, 1, 1», «orange, orange»; 123 из 200, и 133 ответа вовсе без числа |
| **то же плюс QuaRot** | беглая неверная арифметика: 6 вырожденных из 148, «Janet makes $26» |

**Устаревание не портит арифметику — оно загоняет декодер в аттрактор
повторения.** Механистически это ожидаемо: замороженные K/V текущего блока
заставляют каждую позицию смотреть на устаревшие представления соседей, и
модель раз за разом предсказывает тот же токен. Квантизация активаций ломает
иначе — сначала распадается текст, а поворот возвращает беглость, не возвращая
счёта.

#### Две модели кэшируют разные тензоры — и сторона RoPE асимметрию не объясняет

LLaDA-1.5 хранит в префиксе K **до RoPE** (`modeling_llada.py` на 84346fd:
строка 692 `present = (k, v)`, строка 697 `rotary_emb`). LLaDA2.0-mini хранит K
**после QK-Norm и после RoPE**. Это настоящий смешивающий фактор, и
`--kv-rope post` (`86a83c1`) его убирает.

> **Снято 11 сентября, в тот же день, что записано.** Здесь стояла гипотеза, что
> асимметрия §2.5a — разрыв осей ≈0 на 1.5 против +41 на 2.0 — объясняется
> стороной RoPE. Литература предсказывает **обратный знак**. KVQuant (arXiv
> 2401.18079, LLaMA-7B, 3 бита): K по токенам 10.87 PPL, по каналам после RoPE
> 7.05, по каналам **до** RoPE 6.23. Верная ось выигрывает 3.82 PPL после RoPE и
> **4.64 до** — то есть кэш до RoPE должен делать ось на 1.5 **важнее**, а не
> безразличнее. Массивные значения в Q/K живут в низкочастотных вращаемых
> измерениях (Jin et al., arXiv 2502.01563), которые на полотне в 512 токенов
> почти не поворачиваются, так что сторона RoPE здесь и не могла дать сорок
> пунктов.

**Частичный RoPE у 2.0 — наследство, не дефект.** `partial_rotary_factor: 0.5`:
вращаются 64 канала из 128, остальные без позиции. LLaDA2.0-mini получена
продолженным предобучением из авторегрессионной Ling-mini-2.0 (arXiv
2512.15745), а Ling 2.0 взяла частичный RoPE «чтобы усилить экстраполяцию на
длину» (arXiv 2510.22115, §2.1). Весь код проекта учитывает его: модель и кэш
через вендорную `apply_rotary_pos_emb`, зонд через `_probe_rotary_dim` с первого
коммита о кэше (`f395dde`, 10 августа).

**А вот QK-Norm зонд не учитывал, и именно она оказалась причиной — см. §2.5a.**
Она в том же абзаце отчёта Ling 2.0, у 2.0 включена, у 1.5 её нет. Пересъёмка
`check_key_error` исправленным зондом: отношение осей K на 2.0 выросло с 2.04× до
**4.78×**, crest — с 6.10 до 11.83, тогда как у 1.5 1.99× и 6.37. Без нормировки
ключи двух моделей неотличимы, с ней — различаются вдвое.

> **Вся сетка снята при полноточных весах и активациях.** `FP16` в строках
> относится к весам и активациям; три и четыре бита — только к `K/V`. Дампы
> зазоров, откуда взяты 62%, сняты так же. То есть это утверждение **про одну
> ось**, и переносить его на режим, где квантованы ещё и веса, нельзя без
> проверки.
>
> **И есть конкретный механизм, по которому оно может не пережить перенос.**
> Ущерб кэша не доходит до ответа потому, что решения, несущие ответ, имеют
> запас зазора, а возмущение кэша переворачивает только те, у кого запаса нет.
> Квантизация весов сжимает зазоры **по всей модели сразу**. Если запас уже
> подъеден весами, то же возмущение кэша может достать до решений, у которых
> раньше был люфт. Это вопрос композиции **поперёк осей** — тот же, что §2.7.7
> решает внутри кэша, — и он не измерен ни разу.
>
> В эту сторону есть одна клетка: `W4A16` при кэше 16 бит даёт 88.50%,
> `p = 0.503` (§6.1). Чего нет — **угла `W4` + кэш 4 бита + `block`**, где обе
> ручки выкручены. Не запускался.
>
> **Формулировать до тех пор надо так:** «при полноточных весах политика
> обновления кэша не меняет ни одного из двухсот вопросов, при том что двигает
> до 62% зафиксированных решений». Придаточное одно, а честность утверждения
> меняет полностью.

**Угол снят 3 сентября, и утверждение переносится.** `w_bits 4`, полотно 512,
n = 200, те же двести вопросов в обоих прогонах:

| | точность | верны обе | разошлось |
|---|---|---|---|
| `W4` + кэш 16 бит, `every_n:1` | 89.00% | — | — |
| `W4` + кэш 4 бита, `block` (возраст 16) | **90.00%** | 176 | **6** (2 / 4) |

`McNemar p = 0.688`. **Обе ручки выкручены, и угол не хуже** — номинально на
вопрос лучше.

**Механизм подтвердился наполовину, и это стоит сказать точно.** Ожидание было,
что веса съедят запас зазора и кэш достанет до ответов. Он достаёт:

| веса | сколько вопросов двигает кэш |
|---|---|
| FP16 | **0** из 200, множества тождественны |
| `W4` | **6** из 200, net +2, `p = 0.688` |

Раньше не двигалось ничего, теперь шесть вопросов. То есть след у механизма
есть и он измерим. **Но вреда нет:** движение симметрично и неотличимо от нуля.

**Итоговая формулировка, уже без придаточного про веса:** политика обновления и
разрядность кэша не стоят точности ни при полноточных весах, ни при
четырёхбитных. Придаточное остаётся одно — про активации, где ось действительно
дорогая (§6.1).

Двигает что-то только разрядность, и мало: 16 → 4 переставляет двадцать вопросов
местами при том же счёте (`p = 1.0`), 16 → 3 стоит полтора пункта при
`p = 0.61`.

**Решения, которые двигаются, это не те решения, которые несут ответ** — и
теперь это сказано в сильнейшей форме, какая доступна: не «разница внутри шума»,
а тождественные множества.

#### Прямое измерение параллелизма: две ошибки в разных валютах

**3 сентября, `check_parallelism.py`.** Прокси заменён прямым измерением.
Сэмплер снят с фиксированного расписания и фиксирует всё, в чём уверен выше
0.9, так что **число шагов на блок стало наблюдением**. 128 токенов, блоки по
32, 8 промптов.

| биты | `n:1` | `n:2` | `n:4` | `n:8` | `block` |
|---|---|---|---|---|---|
| 16 | **18.22** | 21.78 | 23.03 | 31.06 | 31.91 |
| 4 | 17.94 | 20.31 | 24.66 | 29.84 | 31.88 |
| 3 | 19.16 | 20.44 | 24.53 | 31.53 | 31.91 |

**Строки разошлись — но по устареванию, не по разрядности.**

**Устаревание стоит 1.75×.** 18.22 шага против 31.91 на блок из 32 токенов, то
есть 1.76 токена за шаг против 1.00. Контроль — 16 бит при `every_n:1`, где
блочно-каузальный префикс делает кэш точным по построению.

**Округление не стоит ничего.** Разброс между 16, 4 и 3 битами внутри политики:
1.22 / 1.47 / 1.63 / 1.69 / **0.03** шага, и он немонотонен — четыре бита
местами быстрее шестнадцати. На `block` три разрядности сходятся до сотых.

> **У двух ошибок разные валюты, и это переворачивает практический вывод.**
> Округление бесплатно **по обоим счётам** — ноль вопросов на GSM8K (§5) и ноль
> шагов здесь. Устаревание бесплатно по точности и стоит **1.75× по времени**.
> Значит: **сжимать агрессивно, обновлять часто.** Ровно наоборот тому, как это
> обычно настраивают — реже обновляют, чтобы сэкономить счёт, и держат
> разрядность высокой, чтобы не потерять качество. Экономить надо на битах, а
> тратить на обновление.

**И прокси оказался не только круговым, но и завышенным в семь раз.** §2.7.4
считал долю позиций выше порога и получал падение 58.6% → 4.5%, то есть
«втринадцатеро меньше коммитов за шаг». Прямое измерение даёт **1.75×**. Ссылки
на тринадцатикратность отозваны.

**Порог проверен, число устояло.** Прогон при 0.95 — том, что чекпойнт берёт по
умолчанию:

| порог | точный кэш | `block` | отношение |
|---|---|---|---|
| 0.90 | 18.22 | 31.91 | **1.75×** |
| 0.95 | 20.19 | 31.97 | **1.58×** |

Заявлять надо **диапазон 1.6–1.8×**, а не точку. Отношение падает с ужесточением
порога, и это ожидаемо: строгий порог замедляет и точный кэш, сжимая разрыв.
Безразличие к разрядности сохранилось — разброс между 16, 4 и 3 битами внутри
политики 0.03–2.69 шага, немонотонный, и на `block` три ширины сходятся до 0.03
при обоих порогах.

#### И оно переносится на плотную модель

`check_parallelism.py --model-type llada`, LLaDA-1.5, порог 0.9, те же 8
промптов и блоки по 32:

| | `n:1` | `n:2` | `n:4` | `n:8` | `block` | штраф |
|---|---|---|---|---|---|---|
| **LLaDA-1.5**, 16 бит | **13.00** | 14.19 | 18.88 | 20.34 | 21.16 | **1.63×** |
| **LLaDA2.0-mini**, 16 бит | 18.22 | 21.78 | 23.03 | 31.06 | 31.91 | **1.75×** |

**Обе валюты ведут себя одинаково на двух архитектурах.** Устаревание стоит
1.63× против 1.75×; округление снова не стоит ничего — разброс по разрядности
внутри политики 0.03–1.75 шага, и при `block` четыре бита дают **18.50** против
21.16 у шестнадцати, то есть «быстрее», что бывает только шумом.

> **Оговорка, и она смещает число в одну сторону.** У LLaDA-1.5 внимание
> двунаправленное, и префикс стареет **независимо от политики** — колонки
> `hit`/`age` в `check_block_reuse` стоят на 99.9% и 15.5 во всех строках. Значит
> `every_n:1` здесь **не безошибочная точка**, в отличие от MoE, где
> блочно-каузальная маска делает её точной по построению. Строка 13.00 уже
> содержит устаревание префикса, поэтому 1.63× — отношение между политиками, а
> полная цена устаревания на плотной модели **выше измеренной**.

Заявлять с этим вместе: **1.6–1.8× на двух архитектурах и трёх порогах**, и это
нижняя граница на плотной.

#### И штраф переживает квантизацию весов

Оговорка была в том, что вторая половина тезиса — про время — снималась только
при полноточных весах, тогда как первая, про точность, проверена и при
четырёхбитных. `check_parallelism.py --w-bits 4`, RTN, 224 слоя, LLaDA-1.5:

| кэш | `n:1` | `n:2` | `n:4` | `n:8` | `block` | штраф |
|---|---|---|---|---|---|---|
| 16 бит | **14.19** | 14.56 | 15.34 | 15.69 | 23.91 | **1.68×** |
| 4 бита | 14.34 | 14.19 | 15.47 | 16.59 | 22.19 | 1.55× |
| 3 бита | 13.69 | 13.84 | 13.59 | 14.91 | 23.50 | 1.72× |

**1.68× против 1.63× при полноточных весах** — держится. Четыре измерения на
диапазон: 1.75 (2.0, порог 0.90), 1.58 (2.0, порог 0.95), 1.63 (1.5, FP16),
**1.68 (1.5, W4)**.

Разрядность кэша снова не стоит ничего: разброс внутри политики 0.65–1.88 шага,
немонотонный, и при трёх битах строки местами **быстрее** шестнадцатибитного
контроля (0.96×).

> **Одно наблюдение, которое не стоит переоценивать.** Форма кривой изменилась:
> при полноточных весах подъём был плавным (13.00 → 14.19 → 18.88 → 20.34 →
> 21.16), при четырёхбитных промежуточные политики почти бесплатны, а весь скачок
> собран в `block` (14.19 → 14.56 → 15.34 → 15.69 → 23.91). Восемь промптов на
> строку — читать это как эффект рано.

**Клетка на 2.0 снята, оговорка закрыта.** `--w-bits 4`, RTN, 14692 слоя:

| кэш | `n:1` | `n:2` | `n:4` | `n:8` | `block` | штраф |
|---|---|---|---|---|---|---|
| 16 бит | **19.56** | 25.41 | 25.38 | 31.91 | 32.00 | **1.64×** |
| 4 бита | 20.44 | 22.78 | 29.62 | 29.72 | 29.84 | 1.46× |
| 3 бита | 23.47 | 25.53 | 27.66 | 31.03 | 32.00 | 1.36× |

**Полный квадрат, порог 0.9:**

| | FP16 весов | `W4` |
|---|---|---|
| LLaDA2.0-mini | 1.75× | **1.64×** |
| LLaDA-1.5 | 1.63× | **1.68×** |

Диапазон **1.63–1.75×** на двух архитектурах и двух разрядностях весов, плюс
1.58× на 2.0 при пороге 0.95. Обе половины тезиса стоят на одном основании.

#### Но при квантованных весах разрядность кэша перестаёт быть бесплатной

Заголовочное «округление не стоит ничего по обоим счётам» требует границы.
Строка `every_n:1` — чистое обновление, устаревания нет вовсе, так что всё, что
там видно, есть цена одной разрядности:

| веса | кэш 16 | кэш 4 | кэш 3 | |
|---|---|---|---|---|
| FP16 | 18.22 | **17.94** | 19.16 | разброс 1.22 шага, **немонотонно** |
| `W4` | 19.56 | 20.44 | **23.47** | разброс **3.91 шага, монотонно** |

**Три бита при четырёхбитных весах стоят 1.20× пропускной способности.** При
полноточных не стоили ничего — там четыре бита выходили даже быстрее
шестнадцати, что бывает только шумом.

> **Это та же интеракция, что видна на точности, и в той же стороне.** При FP16
> кэш не двигал ни одного вопроса из двухсот; при `W4` двигал шесть. Веса
> выедают запас зазора, и кэш начинает доставать — теперь и во времени тоже.
>
> **Правило получает границу:** «сжимать агрессивно, обновлять часто» верно при
> полноточных весах. При четырёхбитных три бита кэша уже платят, и распределять
> бюджет надо между тремя осями, а не двумя. Четыре бита при этом остаются
> дешёвыми в обеих клетках.

#### Адаптивная политика: реализована, запущена, ничего не покупает

`mask_ratio` — политика, обновляющая кэш по доле маски, а не по счётчику шагов —
была объявлена в `KVCacheConfig` с самого начала и **ни разу не запускалась**.
Запущена 3 сентября, на обеих моделях.

| | средний возраст | ущерб, 4 бита | ближайшая фиксированная |
|---|---|---|---|
| LLaDA2.0-mini | 5.4 | **20.9%** | `every_n:8` при возрасте 4.0 даёт **16.4%** |
| LLaDA-1.5, ось `token` | 15.5 | **14.3%** | `every_n:8` даёт **12.1%** |

**Адаптивность не покупает ничего сверх своего среднего возраста.** На MoE она
ложится между `every_n:8` и `block`, ровно туда, куда легла бы фиксированная
политика с тем же возрастом, и при этом чуть хуже неё. На плотной то же самое.

Одна аномалия, которую нельзя читать как результат: на 2.0 `mask_ratio` —
единственная строка, где три бита лучше четырёх (19.5 против 20.9). Формально
это область компенсации по §2.7.7, но разница 1.4 пункта на 512 позициях внутри
шума, а при возрасте 16 компенсации нет вовсе. Развести можно только числом
промптов.

> **Четыре имени политик — три поведения.** `never` и `block` возвращают из
`should_refresh` одно и то же: обновление на границе блока и никогда внутри.
Имя `never` вдобавок неверное — она обновляет, просто не внутри блока. Значит
каждая сетка, гонявшая `block`, гоняла и `never`, и считать `never`
непроверенной политикой нельзя. Оставлена как алиас, а не удалена: в записанных
на поде конфигах строка может встречаться, и конфиг, переставший грузиться, —
дыра в воспроизводимости.

**Это отрицательный результат про свою же ручку**, и его стоит нести рядом с
> §5: не только фиксированные интервалы не меняют ответов — адаптивный по доле
> маски не улучшает даже зазоры.
>
> **И на задаче он тоже ничего не меняет — переснято 7 сентября.** `mask_ratio`
> при кэше 4 бита, полноточные веса, полотно 512, те же двести вопросов:
> **89.00%**, 178 из 200. Против `every_n:4` при 16 битах: 172 обе верны, 9
> только база, 6 только `mask_ratio`, **McNemar p = 0.607**. Пятнадцать
> разошедшихся объясняются сменой разрядности, а не политикой — §5 мерил 16 → 4
> отдельно и получил двадцать при том же счёте.
>
> Сравнение двухфакторное, и это надо нести: база 16 бит, `mask_ratio` на
> четырёх. Однофакторным его сделал бы прогон `4 бита / every_n:4` со списком
> ответов. Вывод не меняется — **ни один из двух факторов не виден**.
>
> **Что это даёт §5.** Утверждение звучало про **семейство интервалов** — честно,
> но узко, потому что все политики на задаче были оттуда. Теперь проверена и
> политика, решающая по состоянию полотна, а не по счётчику шагов, так что
> придаточное можно снимать. Критерий «сколько осталось масок» о состаривании
> конкретных записей не знает ничего, а §2.7.7 говорит, какой критерий был бы
> правильным: вклад записи в дефицит. Селективные политики по дрейфу уже есть
> (**Polestar**, arXiv 2607.14107), и наш вклад там — не предложить их, а дать
> величину, которой их можно проверить.

**Оговорки.** Наблюдений на строку — 8 промптов × 4 блока = 32, поблочная
дисперсия не выведена, так что 1.75× против разброса в 1.5 шага велико, но
формально не протестировано. И абсолютный параллелизм низкий: даже точный кэш
даёт 1.76 токена за шаг из тридцати двух возможных, поэтому 1.75× — это
**относительный** штраф, а не тот масштаб ускорений, которым торгуют Fast-dLLM
и dInfer.

#### Значит цена есть, но не в точности

Это не отменяет измерений на зазорах — они верны и точны, — а определяет, в чём
их валюта. Валюта измерена **прямо**, выше в этом разделе: устаревание стоит
**1.75×** по числу шагов, округление — **ничего**.

> Прежде здесь стояло «в тринадцать раз меньше токенов за шаг», взятое из доли
> позиций выше порога (58.6% → 4.5%, §2.7.4). Это была не та величина: доля
> падает в тринадцать раз, а **шаги растут в 1.75**, потому что блок двигает и
> одна уверенная позиция. Отозвано.

> **Устаревание кэша стоит пропускной способности, а не ответов.** Кэшировать
> можно агрессивно; платишь скоростью декодирования, и это ровно та величина,
> которой область торгует — Fast-dLLM, dInfer и DMax продают параллельное
> декодирование.

#### Но у этого утверждения есть адресат, и это не блочная модель

**Переформулировано 7 сентября после чтения `generate_cache.py` у dInfer.**
`cache_update_iter` там по умолчанию **`None`** — принудительных обновлений нет.
Префикс пишется раз на блок и держится; текущий блок при `dual` пересчитывается
каждый шаг.

А под блочно-каузальной маской **префикс от текущего блока не зависит**, значит
держать его — тождество, а не приближение. **В поставляемой конфигурации
LLaDA2.0 устаревания нет структурно.** Не «мало» — нечему быть.

Значит вся ось устаревания на этой модели описывает режим, которого никто не
включает, и штраф 1.6–1.8× по времени к ней относится тоже.

**На двунаправленных моделях всё наоборот.** У LLaDA-1.5 префикс зависит от
замаскированного хвоста, поэтому любое переиспользование — приближение; Fast-dLLM
так свой механизм и называет, **approximate** KV cache. V-verify у dLLM-Cache и
интервал сброса у dKV-Cache существуют ровно затем, чтобы устареванием
управлять.

И эталона там нет: LLaDA-1.5 поставляется с `use_cache: false` и ассертом
«kvcache is not suppotred for MDM» в `forward` (§1). dInfer — фреймворк
LLaDA2.0, его `cache_update_iter` к 1.5 не относится. Значит на модели, где
устаревание реально, вендорской политики не существует вовсе, а всякий кэш —
конструкция третьих лиц, включая наш. Рабочие точки, которые эти третьи лица
опубликовали, — сброс каждые 4/8/16 шагов и раздельные часы промпта и ответа
(§1). А двунаправленных dLLM большинство: LLaDA-8B, LLaDA-1.5, Dream.

> **Правильная форма утверждения — с адресом:**
>
> **На блочно-каузальной модели префикс точен по построению, поэтому единственная
> ошибка кэша — округление, а округление бесплатно по обоим счётам.** Сжимать до
> четырёх бит и не думать.
>
> **На двунаправленной обе ошибки живы**, устаревание неизбежно, и тогда работает
> закон композиции: он говорит, какую из двух покупать, а какую нет.

Так у каждой половины появляется свой адресат, а закон композиции перестаёт быть
про наши две ручки и становится инструментом для тех, у кого обе ошибки
вынужденные.

**Что при этом не задето.** Пол округления, ось группировки, R4, тензорные
сравнения, разрядность как функция траектории — всё снято при `every_n:1`, то
есть **в рабочем режиме**. И сам закон композиции: он проверен вне выборки на
паре `K` × `V`, где устаревания нет вовсе.

Точность по-прежнему подавать как то, что **не** страдает — это объясняет, почему
поставляемые декодеры кэшируют агрессивно и не жалуются на качество.

**Оговорка, которую надо нести вместе с этим.** Двести задач при 90.5% — один
вопрос стоит полпункта, и потолок близко. Эффект в пару пунктов такая метрика
показала бы; эффект в доли пункта — нет. Утверждение «цены нет» верно на этом
полотне и этой задаче, и переносить его на длинные генерации без проверки нельзя.

#### Устаревание до возраста 2 не стоит ни одного вопроса

`--kv-policy` / `--kv-refresh-every`, полотно 512, 200 задач, кэш 16 бит:

| политика | средний возраст | точность | решений сдвинуто на зазорах |
|---|---|---|---|
| `every_n:1` | 0 | **90.50%** | 0.0% |
| `every_n:4` | 2 | **90.50%** | **11.1%** |

**Множества решённых задач совпадают полностью — ноль разошедшихся вопросов из
двухсот.** Не «в пределах шума»: одиннадцать процентов зафиксированных решений
двигаются, и ни один ответ не меняется.

> **Та же поправка, что выше: эти две строки — один прогон, снятый дважды.**
> `--kv-policy` до пути задачи не доходил, поэтому «ноль разошедшихся» здесь
> ожидаем тождественно. Одиннадцать процентов сдвинутых решений сняты другим
> инструментом, в другом режиме. Строка держится как описание канонической
> точки и не держится как перенос оси устаревания на ответы.

Четыре бита при том же интервале дают те же 181 из 200, но **двадцать вопросов
расходятся**, ровно по десять в каждую сторону (`p = 1.0`). То есть разрядность
меняет маршрут и не меняет счёт, а устаревание при возрасте 2 не меняет даже
маршрута на уровне ответов.

#### И контроль не воспроизвёл беcкэшевый прогон — ровно как предсказано

`every_n:1` при 16 битах обязан быть точным: под блочно-каузальной маской
префикс не зависит от текущего блока, а шестнадцать бит не округляют. Он даёт
90.50% против беcкэшевых 91.50% — **два вопроса из двухсот**.

Раз `n:1` и `n:4` совпали между собой идеально, разница не в политике, а между
«через кэш» и «без кэша». (Посылка оказалась тривиальной: политика на этом пути
не действовала, см. поправку к сетке выше. Вывод от этого не меняется —
сравнение «через кэш против без кэша» варьировало именно то, что называет.)
А §4.1 уже измерил, чем она может быть: на MoE
переиспользование префикса **не побитово воспроизводимо**, `stale(prefix)`
0.010–0.053 без закрепления маршрутов, и причина — диспетчеризация экспертов.

**Цикл замкнулся:** аномалия, найденная на тензорах, проявилась на задаче в
предсказанном размере — один пункт, два вопроса. Утверждение «префикс точен по
построению» остаётся верным про маску и неверным про арифметику, и цена этого
теперь известна в единицах задачи.

**Переиспользование кэша не стоит ничего измеримого.** Сильнейшая форма этого
утверждения — не сравнение с беcкэшевым прогоном, а сравнение **разрядностей
между собой**: шестнадцать бит и четыре дают **181 из 200 оба**, при двадцати
разошедшихся вопросах и балансе десять на десять, `p = 1.0`. Здесь протокол
заведомо один, так что расхождение выше эту строку не задевает.

Против беcкэшевого прогона знак не установлен. Августовские 93.00 отозваны
(§5), рабочее число — **90.50**; но отзыв знака не даёт, потому что расхождение
двух прогонов одной конфигурации не объяснено, а значит и 90.50 нельзя
предъявлять как «кэш дороже на пункт». Заявлять надо цену **разрядности**, а не цену кэша как такового.

Содержательно: те ~5% фиксируемых решений, что двигаются при 16 битах (§2.4,
контрольная строка 94.79% вместо 100%), **до ответа не доживают** — позиция
остаётся под маской и решается заново с обновлённым контекстом.

**Ускорение 3.6–6.7 раза, по загрузке узла.** На пятидесяти задачах вышло 6.7×,
на двухстах 3.6× — и видно почему: за прогон темп уполз с 55.6 до 82.5 с на
задачу, то есть карту поджимали соседи. Писать надо диапазон, не одно число.
Практически это и делает фазу D подъёмной.

**Четыре бита: три новые ошибки из пятидесяти**, ни одной исправленной. Точный
тест p = 0.25 — **не установлено**. Прогон на двухстах идёт.

Один случай стоит прочесть целиком (задача про ботинки при 4 битах):
рассуждение верное до последней строки, «Therefore, the boots cost $104», а
зафиксировано **109** — ответ соседней задачи из того же набора. Ровно тот режим
отказа, который предсказывают метрики: сдвинулся argmax на фиксируемой позиции,
переписать нельзя.

---

## 6. Веса: фон, а не предмет

### 4.4. Роутер усиливает некоррелированное возмущение, и это сшивает обе линии

`check_dispatch.py`, полотно 160, три доли маски. Активации квантуются на лету
до четырёх бит, **веса не трогаются** — солвер не нужен, и подозреваемый один.
**Роутер оставлен в fp16 намеренно:** возмущать сам вентиль значило бы двигать
раздачу по построению; вопрос в том, хватает ли возмущения всего остального.

Хватает — и это опровергает обоснование, на котором стоит политика этого
проекта. «Роутер не квантуем, он 0.06% модели, зато адресация сохраняется»:
первая половина верна, **вторая нет**. Неквантованный вентиль не защищает
раздачу, потому что портится не он, а то, что в него приходит. Совет держать
роутер в полной точности остаётся правильным, но он закрывает один канал порчи
из двух, а измеримый ущерб идёт по второму.

| активации | доля маски | согласие экспертных наборов | худший слой |
|---|---|---|---|
| по токенам | 1.00 | **31.7%** | 19.0% |
| по токенам | 0.50 | 51.5% | 41.8% |
| по токенам | 0.00 | 66.3% | 54.0% |
| группа 128 | 1.00 | **69.0%** | 54.9% |
| группа 128 | 0.50 | 76.9% | 70.2% |
| группа 128 | 0.00 | 83.3% | 79.0% |
| случайная пересдача | — | **3.1%** | — |

Пол измерен, а не предположен: случайная пересдача экспертных наборов даёт 3.1%,
так что 31.7% — это не «треть уцелела», а сильнейшее разрушение при живом поле.

#### Двукратная разница в маршрутах даёт четырнадцатикратную в качестве

Расхождение раздачи: **68.3% по токенам против 31.0% с группой 128.**

> **Отозвано 3 сентября: «85 пунктов против 6» — ошибка колонки.** Здесь
> стояло, что двукратная разница в маршрутах даёт четырнадцатикратную в
> качестве. Восемьдесят пять — ущерб от по-токенной нарезки на 2.0, это верно.
> А **шестёрка взята из колонки «дошли до ответа»** той же строки (6.0%), а не
> из ущерба при группе 128: **клетка `W4A4` с группой 128 на 2.0 никогда не
> снималась** (§6.1, прогон пропал). Ближайшее похожее число — 6.5 пункта — это
> **другая модель**, LLaDA-1.5.
>
> Что остаётся: расхождение раздачи 68.3 против 31.0 измерено, ущерб 85 пунктов
> от по-токенной нарезки измерен. **Связь между ними на 2.0 не количественная** —
> вторая точка отсутствует.
>
> **Восстановлено 3 сентября.** Пропавшая клетка переснята: `W4A4` с группой 128
> на 2.0 даёт **84.50%**, то есть **6.0 пункта** ущерба (§6.1). Теперь связь
> количественная и держится на измерениях с обеих сторон: раздача расходится
> **68.3 против 31.0 — в 2.2 раза**, качество **85.0 против 6.0 — в 14.2 раза**.
> Переворот раздачи компонуется через девятнадцать маршрутизируемых слоёв,
> поэтому связь резко нелинейна.

**Значит поканальная нарезка активаций ломает у MoE не арифметику, а адресацию.**
Форма провала это подтверждает независимо: до ответа доходят **6%** ответов
против 73% у плотной LLaDA-1.5 (§6.1) — так выглядит текст из чужих экспертов, а
не сбившийся счёт.

#### И это диффузионный результат, а не общий про MoE

Согласие **растёт по мере раскрытия полотна**: 31.7 → 51.5 → 66.3.

Причина в том, что при высокой доле маски почти каждая позиция несёт одну и ту
же строку эмбеддинга, скрытые состояния сходятся, и top-k роутера стоит **у
ничьей**. Любое некоррелированное возмущение её переворачивает. Это ровно то,
что `dllmquant/cache.py` формулировал как гипотезу про коррелированность ошибок
округления при высокой маске; здесь она измерена на маршрутах.

**А диффузионная модель проводит в этом режиме начало каждого блока.**

#### Механизм, объясняющий четыре независимо измеренных числа

1. Высокая доля маски → позиции почти одинаковы → решения роутера у ничьей.
2. Некоррелированное возмущение — округление, в активациях или в кэше — их
   переворачивает.
3. Переворот компонуется через девятнадцать слоёв.

Отсюда: **тридцатитрёхкратный** пик дрейфа латентов (§4.3), **ненулевой**
`stale(prefix)` при точной по построению маске (§4.1), **пятикратный до
тринадцатикратного** пол
округления кэша против плотной модели (§2.7.8) и **восемьдесят пять пунктов** от
поканальных активаций (§6.1).

#### То же самое, но возмущён только кэш

`check_dispatch.py --mode cache`. Веса, активации и роутер **не тронуты** —
меняется только содержимое стора, и прочтение идёт тем же оконным форвардом, что
и эталон. Рабочая точка та же, в которой снята вся остальная работа.

| разрядность кэша | доля маски | согласие наборов | худший слой |
|---|---|---|---|
| 4 бита | 1.00 | 91.8% | 71.5% |
| 3 бита | 1.00 | **81.3%** | **53.5%** |
| 4 бита | 0.50 | **84.3%** | 73.0% |
| 3 бита | 0.50 | **77.6%** | 60.9% |
| 4 бита | 0.00 | 94.0% | 90.2% |
| 3 бита | 0.00 | 91.5% | 88.7% |
| случайная пересдача | — | 3.2% | — |

**Квантизации одного лишь кэша хватает, чтобы переадресовать от 6 до 22%
токенов, а на худшем слое до 46%.** Роутер — живой канал для ошибки хранения, а
не только для ошибки в активациях.

#### Зависимость от маски здесь обратная, и это подтверждает механизм

У активаций согласие росло монотонно с раскрытием полотна: 31.7 → 51.5 → 66.3.
Здесь хуже всего **посередине**, при доле маски 0.50.

Объяснение — тот же механизм, применённый к самой ошибке. При полной маске почти
каждая позиция несёт одну строку эмбеддинга, значит `K` у них почти одинаковы —
и **ошибка округления у них тоже почти одинакова**. Когерентная ошибка двигает
все позиции согласованно и ничью переворачивает реже. При 0.50 записи
разнообразны, ошибки независимы, и раздача плывёт сильнее всего. При 0.00
решения просто далеки от ничьей.

`dllmquant/cache.py` предполагал коррелированность ошибок округления при высокой
маске как **фактор риска**. Знак оказался обратным: **корреляция защищает
раздачу**.

#### Чего это ещё не доказывает

Что весь разрыв пола округления с плотной моделью (5–13×) объясняется
роутером — **не доказано**. Есть второй кандидат, архитектурный и не связанный с
маршрутизацией: **GQA**. У LLaDA2.0-mini 16 голов запросов на 4 головы `K`, то
есть одна запись кэша читается **четырьмя** головами; у LLaDA-1.5 32 на 32, то
есть одной.

Разделить можно: у `V` то же разделение голов, но `V` не проходит через софтмакс
и на выбор экспертов влияет иначе. Сравнение «квантован только `K`» против
«квантован только `V`» при равной разрядности отделит усиление разделением голов
от усиления роутером. Прогон короткий, флаги `--kv-key-bits` и `--kv-value-bits`
уже есть.

#### Почему это не противоречит тому, что закрепление режет устаревание

На тензорах закрепление маршрутов снимает **16–29%** колонки устаревания и
0.5–2.6% колонки округления (§4.5). Выглядит как обратное, но говорит о том же:
роутер усиливает не возмущение вообще, а **некоррелированное** возмущение.
Устаревание — когерентный снос всего префикса, он двигает позиции согласованно,
и ничью переворачивает реже; поэтому его вклад в churn измеримый, но
пропорционально меньший, чем у округления.

Колонка округления там, впрочем, вообще не могла показать усиления: она измеряет
`rel(Q(K), K)`, ошибку самого тензора, которая через сеть не проходит.


### 4.5. Закрепление маршрутов на тензорах: треть «устаревания» — это churn

`measure_drift`, четыре бита, задержки 1/2/4, с `--pin-routes` и без. Оба
контроля прошли: нулевой слой плотный и совпал **до пятого знака**, а
`stale(prefix)` при закреплении **0.00000** во всех строках.

| слой, d=1 | устаревание | округление |
|---|---|---|
| 4 | 0.388 → 0.283 = **−27%** | 0.0689 → 0.0683 = −0.9% |
| 8 | 0.391 → 0.277 = **−29%** | 0.0635 → 0.0632 = −0.5% |
| 12 | 0.393 → 0.306 = **−22%** | 0.0610 → 0.0595 = −2.3% |
| 16 | 0.406 → 0.340 = **−16%** | 0.0676 → 0.0659 = −2.6% |

**От шестой части до трети того, что колонка называет устареванием текущего
блока, — это churn маршрутов, а не движение состояния.** Значит `d_возраст` на
MoE систематически завышен относительно «чистого» устаревания, и часть разрыва с
плотной моделью (62.3% против 43.5% на блоке) объясняется им.

**Колонка округления здесь ничего не могла показать, и это ограничение замера, а
не результат.** Она измеряет `rel(Q(K), K)` — ошибку самого тензора, которая
через сеть не проходит; закрепление меняет сохранённый тензор чуть-чуть, вот она
и меняется чуть-чуть. Усиление живёт на решениях, и меряется оно
`check_block_reuse --pin-routes`, а не здесь.

Побочно: косинус между двумя смещениями в свободном прогоне заметно
отрицательнее на глубоких слоях (−0.0517 против −0.0332 на слое 16 при d=1) —
churn добавляет отрицательную корреляцию между ошибками.

### 4.6. На решениях закрепление маршрутов не работает как вмешательство

Проверить усиление напрямую казалось делом одного флага: снять ущерб с
закреплёнными маршрутами и без, и посмотреть, схлопнется ли пол округления к
плотным 0.7%. Не схлопнулся — вырос, и вместе с ним выросло всё остальное.

| | без закрепления | с закреплением |
|---|---|---|
| пол округления, 4 бита | 3.7% | **7.3%** |
| пол округления, 3 бита | 5.4% | **9.6%** |
| `every_n:4`, 16 бит | 11.1% | **16.9%** |
| `block`, 16 бит | 61.9% | **70.3%** |

Причина в самом логе: **зазоры решения обрушились**. По блокам 3.32 / 2.79 /
3.20 / 2.93 логита против 3.37 / 6.44 / 6.27 / 7.25 без закрепления — в блоках
1–3 вдвое.

Маршруты снимаются на полностью замаскированном полотне и держатся весь прогон,
а по мере раскрытия становятся всё менее уместными. Модель идёт по чужим
экспертам, теряет уверенность, и **то же возмущение переворачивает вдвое больше
решений просто потому, что зазор вдвое у́же**.

> **Закрепление маршрутов меняет рабочую точку, а не выключает усилитель.**
> На тензорах это безвредно — там сравниваются нормы одного и того же прогона.
> На решениях сравнивать закреплённый прогон с незакреплённым нельзя вовсе.

Первая попытка была испорчена ещё и грубее — маршруты снимались с нулевого
промпта и навязывались всем восьми, — но и после починки метод остаётся
непригодным для этого вопроса.

**Как проверять правильно.** `check_dispatch` на **кэше**, а не на активациях:
квантовать K/V, записать маршруты, сравнить с неквантованным прогоном. Рабочая
точка не меняется, усиление меряется прямо, и это та же конструкция, которая уже
дала §4.4.
### 6.1. Что измерено

**LLaDA-1.5:** FP16 84.5, W4A16 83.0, W4A4 по-токенно **26.0**, W4A4 с группой
128 **78.0**. Пятьдесят два пункта от одной ручки — размер группы активаций.

> **Протокол этих четырёх чисел отличается от протокола 2.0 по четырём пунктам,
> и все неустранимы без переснятия.** Четвёртый добавлен 7 сентября: в конфиге
> активационных прогонов 2.0 (`w_w4a4_g128.json`) **поля `kv_policy` нет вовсе** —
> оно попало в шапку позже, — так что политика обновления там неизвестна, а числа
> 1.5 сняты до всей работы с кэшем и почти наверняка вовсе без него. Значит по
> политикам эта ось **не мерилась ни разу**: одна клетка на модель, и клетки
> разные.
>
> Провал это не задевает — 85 пунктов и 6% дошедших до ответа политикой
> обновления не объясняются, вся её ось стоит десятки пунктов только при `block`
> и трёх битах. Задевает **сравнение**: пару «6% против 73%» надо нести как
> качественную характеристику двух режимов отказа, а не как одно измерение. Обе строки W4A4 сняты **с включённым QuaRot**
> (`rotation: True` в конфигах дампов) и на **ста** задачах, а база FP16 — без
> поворота и на двухстах; полотно в конфигах тех прогонов не записано вовсе.
> На **внутреннее** сравнение это не влияет: поворот в fp16 точен (инвариантность
> 1.4e-06…5.5e-06 ниже), значит база годится, а разные `n` разбор считает на
> общих вопросах. Но **разрыв 52 пункта нельзя ставить рядом с разрывом на 2.0**
> как одно и то же измерение: там поворота нет.

~~**Разрыв воспроизвёлся на MoE и вырос.** По-токенная нарезка активаций стоит
там **85 пунктов** против 58.5 здесь.~~

> **Отозвано 7 сентября: разрыва нет, он был артефактом поворота.** Строка
> `W4A4` по токенам переснята на 1.5 **без QuaRot**, под протоколом 2.0 —
> полотно 512, шагов 256, 200 задач:
>
> | | база | `W4A4` по токенам | падение |
> |---|---|---|---|
> | LLaDA-1.5, без поворота | 83.50% | **0.00%** | **83.5 п.** |
> | LLaDA2.0-mini, без поворота | 90.50% | 5.50% | **85.0 п.** |
>
> **Обе теряют одинаково.** Прежние 58.5 против 85 держались на том, что у 1.5
> поворот был **включён**, а у 2.0 нет: QuaRot стоил там не меньше двадцати шести
> пунктов, и без него плотная модель падает в ноль.
>
> **Значит «MoE хуже переносит поканальную нарезку» не подтверждается.** Клетка
> переезжает из архитектурных различий (§2.5a) в общее.
>
> **А режим отказа различается, и теперь это измерено при совпадающем повороте:**
>
> | | доходят до ответа | из них верных |
> |---|---|---|
> | LLaDA-1.5 | **43.5%** (87 из 200) | **0** |
> | LLaDA2.0-mini | **6.0%** | 5.5% |
>
> Плотная говорит и считает неверно; MoE перестаёт говорить. Качественное
> различие настоящее, количественного нет.
>
> **И сама эта ретракция снята через сутки — 7 сентября.** Оговорка «прогон 1.5
> шёл через RTN» оказалась не оговоркой, а причиной: снятая тем же путём клетка
> **`W4A16` — только веса, активации нетронуты — даёт 1.5%**.
>
> | LLaDA-1.5, RTN, без поворота, без кэша | точность | падение от 83.50 |
> |---|---|---|
> | `W4A16`, **только веса** | **1.5%** | **82.0 п.** |
> | `W4A4`, группа 128 | 4.0% | 79.5 п. |
> | `W4A4`, по токенам | 0.0% | 83.5 п. |
>
> **Веса через RTN убивают модель сами по себе.** Активации после этого добавляют
> шум на мёртвом: 1.5 → 4.0 → 0.0, порядок даже не монотонный. Значит и «обе
> теряют по восемьдесят четыре пункта», и «разрыв между нарезками четыре пункта, а
> не пятьдесят два» **измерены ни на чём**.
>
> **Состояние оси активаций на 1.5: валидного измерения нет.** Старые числа несут
> четыре расхождения протокола, новые — мёртвые веса. Ни один набор не
> поддерживает кросс-модельного утверждения, ни в ту сторону, ни в другую.
>
> **Что тремя прогонами всё же установлено, и чисто:** на LLaDA-1.5 калибровка
> весов стоит около **восьмидесяти двух пунктов**. RTN на четырёх битах там не
> работает вовсе — что и объясняет, почему прежние числа §6.1 снимались
> калиброванным путём.
>
> **Чем чинится:** те же три клетки без `--rtn`, то есть полным путём CGQ, как на
> 2.0. Дороже — калибровка идёт по гессианам, — но иначе строка не считается.
>
> **Первая клетка переснята 9 сентября, и разрыв подтверждён.** `W4A16`
> калиброванным путём, кэш 16 бит, `block`, окно свежее, 200 задач:
>
> | `W4A16` на LLaDA-1.5 | точность | вырожденных ответов |
> |---|---|---|
> | RTN | **1.50%** | 121 из 200 |
> | **калиброванный CGQ** | **82.50%** | 0 |
>
> **Восемьдесят один пункт за калибровку весов**, и база без кэша здесь 83.00%,
> то есть четыре бита на весах стоят полпункта. RTN на четырёх битах плотную
> модель просто убивает — что и объясняет, почему числа §6.1 всегда снимались
> калиброванным путём. Две оставшиеся клетки (`W4A4` двумя нарезками) в очереди.

> **Отозвано 3 сентября.** Здесь стояло «при верной нарезке модели теряют
> одинаково — 6.5 против 6.0», и из этого делался вывод, что «MoE квантуется
> хуже» неверно как общее утверждение. **Шестёрка была прочитана из колонки
> «дошли до ответа»** строки `W4A4 по токенам` (6.0%), а не из ущерба при группе
> 128. Этой клетки на 2.0 нет вовсе — прогон пропал, см. ниже. 6.5 — число
> **LLaDA-1.5**.
>
> Значит сравнение при верной нарезке было не сделано.

**Прогон переснят 3 сентября: 84.50%, 169 из 200.** `w_bits 4, a_bits 4,
a_group_size 128, rotation false, полотно 512, кэш 16 бит, n = 200`. Клетка
закрыта, и вывод, который пришлось отозвать, **подтверждается независимым
измерением**:

| | FP16 → группа 128 | между нарезками |
|---|---|---|
| LLaDA-1.5 | 84.5 → 78.0 = **6.5** | 78.0 против 26.0 = **52** |
| LLaDA2.0-mini | 90.50 → 84.50 = **6.0** | 84.50 против 5.50 = **79** |

**При верной нарезке модели теряют одинаково — 6.0 против 6.5.** Значит «MoE
квантуется хуже» неверно как общее утверждение; верно узкое: **хуже переносит
именно поканальную нарезку активаций**, 79 пунктов против 52.

> **Отзыв был правильным, совпадение числа — случайность.** Прежние 6.0 читались
> из колонки «дошли до ответа» и опоры не имели; теперь то же значение получено
> измерением. Первое не оправдывает второго.

**Парный тест посчитан 7 сентября, и он меняет формулировку.** Базовая строка
переснята под тот же протокол (`fp16_kv16_st256_n200.json`): конфиги совпадают
во всём, кроме самой квантизации, те же двести вопросов.

| | обе верны | только FP16 | только `W4A4` | McNemar |
|---|---|---|---|---|
| FP16 90.50% против `W4A4` g128 84.50% | 163 | **18** | **6** | **p = 0.023** |

**Шесть пунктов — измеримая потеря, а не шум.** Значит ряд, в который я ставил
эту клетку, был неверен:

| | цена | тест |
|---|---|---|
| веса 4 бита (`W4A16`) | 2.0 пункта | `p = 0.503` — **не отличимо от нуля** |
| веса + активации 4 бита, группа 128 | **6.0 пункта** | **`p = 0.023` — отличимо** |

**Даром только веса.** Активации на четырёх битах стоят измеримо и при верной
нарезке — просто в четырнадцать раз меньше, чем при неверной (6 против 85).
Утверждение «при верной нарезке модели теряют одинаково» остаётся верным как
сравнение двух чисел, но подкреплено парным тестом только со стороны 2.0: на 1.5
протоколы строк не совпадают, и там теста нет.

Обрывов 5 против 9 — четыре лишних в квантованном прогоне при двенадцати чистых
разошедшихся, так что объяснить ими разницу нельзя. Обрывов 9, из них 7 неверных, то есть потолок при
идеальном дописывании 88.0%. И конфигурация этого прогона — полный путь CGQ
(`rtn false, ia_aq true, certainty_weighting true`); если строка «по токенам»
снималась через RTN, разрыв в 79 пунктов смешивает два метода, хотя объяснить
им такую величину нельзя.

**LLaDA2.0-mini, полотно 512, кэш 16 бит, n = 200** — переснято 31 августа,
прежняя пара 77.0 / 74.5 при полотне 256 отозвана (раздел 5):

| | точность | дошли до ответа | ущерб | McNemar |
|---|---|---|---|---|
| FP16 | **90.50%** | 93.0% | — | — |
| W4A16 | **88.50%** | 89.5% | 2.00 | **p = 0.503** |
| W4A4 по токенам | **5.50%** | **6.0%** | 85.00 | p < 0.001 |
| **W4A4 группа 128** | **84.50%** | — | **6.00** | не считан |

**Четыре бита на весах не стоят ничего и на MoE.** `p = 0.503` против
`p = 0.648` у LLaDA-1.5 — две модели, один вывод, обе с парным тестом на одних
и тех же двухстах задачах. Разность в 2 пункта сама по себе ничего не значила
бы при стандартной ошибке 2.1.

**А поканальная квантизация активаций на MoE хуже, чем на плотной.** У 1.5
разрыв между двумя нарезками 52 пункта (78.0 против 26.0), у 2.0 ущерб от
по-токенной нарезки **85 пунктов**. И ломается не арифметика: до ответа
доходят **6% ответов** против 73% у 1.5. Модель перестаёт говорить, а не
считать.

Клетка `W4A4` с группой 128 на 2.0 не снята — прогон пропал, без неё разрыв
измерен только у 1.5.

**Выбросы растут по мере раскрытия полотна:** 4.1 при полной маске → **8.7** при
раскрытом. Поворот сажает на 3.5 независимо от того, откуда начинал. Числа
одинаковы в bf16 и fp32, значит это свойство модели, а не формата. Это
механистическое основание §2.4.

**Голодание экспертов:** при полотне 128 голодало 9 слоёв из 778, при 512 —
**ноль**. Калибровочный бюджет в MoE считается в токенах, дошедших до эксперта,
а не в промптах.

**Поворот верен:** float32, порог 1e-04 — инвариантность **1.4e-06 … 5.5e-06**,
маршрутизация 100% на всех долях маски. Ни одного пропущенного слоя.

**Полный прогон стоит 5.4 часа**, не 20.5: прежняя цифра была следствием
загруженной карты.

### 6.2. Как подписывать артефакт

`out/llada2mini_w4a4` — это **QuaRot + GPTQ на траекторной калибровке**, не CGQ.

Причина найдена арифметикой: отчёт дал 14692 решённых, 276 голодавших, 14316 в
откате, и разность **ровно 100** — столько в модели неэкспертных слоёв (40
проекций внимания + 3 плотного MLP блока 0 + 57 проекций общих экспертов).
Значит взвешивание по уверенности получили все неэкспертные слои и ни одного
экспертного.

`_watch_routers` и `_weights_for_expert` появились 13 августа в 21:18; артефакт
записан 13 августа в 09:45 после 20.5-часового прогона, то есть стартовал
12-го около 13:15 и целиком отработал **до того, как этот механизм написали**.

Это не брак: `quarot_diffusion()` — предусмотренная проектом опорная точка.
Точка CGQ, если нужна абляция, стоит один прогон на 5.4 часа.

---

## 7. Что снято и опровергнуто

Держать в тексте: показывает, что альтернативы проверялись.

**«Размер группы решает»** — артефакт неверной оси (§2.3).

**«Один бит K стоит одиннадцати битов V»** — там же (§2.6).

**Граница частичного RoPE.** Предполагалось, что штраф группы 128 вдоль каналов
идёт от смешивания повёрнутой (каналы 0–63) и неповёрнутой (64–127) статистики.
Тогда группа **64**, ложащаяся ровно на границу, должна была сравняться с 32. Не
сравнялась: 47.92% против 70.83%, а ошибка растёт плавно (2.16e-01 → 2.75e-01 →
3.14e-01) без ступеньки. Значит дело в выбросах, а не в границе.

**Позиционное сравнение маршрутов.** Ранняя метрика считала перестановку внутри
top-k изменением и завышала ущерб в разы, породив ложный порядок по доле маски.
Заменена на сравнение множествами.

**«Роутер — источник ущерба».** Уточнено: усиливает норму, но решения двигает
арифметика (§3.3).

---

## 8. Инструменты

| скрипт | что меряет |
|---|---|
| `check_block_cache.py` | биты × группа × ось × доля маски; асимметрия K/V; пол случайности; зазор решения |
| `measure_drift.py` | разделение устаревания и округления по слоям и задержкам |
| `trace_window_path.py` | послойное расхождение, согласие маршрутов, потерянная масса, гистограмма рангов, закрепление маршрутов |
| `check_router.py` | согласие маршрутов от разрядности **весов** |
| `check_expert_weights.py` | сверка адресации весов уверенности по строкам экспертов |
| `check_static_scales.py` | разброс шкал по каналам между полотнами и долями маски; статические шкалы против динамических при равных **полных** битах |
| `check_lowrank.py` | спектр остатка квантизации по рангам, против «того же бюджета плоским битом»; развёртка ранг × точность факторов |
| `evaluate.py --kv-cache` | GSM8K через кэшированный сэмплер |
| `quantize.py --rotate-only` | повернуть, не квантуя; `--max-blocks N` — остановиться после N блоков |

**Три метрики, без которых числа врут.**

`argmax@k` — доля сохранённых argmax среди позиций, которые реально фиксируются.
Плоский argmax по всем 32 позициям окна завышает ущерб на 10 пунктов: остальные
позиции решаются заново на следующем шаге.

`slots@k` — совпали ли сами эти позиции. Ловит поломку расписания раскрытия,
которой не видно иначе (§2.5).

**Пол случайности** — 16-битный кэш с перемешанными вдоль оси токенов записями:
распределение идеальное, информации ноль. Без него у argmax нет осмысленного
нуля (два прогона одной модели совпадают во многом по априорным предпочтениям).

**Полотно — настоящий текст с дырами.** Ранняя версия заполняла раскрытые
позиции случайными id токенов; модель, читающая бессмыслицу, неуверена во всём,
пол проваливался до 50%, и различать разрядности было нечем.

---

## 9. Что идёт и что дальше

**Идёт:** GSM8K на 200 задачах, четыре бита (карта). Контроль на 16 битах уже
отработал — §5.

### Одна нить через три независимых замера

Стоит назвать её прямо, потому что она связывает фазы B и C и задаёт, что
делать дальше.

**Ущерб сосредоточен, а бюджет тратится равномерно — в этом и вся вилка.**

* **Роутер** (§3): 0.061% параметров решают больше, чем остальные 99.9%.
* **Ось группировки** (§2.1): выбросы K сидят в фиксированных каналах, и группа,
  которая их накрывает, портит шкалу соседям.
* **Остаток квантизации** (§2.6a): почти вся энергия — в **одном канале из
  128**, и первая компонента не привязана к позиции (согласие 6–12%, то есть
  случайность), значит **якорей там нет**.

Отсюда главный вывод обзора, и он не про низкий ранг:

> Лишний бит, выданный **всем** каналам, — заведомо худшее употребление
> бюджета, чем любое прицельное.

### Фаза C: что снято

**Статические шкалы — снято (§2.6b), но как чужой результат.** Калибровка не
стоит ничего измеримого, шкалы уходят из кэша в модель, сжатие
**3.75× → 3.88×** (статика на K) или **4.00×** (на обеих сторонах, там впритык).
Обрезание не мешает: 0.08–0.85% упёршихся записей.

**Новизны в этом нет: это KVQuant** (NeurIPS 2024), вплоть до именованного
бейзлайна «статика на K, динамика на V, группа 128». Идёт в текст как
воспроизведение на MoE-диффузионной модели. **Своё здесь одно** — причина
асимметрии K/V померена в числах (совпадение старших каналов 0.64–0.76 против
0.27), и она же объясняет выигрыш оси `token` у K.

И **отрицательное по самой диффузионной части замысла**: корзины по доле маски
не заработали ничего, одна корзина и пять неразличимы. Экономию даёт калибровка
как таковая. Метод проще, чем задумывался, и заявлять надо это.

**Низкий ранг — обзор снят (§2.6a), и он указал мимо себя.** Остаток
низкоранговый, но ранг — инструмент не той формы: компонента ранга 1 это внешнее
произведение, и она платит за токенный сомножитель, не несущий ничего сверх
того, что уже сказал канал. Прямой соперник — **смешанная разрядность по
каналам**, и на синтетике он бьёт ранг в 1.6 раза при равной точности.

Попутно снято «структура рассасывается с глубиной» (три точки не задают тренд) и
исправлена собственная арифметика цены (шкалы фактора нарезались по токену,
завышая цену вшестеро).

**Якоря — подорваны попутно.** Геометрия первой компоненты остатка не привязана
к позиции ни на одном слое и ни при одной доле маски. Отдельный замер про
подвижность якорей в самом K ещё возможен, но ожидание от него упало.

### Что делать дальше, по убыванию отдачи

1. **Смешанная разрядность по каналам.** Дать самым широким каналам лишние
   биты. Индексы этих каналов — свойство слоя, а не полотна (та же посылка, на
   которой выиграла ось), значит они живут в модели и кэшу не стоят ничего, как
   статическая шкала. На синтетике «1 канал +4 бита» оставляет 0.846 остатка за
   0.031 бита против 0.844 за 0.051 у ранга 1. Это направление, к которому
   привёл обзор низкого ранга, и оно сильнее его самого.
2. **Перенести обзорные выводы на решения.** Всё в §2.6a мерено по энергии
   остатка, а не по `argmax@k` и `slots@k`. При полной маске первый вырожден
   (пол 68.75%), так что нужна развёртка с полом случайности и на промежуточных
   долях маски, а не только на краях.
3. **Кэш на повёрнутой модели.** `--rotate-only` написан, место есть. Мерить на
   парах с малым V (`4,2`, `3,2`): R3 крутит V, а K не трогает вовсе.
4. **Длина блока и положение окна.** Всё снято при длине 32 на последнем блоке.

### Главная незакрытая дыра — накопление между блоками

Всё снято на **одном** окне при чистом префиксе, а генерация — шестнадцать
блоков подряд, где ошибка копится. Четыре бита могут быть бесплатны на одном
шаге и не бесплатны на шестнадцати.

Аналитическая таблица этого не увидит; ловит только оценка на задаче — и
контроль при 16 битах её уже прошёл (§5), так что для самого переиспользования
накопления нет. Осталось проверить то же для четырёх бит, чем идущий прогон и
занят.

Это же совпадает с пунктом «переиспользование текущего блока»: и там, и там
нужен кэш, подключённый к сэмплеру, и замер вдоль траектории, а не на снимке.
Там же живёт единственная ось, где устаревание ненулевое (§4.2), и где, по
измерению, оно перекрывает округление в 4–10 раз — то есть политика обновления
важнее битов.

---

## 10. Две привычки, которые окупились

**Считать раньше, чем запускать.** Обе трудные причины сегодня нашла
арифметика, а не прогоны: вырождение CGQ — разность 14692 − 276 − 14316 = 100,
совпавшая с числом неэкспертных слоёв; падение развёртки — 739 − 224 = 515,
совпавшее с запрошенной длиной RoPE. Четыре получасовых прогона до этого не
дали ничего.

**Не судить по перенесённому порогу.** Дважды проверка срабатывала впустую,
потому что константа была взята из другого измерения (порог инвариантности
поворота, приложенный к логитам). Критерий должен стоять на уровне последствия
— «сдвинулось ли фиксируемое решение», — а не на величине, для которой нет
эталона.
